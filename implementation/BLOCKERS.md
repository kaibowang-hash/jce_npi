# Blockers

No hard blocker is active. Production ERPNext credentials and production activation are explicitly out of scope for implementation and will remain an external activation item, not a development blocker.

