# Decision Log

| Date | Decision | Basis | Reversible / rollback |
|---|---|---|---|
| 2026-07-21 | `GOAL.md` is the sole canonical goal entry; no duplicate `00_GOAL.md`. | Controller compatibility rule | Yes; index-only decision |
| 2026-07-21 | Use controller Phases 0–9 and map Pack M0–M9 tasks into them. | Higher-priority instruction | Yes; trace mapping can evolve |
| 2026-07-21 | Target Frappe v15 LTS-compatible custom app and CSV translations until executable environment verification; pin exact patch in Phase 1. | Stability and Pack localization guidance | Yes; upgrade via ADR/migration |
| 2026-07-21 | React 18 + TypeScript + Vite; local `npi-ui` adapter around Siemens iX Classic Light. | Required architecture and replaceability | Yes; adapter isolates library |
| 2026-07-21 | MariaDB and Redis use isolated local containers; browser uses NPI BFF only. | Frappe compatibility and security boundary | Yes; compose teardown/volume backup |
| 2026-07-21 | ERP integration defaults to Mock; sandbox requires explicit URL/credentials; production endpoints are rejected. | Controller prohibition | Yes; configuration-only activation |
| 2026-07-21 | Use the repository V1.2 Execution Pack as the sole machine-execution baseline; record DOCX differences without expanding or blocking Pack scope. | Latest explicit user direction | Yes; a future approved Pack may reconcile the DOCX crosswalk |
| 2026-07-21 | Require a Codespaces container rebuild for Phase 1.1 instead of installing missing tools into the stale running container. | `make verify-dev-environment` found the runtime does not match the committed reproducible devcontainer | Yes; rebuild uses committed configuration and preserves guarded volumes |
| 2026-07-21 | Treat clone-local Git LFS hooks as invalid residue and remove them; do not add Git LFS to the devcontainer while the repository has no attributes, pointers or LFS-tracked files. Continue using Codespaces' native VS Code Git credential bridge. | Hook, config, history, attribute and non-interactive remote-authentication inspection | Yes; intentional future LFS adoption can add approved attributes and reproducible installation, then regenerate hooks |
| 2026-07-21 | Retain the verified immutable Python image digest; remove `dl.yarnpkg.com` lines from `/etc/apt/sources.list` and delete `/etc/apt/sources.list.d/*yarn*` before package refresh; explicitly disable Yarn APT; lock both official Feature OCI digests; and pin npm 10.8.2. Keep Docker-in-Docker Feature 3.0.1 because the creation log and registry both resolve it and it was not reached by the failed build. | Fresh Codespaces creation log, repository Yarn-requirement scan, MCR/GHCR metadata, Dev Containers lock resolution and official tool registries | Yes; revert the root-cause checkpoint or deliberately update image/Feature locks with the same registry gate |
| 2026-07-21 | Make post-create Docker readiness wait idempotently for up to 120 seconds by default and emit daemon diagnostics before returning a real timeout failure. | The previous fixed 30-second loop could turn normal DinD startup variance into an opaque setup failure | Yes; timeout remains configurable through a positive development-only environment value |
| 2026-07-21 | Install the pinned Vite CLI through the Node Feature's writable `vscode:nvm` global prefix without `sudo`, and fail if that prefix is not writable. | Fresh-target post-create evidence showed that `sudo` sanitized the Node PATH before npm's `#!/usr/bin/env node` launcher ran. | Yes; revert the bootstrap change or use another reviewed user-owned fixed prefix. |
| 2026-07-21 | Verify Docker client/server against the selected semantic version `28.3.3` with only a numeric package-revision suffix; require Compose major v2 and record complete observed versions without describing package/Compose patch values as installation pins. | The locked Feature selects the Docker semantic version but resolves the available Moby package revision and current Compose 2.x patch during build. | Yes; update the selected version and fresh-target evidence together, or adopt a separately approved exact package/checksum installation path. |
| 2026-07-22 | Freeze the Phase 3 frontend/runtime facts at the reviewed lock and Frappe v15 evidence: React 18.3.1, Siemens iX 3.2.0 / iX React 3.2.0 / iX Icons 3.1.1, Frappe 15.115.4 at `a3d8090ba80cb91d3ed72ea90bec67df201db5c1`, and locale codes `en`, `zh`, `zh-TW`. | Lockfile/license/audit review plus the final disposable normal Website User runtime returned 556 direct catalog entries per locale, persisted locale across fresh sessions and proved Administrator isolation. | Yes; any dependency or Frappe/language change requires its ADR, lock/runtime evidence and affected gates to be updated together. |
| 2026-07-22 | Force-regenerate every visual baseline with `--update-snapshots=all` and require clean comparison at `maxDiffPixelRatio: 0`. | The previous percentage tolerance could preserve stale text-bearing screenshots even when a small rendered region changed. Exact update/compare each passed 129/129 and representative current images were reviewed. | Yes; tolerance can change only through an approved evidence-based gate decision, never to conceal a failure. |
| 2026-07-22 | Return the first Phase 3 gate candidate to implementation repair round 1 before activating Phase 4. | Independent review found real localization/Worklist error-context, Frappe CSRF, unexpected-error-envelope and telemetry-route privacy gaps despite the earlier passing happy-path evidence. A candidate decision cannot advance the controller. | Yes; a repaired independent technical `PASS` may record `TECHNICAL_PASS_PENDING_UAT` and automatically activate Phase 4. |
| 2026-07-22 | Keep `implementation/REQUIRED_INPUTS.md` as the only external-input request and treat its missing material as a scoped dependency. | The repository lacks a complete current ERPNext customization baseline, representative data provenance and Phase 3 business sign-off, but NPI-owned domain work, contracts, explicit mocks and tests can proceed safely. | Yes; validated supplied facts can close only their affected holds; production access still requires separate authority. |
| 2026-07-22 | Start Phase 4 with a requirement anchor that resolves Pack allocation before business code; do not treat Phase-4 trace placement as silent authority for portfolio/KPI, portals, live notifications or ERP-owned actual-cost behavior. | `EXECUTION_PLAN.md`, controller state, `ROADMAP.md` M3 and backlog M3 define Project/Gate scope, while some Phase-4 trace rows can imply capabilities owned by later milestones or missing ERP facts. | Yes; an approved Pack crosswalk can remap the rows without changing requirement IDs or inventing business rules. |
| 2026-07-22 | Accept the repaired Phase 3 technical gate as `TECHNICAL_PASS_PENDING_UAT` and automatically activate Phase 4 `P4-00`. | Independent final review found no release-blocking issue after focused repairs and final aggregate/runtime/browser/visual evidence: 58 Python tests, 110 frontend tests, 63 E2E cases, 129 exact visual cases, 556 catalog entries per locale, successful Frappe migration/runtime checks and zero npm audit findings. Named business UAT and provenance-backed sanitized data remain explicitly unsigned. | Yes; Phase 3 can receive full business acceptance when the external evidence is supplied, while a future Phase 4 rollback leaves the Phase 3 technical checkpoint intact. |
| 2026-07-22 | Pass P4-00 and activate P4-01 with a policy-driven Project/Gate boundary: distinguish persisted Domain WorkItems from My Work projections; remap ERP draft/cost to Phase 8 and portfolio/portal/notification extensions to Phase 9; fail closed for eight unresolved production rule packages. | The domain model, OpenAPI and prototype used conflicting WorkItem vocabularies, while numbering, templates, RACI approvals, health, exception eligibility, Gate invalidation and lifecycle authority lack an approved production fact source. Generic versioned infrastructure and explicit synthetic fixtures can proceed without guessing those rules. | Yes; validated business policies can be added as new immutable versions and remapped requirements keep their original IDs and acceptance criteria. |
| 2026-07-23 | Make `npi_tenant_id` an explicit per-Site Project trust boundary: tenant authority comes from trusted Site configuration rather than caller input; a supplied tenant ID must match it; absent or invalid configuration fails closed; and tenant mismatch is rejected without disclosing object existence. | P4-01 has one trusted Frappe Site context but no approved multi-tenant principal-to-tenant mapping; accepting a browser tenant as authority or using a silent default would allow cross-tenant confusion. | Yes; a reviewed multi-tenant mapping may replace the per-Site setting while preserving fail-closed authorization and migration compatibility. |
| 2026-07-23 | Pass bounded P4-01 and activate P4-02 using immutable published-template snapshots, atomic idempotent Project/G0/G1 creation, strict Project BFF contracts, and owner/System Manager access only. Do not claim full FR-PM-001/003/004 completion. | Domain, contract, runtime, permission, concurrency and UI evidence proves the generic Project foundation while production deliverables/roles/duration, complete reference-submission policy, charter fields and the immutable G1 charter baseline remain absent by design. | Yes; later slices add versioned policies and fields without rewriting published templates or historical Project snapshots. |
| 2026-07-23 | Extend the shared Frappe-compatible source catalog to 738 direct entries per `zh` and `zh-TW` locale and globally regenerate exact visual baselines after the live Project copy change. | The catalog hash and shell copy are rendered across the application, so limiting regeneration to the 12 new live Project images would leave stale text-bearing evidence. All 141 cases were regenerated and compared at zero tolerance. | Yes; future literal-source changes repeat the same complete catalog extraction and all-baseline exact comparison. |
| 2026-07-23 | Resolve Domain WorkItem cursors only after Project authorization by deriving their HMAC key from an already-persisted read-only Site encryption key; missing/invalid configuration fails 503 without auto-provision or item query. Require Project-plus-tenant identity for every tenant-bearing related record. | Independent P4-02 security review found that Frappe's password helper can write a missing key, API pre-validation could disclose Project availability, and project-only reference checks admitted same-Project/wrong-tenant corrupted records. | Yes; a future reviewed key-rotation service or tenant model may replace the internal derivation while preserving authorization order, no read-side mutation, signed query binding, and fail-closed isolation. |
| 2026-07-23 | Permit only identity-preserving, start-preserving, non-expansive finite end-dating of an existing disabled Project membership; hold broader role/substitution temporal semantics for authoritative business policy. | A disabled account otherwise could not be closed out, while automatically deleting, extending, or retrospectively rewriting its responsibility history would invent a Class-B rule. | Yes; a future versioned temporal policy can govern new and historical role/substitution relations without rewriting the membership identity or audit history. |
| 2026-07-23 | Pass bounded P4-02 and activate P4-03 while retaining FR-PM-005/006/007 and FR-CO-002 as foundations and FR-PM-009 as verified only within its Project-domain acceptance. | Cumulative aggregate, 63 affected Python, fresh Frappe runtime, P4-02 browser, 147 exact visual, trilingual manual, Task Diff/trace, and independent release reviews passed; production assignment, scheduling, lifecycle, notification, and unified My Work scope remains later or held. | Yes; P4-03 is additive, and a later Phase/PR Level 3 gate can return the branch for repair without relabeling unfinished requirements. |
| 2026-07-24 | Pass bounded P4-03 and activate P4-04 using an independent versioned Gate Template, one-time frozen Project Gate requirements, and append-only exact WBS/private File Revision evidence. Keep decision authority, P0 pass blocking, waiver, reopen/invalidation, future evidence resolvers, uploads, production template contents, and production scanner/ERP behavior out of P4-03. | The Level 3 aggregate, additive and idempotent migrations, complete P4-01/P4-02/P4-03 runtime, 153 non-visual browser cases, 159 exact visual cases, trilingual original review, focused security, trace, and independent release reviews passed. Historical exact Gate Template versions remain readable after their root is disabled; new bindings fail closed. Private File evidence revalidates the exact live File identity and never returns a raw URL. | Yes; P4-04 can add a new immutable versioned review/decision policy without rewriting frozen P4-03 requirements or evidence. Any future normal-user file route or evidence resolver requires its own authorization and retention contract. |
| 2026-07-24 | Stop P4-04 after its read-only inventory and implementation-boundary plan to create the final recoverable Codex CLI-to-Cloud checkpoint. Retain no partial P4-04 product change and do not label the task `PASS`. | The explicit handoff instruction requires completing only the current minimum consistent unit, updating durable recovery truth, committing and pushing, then stopping. The incomplete local transport-role draft did not constitute a coherent or validated slice and was removed before checkpointing. | Yes; Codex Cloud resumes only P4-04 from its committed plan. P4-03 evidence remains valid and unchanged. |
| 2026-07-24 | Retain the tested P4-04 backend foundation for the final Cloud handoff, but exclude late untested core-repository, idempotency/DocPerm, impact-action, and frontend drafts; keep P4-04 `IN_PROGRESS`. | The explicit stop instruction permits completing the current minimum consistent unit, not starting another. Domain, policy/history scaffolds, Gate Shell state, transport role, and strict API/contracts have bounded Level 1 evidence. The late repository draft called undefined methods and lacked tests; the frontend draft had no accepted page/test/localization slice. Catalog generation also reports 265 missing sources per Chinese locale. | Yes; the excluded drafts can be reimplemented from the committed protocol and plan. No production data or accepted P4-03 evidence is changed. |
| 2026-07-24 | Retain the completed current P4-04 implementation/repair batch as an honest CLI-to-Cloud checkpoint, keep P4-04 `IN_PROGRESS`, and do not activate P4-05. Correct the P4-04 `FR-SG-007` scope to successor-cycle invalidation and downstream denial without automatically creating an impact Domain WorkItem; P4-05 owns work/lifecycle projection. | The live repository, sealed actor-bound receipts, permissions, focused runtime, strict Review Room parser, command reconciliation, reconstructable audit UI, 1740-entry trilingual catalogs, 116 backend tests, 93 frontend tests, four affected E2E cases, and three exact trilingual visuals pass at Level 1. The earlier impact-action requirement came from the generated plan rather than authoritative `FR-SG-007`; the complete P4-04 state matrix, Level 2, and triggered Level 3 remain pending and cannot be called PASS. | Yes; the checkpoint is additive and recoverable. A later approved contract may add a work projection in P4-05 without rewriting immutable P4-04 decisions/events; rollback uses the checkpoint parent before retained history exists. |
| 2026-07-25 | Accept the bounded P4-04 domain/implementation foundation after repairing decision-lineage, schema-version, legacy-exception, File-delete dependency, transport-capability, and historical downstream-state defects; preserve P4-03 as `EVIDENCE_CONFIRMED`; create a thread-migration checkpoint with P4-04 still `IN_PROGRESS`. | Targeted reconciliation passed 123 Gate Review Python, 46 shared backend boundary, 11 Gate Evidence/Gate Shell, 116 frontend, 72 live Gate browser, and 23 exact visual cases with 1742 complete direct catalog entries. The final live runtime did not execute because the local Docker/MariaDB prerequisite could not be restored; migration, runtime, Level 2, and Level 3 remain explicitly pending. | Yes; resume the existing additive P4-04 slice after restoring the controlled runtime without resetting its volume. Do not repeat P4-03 Level 3 or activate P4-05 before genuine P4-04 Gate evidence. |
| 2026-07-25 | Resume V1.2 Autopilot Continuous Delivery through P4-04, P4-05, and Phases 5–9; mark every historical thread-local stop/pause/handoff boundary `SUPERSEDED_BY_LATEST_USER_AUTOPILOT_AUTHORIZATION`. | Latest explicit user authorization and `implementation/ACTIVE_EXECUTION_GOAL.md`; repository truth identifies P4-04 as the only active atomic task at synchronized clean HEAD `f3d9c06`. | Yes; a later compatible user instruction may change execution timing, but cannot silently weaken permanent product, architecture, security, localization, ownership, or Gate requirements. |
| 2026-07-25 | Replace the end-of-life Node 18/npm 10 baseline with exact Node 24.18.0 LTS/npm 11.16.0 and upgrade only the lint/test parents needed to resolve every `brace-expansion` path to patched 5.0.8; retain product dependencies and the locked Dev Container Feature digest. | P4-04 Level 3 exposed newly published High-severity GHSA-mh99-v99m-4gvg; ADR-011, official Node release/index metadata, clean npm 11 lock resolution and zero-vulnerability complete/production audits. | Yes, but do not restore an end-of-life or known-vulnerable baseline; replacement requires another supported LTS with the same registry, fresh-target and full-gate evidence. |
| 2026-07-25 | Pass bounded P4-04 and activate P4-05. Treat exact invalidation, successor review cycles, and downstream denial as the P4-04 `FR-SG-007` boundary; create no automatic impact Domain WorkItem and install no production Gate policy. | The complete Level 3 Gate passed 417 Python and 337 frontend tests, 1746 direct trilingual sources, two migrations, all six live runtime lanes, 204 non-visual browser cases, forced and clean 170-case exact visuals, strict Node 24 install/audit/fresh-target checks, Task Diff/trace, security, and independent release review. | Yes; P4-05 may add live work projection and internal activity without rewriting immutable P4-04 history. After retained decisions exist, rollback disables routes and uses a reviewed forward fix rather than deleting history. |
| 2026-07-25 | Pass bounded P4-05 and Phase 4; activate only `P5-00 — Phase 5 requirement anchor for Design, Documents, Baselines, and EBOM`. Keep the My Work index derived and query-time reauthorized; keep template improvements proposed-only; install no production Project-control policy. | The Level 3 Gate passed 587 Python and 492 frontend tests, 2,221-entry direct trilingual catalogs, additive/idempotent Site synchronization, complete cumulative live runtime, a 296-source/184-row/127-active atomic My Work rebuild, fourteen-route disable/recovery, 227 non-visual browser cases, forced and clean 188-case exact visuals, and independent requirement/domain/permission/security/migration/release review. Phase 4 trace closes as 6 verified, 13 verified foundations and 1 partial foundation. | Yes; before retained P4-05 history the task checkpoint may restore a disposable environment. After retained history, set the tested route-disable switch, preserve additive/immutable records and deploy a reviewed forward fix. P5-00 may remap only Pack-approved Phase 5 scope and must pass before P5-01 product code. |
| 2026-07-25 | Pass `P5-00` and activate `P5-01 — Document and design revision` using the Pack's five M4 tasks. Keep Controlled Document, Document Revision and exact File Revision distinct; keep NPI working EBOM separate from ERPNext formal Item/MBOM; treat external retrieval, real Office/CAD preview, CAD/PDM and real ERP execution as explicit held/later acceptance. | `FR-DS-001..014`, M4 Roadmap/backlog, accepted file/integration ADRs, current File Revision and integration foundations, data ownership, OpenAPI, required inputs and independent requirement/security/ownership reviews. The existing generic Execution Request and in-memory reliable-message seed are not accepted as P5-05 completion. | Yes; P5-00 is documentation/trace only. Future Phase 5 records are additive and immutable; before retained history restore the Phase 4 checkpoint, and afterward disable routes/dispatch, preserve history and use a reviewed forward fix. |
| 2026-07-25 | Supersede the 2026-07-21 Pack-only completeness decision with the accepted additive DOCX–Pack reconciliation. Retain 173 original Pack IDs, add 95 DOCX IDs and 13 clarification IDs, and distinguish them with `trace_kind` in one 281-ID machine trace. | User-supplied reconciliation report; deterministic extraction of 229 DOCX requirements; 134 same IDs, 95 DOCX-only IDs and 39 Pack-only normalized IDs; accepted addendum and coverage matrix fixed to checkpoint `930b5a2`. | Yes; mappings may evolve through another reviewed addendum, but original IDs and historical Gate evidence are never erased or relabelled. |
| 2026-07-25 | Insert R1 as a reconciliation/shared-UX bridge while retaining the P5-01 backend checkpoint and all accepted Phase 4/P5-00 evidence. R1-01 permits reconciliation tooling but no product runtime code; the shared design/i18n bridge must pass Level 3 before P5-01 resumes. | Accepted reconciliation sequence and risk of multiplying shared visual drift before Phase 6/7. | Yes; R1 product changes remain additive and can be rolled back before retained branded history. Historical product checkpoints remain intact. |
| 2026-07-25 | Use only `docs/Brand Asset/Brand Asset Instruction.csv` and the exact five supplied SVGs for LaunchFlow brand work. Preserve `NPI_ONE`, `ERPNEXT`, `/api/npi/v1` and other stable internal identities; do not invent a JCE asset. | Explicit user sole-source instruction, supplied asset package and ADR-012. | Yes for the display adapter; historical controlled outputs are forward-corrected, not rewritten. |
| 2026-07-25 | Register the subsequently supplied `Core.png`, its CSV usage rule and the approved `JCE Core` display name as resolution of DR-REC-006, while retaining FR-BR-002 implementation in Phase 8/M7-09 and keeping `ERPNEXT` as the stable internal system code. | Latest user-approved amended plan plus the exact asset and usage instruction added to the sole-source brand package. | Yes before branded JCE outputs exist; afterward use a reviewed forward display change without rewriting historical records. |
| 2026-07-25 | Treat the report's `trial_summary_released` as illustrative, not a contract value. Phase 7 owns the immutable NPI snapshot foundation; Phase 8 owns the final read-only projection contract/event after DR-REC-009. | Existing integration-event schema requires a dotted event type, while release authority, payload version, redaction and consumer mapping are unapproved. | Yes; a later accepted contract can select a dotted event identity without changing Trial truth ownership. |
| 2026-07-27 | Append `FR-UX-043` as a P0 Phase 5 `ADDENDUM_DIRECT` requirement, allocate it to R1-05/UX-A3 and update the current machine trace from 281 to 282 rows (173/95/14), while preserving all historical 281-row R1-01 and earlier Gate evidence. | The user-approved 2026-07-26 amended plan explicitly defines compact icon-first actions and allocates the approved icon adapter to UX-A3; the repository reconciliation predates that plan. DR-REC-005 already selects the existing local iX/company adapter and rejects unapproved Octicons. | Yes; future requirement corrections remain additive. Reverting this independent planning checkpoint restores the prior current trace before product adoption, but accepted historical evidence is never rewritten. |
| 2026-07-30 | Pass R1-05 Stage 3 with a closed local icon-action policy and activate only R1-06. Keep primary, high-risk and ambiguous actions visibly labelled; retain affected Linux visual evidence without rewriting unrelated baselines changed only by current package/font rendering drift. | FR-UX-043 policy/unit/browser/i18n evidence, original-resolution review, exact affected `6/6` digest-pinned Linux visual run, complete repository/non-visual CI and complete `50`-commit PR-branch secret scan. Three historical synthetic false positives are excluded only by exact reviewed fingerprints guarded by repository tests. The diagnostic full visual run showed broad environment drift outside the R1-05 diff, while the accepted historical complete matrix remains immutable evidence. | Yes; the local mappings/consumers are reversible presentation changes. R1-06 may replace the temporary R1-05 visual CI scope with the approved additive 1440×900 governance, but must preserve exact affected verification and may not normalize unrelated baselines without review. |
| 2026-07-30 | Bound R1-06 controlled undo to the existing current-actor My Work closed-view grid reset prototype; keep every business bulk action disabled and full UX-026 bulk acceptance held. Require a real Product Owner approval of the versioned clickable prototype before any reset/undo backend command, while allowing independent 1440 P0 visual governance to continue. | The repository exposes no approved business bulk-status contract or reversibility policy, but already has an authenticated actor-bound grid preference and low-risk reset interaction. UX-030 explicitly requires Product Owner approval before complete backend implementation; technical fixtures, screenshots and automated tests are not that approval. | Yes; Stage 0 is documentation-only. A supplied approval may activate the fixed command stage; a different approved eligible action requires a reviewed re-anchor rather than silently broadening this plan. |
| 2026-07-30 | Pass R1-06 Stage 1 only as technical prototype/governance evidence, retain the Product Owner decision as unsigned, keep Stage 2 fail-closed and activate independent Stage 3. | The exact no-mutation prototype, complete state route, direct three-language copy, accessibility/industrial review, source-bound pending manifest and backend-entry rejection passed local and CI checks. CI #67 also passed the complete repository, 279-case non-visual browser, npm audit, fixed Linux visual and secret-scan lanes. None of that evidence is Product Owner approval. | Yes; the demo-only prototype is removable without retained business data. A real approval tied to the unchanged revision may activate Stage 2; any source/action/state change invalidates the current digest and requires a new review. |
| 2026-07-30 | Pass R1-06 Stage 3 and the Level 2 Task Gate for the currently executable scope; retain the Stage 2 Product Owner approval hold; skip conditional R1-07 because DR-REC-001 remains pending; activate only the cumulative R1 shared Shell/design/i18n Level 3 exit Gate. | The exact current six-P0-screen × three-language 1440×900 registry, density/overflow assertions, strict baseline/workflow verifier, all-image original review and clean digest-pinned CI #70 passed. Stage 1 remains a no-mutation prototype with an unsigned approval manifest, so technical evidence cannot activate the production reset/undo command. DR-REC-001 still explicitly blocks only FR-UX-042/R1-07. | Yes; the prototype and visual governance are additive/reversible and contain no retained business data. A later real approval may activate the fixed Stage 2 command or R1-07 through its own reviewed task, without rewriting this scoped Task Gate. |
| 2026-07-30 | Pass the cumulative R1 shared Shell/design/i18n Level 3 exit Gate, release `R1_SHARED_BRIDGE`, preserve every scoped external-decision hold and resume only P5-01 at its retained backend checkpoint. | The exact synchronized candidate passed CI #72: 763 Python, 634 frontend unit, 279 non-visual browser, 24 fixed-Linux affected visual cases, 2,782 complete direct trilingual sources, both zero-vulnerability audits and both secret scans. The current 231-case visual inventory is completely covered by the accepted 210-case matrix plus every source-affected replacement and 21 additive cases; unrelated renderer-only historical drift was not normalized. API/schema/runtime/migration evidence remains reusable because production backend changes after the final R1-05 Level 3 runtime checkpoint are limited to additive translation CSV rows. Independent cumulative release review found zero blocker/major/minor findings. | Yes; R1 changes remain task-reversible within their recorded immutable-history rules. P5-01 resumes with a comparison audit before frontend/runtime work and is not marked complete. |
| 2026-07-30 | Retain the complete bounded P5-01 backend/domain/DocType/repository/BFF/API/contract checkpoint after the required reconciliation/current-shared-boundary audit. | The dedicated document implementation, nine DocTypes and data ownership were untouched after `930b5a2`; later shared BFF/OpenAPI/catalog changes are compatible additions, and two test edits preserve identical assertions. Focused P5-01 tests passed `63/63`; exact recovery checkpoint `c980571` passed CI #73 with 764 Python, 634 frontend unit, 279 non-visual browser and 24 fixed-Linux visual checks. | Yes; the audit changes only evidence/controller state. Continue only the unfinished P5-01 frontend/runtime slice and do not reimplement or weaken retained invariants. |
| 2026-07-30 | Retain the P5-01 frontend/browser/static-runtime vertical slice as an `IN_PROGRESS` recovery checkpoint, but do not pass P5-01 or activate P5-02 until the fixed controlled Frappe Site result exists. | Strict data sources, dense Project Documents UI, real dirty-state registration, URL-free binary handling, direct trilingual catalogs, complete frontend unit/build/static checks, six browser cases, three exact visuals, the controlled-Site verifier and shared runtime regression tests pass. The real command failed closed before writes because this host has no Docker CLI/daemon/Compose or fixed repository Bench; static and mocked evidence cannot replace two real migrations, persistence, replay, scanner and binary proof. | Yes; the frontend/checkpoint is additive and task-reversible before retained document history. After retained history exists, use the tested document route switch and a reviewed forward fix without deleting revisions, files, locks, audits or idempotency receipts. |
| 2026-07-30 | Add a manual-only P5 controlled-document runtime job to the existing CI workflow and run it on a fresh ephemeral GitHub runner when the current host has no Docker or fixed Bench. | CI #79 closes the frontend/browser/static/security checkpoint, but P5-01 still requires the unchanged real `--document-only` command. The job reuses exact toolchain pins, the repository's fixed Site/database/runtime marker guards and no production or external endpoint; normal PR runs skip it. | Yes; remove the manual job after equivalent durable runtime capacity exists. Its ephemeral Compose volumes contain only synthetic runtime data and are deleted after the job; retained local or production data is never targeted. |
| 2026-07-30 | Stop automatic delivery at `BLOCKED_EXTERNAL` after the fifth complete P5-01 controlled-runtime repair round; do not start a sixth round, pass P5-01 or activate P5-02. | Manual run `30566120000` passed the guarded Site, both app installations, both migrations and the corrected schema fixture, then failed Project creation with HTTP `422` because the synthetic fixture used non-email owner `Administrator`. The controller explicitly defines a necessary Gate still failing after five complete repair rounds as an exhaustive Hard Blocker. | Yes; one explicit user authorization for an additional bounded repair round may resume only the disposable-owner fixture correction and unchanged Gate. |
| 2026-07-31 | Resume exactly one additional bounded P5-01 controlled-runtime repair round; correct only the synthetic Project owner fixture and retain every Project/document contract, permission and Gate criterion. | The user explicitly selected the recorded authorization text. The repair creates one namespaced canonical-email Website User without System Manager, uses it only as Project owner, cleans it in a bounded `finally`, and proves absence in the replay process. Local affected `91/91` and complete tracked Python `774/774` checks pass; normal CI and the real controlled-Site Gate remain pending. | Yes; this is verifier/test/evidence only. Revert the repair checkpoint before accepting any runtime result; never weaken the email/enabled-owner contract or rewrite retained controlled history. |
| 2026-07-31 | Stop again at `BLOCKED_EXTERNAL` after the single authorized extra P5-01 round advanced through Project and draft-policy creation but failed policy publication at the shared Frappe Datetime persistence boundary. | Candidate `a2d98e2` passed normal CI `30569830739`; controlled run `30570343315` passed fixed setup, migrations, schema, owner, Project, policy root and draft, then publication returned HTTP `500`. The P5 controller supplies canonical ISO `T`/`Z` strings to Frappe database `Datetime` fields, while the pinned Frappe persistence format is space-separated/timezone-naive and already-string values pass through unconverted. The same root affects downstream Document fields, and the extra authority is exhausted. | Yes; a new explicit bounded authorization may separate canonical/API time from Frappe storage time across the affected P5 controllers, add sanitized failure diagnostics, and rerun the unchanged Gate. No contract, permission, timestamp semantic or PASS criterion may be weakened. |
| 2026-07-31 | Resume one additional bounded P5-01 Datetime persistence repair round and no broader product scope. | The user supplied the exact recorded authorization text. The permitted batch is limited to one Frappe-compatible storage adapter across all thirteen P5 Document `Datetime` fields, semantic immutable/parent comparisons, canonical snapshot preservation, sanitized bounded verifier diagnostics, affected tests, normal CI and one unchanged controlled-Site dispatch. | Yes; revert the bounded repair before accepting retained runtime history. No Requirement, API, permission, architecture, data ownership, timestamp semantic or Gate criterion changes are authorized. |
| 2026-07-31 | Stop at `BLOCKED_EXTERNAL` after the bounded Datetime repair passed normal CI and its single authorized controlled-Site dispatch advanced to the first document checkout, which returned HTTP `500`. | Exact candidate `7aa14ed` passed CI `#98`; controlled run `#99` passed the previously failing policy publication plus controlled document creation and immediate replay. The document-workspace assertion emitted only the status, so the retained evidence cannot uniquely attribute the failure among lock-event insert, projection save, audit append or response reconstruction. The single dispatch is exhausted. | Yes; one new explicit bounded authorization may extend the existing sanitized diagnostic to the workspace boundary, run one diagnostic-only controlled Site, fix only the proven checkout transaction root, run affected checks/normal CI and execute one final unchanged Gate. No contract, permission, architecture, data ownership, schema or PASS criterion may be weakened. |
| 2026-07-31 | Resume one additional bounded P5-01 checkout diagnostic/repair round and no broader product scope. | The user supplied the exact recorded authorization text. The permitted sequence is limited to extending sanitized diagnostics to the document-workspace boundary, one diagnostic-only controlled-Site dispatch, fixing only the proven checkout transaction root, affected tests and normal CI, and one final unchanged controlled-Site Gate. The diagnostic checkpoint reuses the existing safe BFF record and adds strict trace/schema/path/type/64-KiB-tail bounds; it changes no product code. | Yes; revert the diagnostic checkpoint before accepting any runtime result. No Requirement, API, permission, architecture, data ownership, schema, transaction, lock, audit, idempotency or PASS criterion change is authorized without proof from the diagnostic dispatch. |
| 2026-07-31 | Stop at `BLOCKED_EXTERNAL` after the sole checkout diagnostic dispatch proved only the shared Frappe `ValidationError` class, not a unique transaction stage. | Exact checkpoint `e4b284f` passed normal CI `#101`; diagnostic run `#102` passed fixed setup/migrations and cleanup, then emitted only `ValidationError / UNEXPECTED_BFF_EXCEPTION`. Command-receipt, lock-event, exact projection and receipt-seal validations still share that class. The explicit authority permits fixing only a proven root and the one diagnostic dispatch is exhausted. | Yes; one further explicit bounded authorization may add only allowlisted checkout stage codes, run affected checks/normal CI and one stage-diagnostic controlled Site. After that, repair only the proven stage and retain the already authorized final unchanged Gate. |
| 2026-07-31 | Resume one additional bounded P5-01 checkout stage-diagnostic checkpoint and one controlled-Site diagnostic dispatch. | The user supplied the exact recorded authorization text. The checkpoint may emit only one of six closed checkout stage codes, a validated exception type and the exact trace ID; it may then repair only the stage proven by the single diagnostic run. The already authorized final unchanged Gate remains reserved. | Yes; revert the diagnostic-only checkpoint before accepting a runtime result. No Requirement, API, permission, architecture, data ownership, Schema, transaction order, lock, audit, idempotency or PASS criterion change is authorized. |
| 2026-07-31 | Repair only the proven P5-01 `DOCUMENT_CHECKOUT_PROJECTION_SAVE` stage and retain the final unchanged controlled-Site Gate. | Diagnostic checkpoint `954bd0d` passed normal CI `#104`; the sole authorized dispatch `30600943765` matched that SHA and safely proved `ValidationError / DOCUMENT_CHECKOUT_PROJECTION_SAVE` while receipt insert, immutable lock-event insert, setup, migrations and cleanup passed. The bounded repair binds the projection save to the exact acquisition-event name returned by the successful insert and retains database-backed exact tenant/Project/Document/lock/event/holder/expiry validation. | Yes; before retained history, revert the exact-event projection binding. After retained history, use the document route switch and a reviewed forward fix. No DocType, public contract, permission, lock, version, audit, idempotency, transaction order or Gate criterion changes. |
| 2026-07-31 | Stop at `BLOCKED_EXTERNAL`, forward-revert the disproven projection selector candidate and do not dispatch another controlled Site without new bounded authority. | Candidate `b2d7ca9` passed complete normal CI `30601670711`, but the retained exact-SHA unchanged Gate `30601980685` passed setup, migrations and cleanup before returning the same safe `ValidationError / DOCUMENT_CHECKOUT_PROJECTION_SAVE`. The evidence therefore proves only the outer `document.save()` stage, not an inner validation substage. Both authorized controlled executions are exhausted. | Yes; one new explicit bounded authorization may add only closed projection-validation substage diagnostics, run one controlled-Site diagnostic dispatch, repair only the proven substage, run affected checks/normal CI and execute one final unchanged Gate. No Requirement, contract, permission, architecture, data ownership, Schema, transaction, lock, audit, idempotency or PASS criterion may change. |
| 2026-07-31 | Resume exactly one bounded P5-01 projection-validation diagnostic/repair round and repair only the proven revision substage. | The user explicitly authorized one closed validation-substage diagnostic, one diagnostic controlled-Site dispatch, repair of only the uniquely proven substage, affected checks/normal CI and one final unchanged Gate. Checkpoint `57b4314`, normal CI `30604536515` and dispatch `30604964265` safely proved `DOCUMENT_CHECKOUT_PROJECTION_REVISION / ValidationError`. Frappe's empty-`Int` zero hydration versus domain `None` normalization was the only consistent empty-revision path; repair `7dc4dc0` retains every non-empty exact-successor check and passed focused `44/44`, P5 Document `88/88`, tracked Python `789/789` and normal CI `30605323680`. | Yes before retained history; revert only the empty-revision normalization. No Requirement, API, permission, Schema, lock, version, audit, idempotency, transaction order or Gate criterion changed. |
| 2026-07-31 | Stop at `BLOCKED_EXTERNAL` after the single final unchanged Gate found a new post-checkout `PdfStreamError`; do not run P5-01 Level 2, start P5-02, guess another repair or dispatch another controlled Site. | Final Gate `30605683679` matched `7dc4dc0`; repository/E2E/security, fixed-Linux visual, exact setup, both migrations and cleanup passed, but the controlled job emitted only `UNEXPECTED_BFF_EXCEPTION / PdfStreamError` with one exact trace ID. The prior revision-substage failure did not recur, while the generic code does not prove a unique revision/upload transaction stage. The authorized final Gate is exhausted and no controlled runtime PASS exists. | Yes; a new explicit bounded authority may add only closed revision/upload transaction-stage diagnostics, execute one diagnostic controlled Site, repair only the uniquely proven stage, rerun affected checks/normal CI and execute one final unchanged Gate. No Requirement, API, permission, Schema, file-integrity rule, lock, version, audit, idempotency, transaction order or PASS criterion may change. |
| 2026-07-31 | Resume P5-01 under one product-root-based recovery envelope and correct Auto Pilot repair-round classification without changing the V1.2 product path. | The user approved the proposed recovery plan. Environment/bootstrap/verifier/clearly synthetic fixture-precondition failures and behavior-neutral closed diagnostics remain fail-closed but no longer consume the five uniquely proven product-root rounds. P5-01 may add one complete closed revision/upload stage inventory, execute one diagnostic controlled Site, repair only the uniquely proven stage, rerun affected checks/normal CI and execute one final unchanged Gate before Level 2. | Yes; the controller wording and diagnostic checkpoint are additive. Requirements, API, permission, Schema, ownership, file-integrity rules, lock, version, audit, idempotency, transaction order and PASS criteria remain frozen; historical blocker evidence is not rewritten. |
| 2026-07-31 | Pass P5-01 Level 2 and activate only P5-02 after the exact product SHA passes complete ordinary CI and the final unchanged controlled-Site Gate. | A closed diagnostic proved that the PDF failure was an invalid synthetic fixture precondition, not a product-validation defect. A later closed HTTP assertion proved the only product root: Frappe GET `targetVersion` text was passed to a JSON-body-only integer parser. Repair `5a9cd3d` adds strict canonical positive query parsing only; normal CI `30610355829` and controlled workflow `30610747931` pass with no Requirement, OpenAPI, ownership, DocType, permission, lock, version, audit, idempotency, transaction-order, file-integrity or PASS-criterion change. | Yes; before retained history, revert the bounded parser/fixture/diagnostic commits. After history, use the P5-01 route switch and reviewed forward fix. P5-02 must append new review/release history without rewriting P5-01 records or inferring production authority/signature/scanner rules. |
| 2026-07-31 | Implement P5-02 with a separate immutable release-policy version, append-only review/confirmation/lifecycle history and a guarded current-state projection; do not mutate the P5-01 revision snapshot. | V1.2 requires exact review/release authority, confirmations, immutable released history and server-side binary retention, while production roles/quorum/signature/scanner rules remain held. A separate exact synthetic policy and append-only records satisfy the safe mechanism without deriving authority from Project owner/RACI/System Manager/transport role or rewriting retained revision/file hashes. | Yes; before retained P5-02 history, remove the additive DocTypes/routes. After history exists, activate the independent P5-02 route switch and deploy a reviewed forward fix while retaining policies, confirmations, events, released File Revisions, Frappe Files, audits and receipts. |
| 2026-07-31 | Implement P5-03 with an independent exact baseline-policy authority, immutable released-revision/File/hash packages, exact Gate evidence and only explicitly registered dependencies; reuse the existing Gate Review invalidation/successor-cycle mechanism instead of inventing another impact state machine. | FR-DS-006 and the Phase 5 anchor require exact immutable packages and visible successor impact, while production baseline contents/authority and the dependency matrix remain held. Gate attachment is the explicit downstream dependency registration; successor impact can therefore preserve the baseline and prior Gate decision while the already versioned review policy owns resolution. | Yes; before retained P5-03 history, remove the additive DocTypes/routes. After history exists, use the independent P5-03 route switch and a reviewed forward fix while retaining policies, baselines, members, Gate references, dependencies, impact events, reviews, audits and receipts. |
| 2026-08-03 | Resume the existing Goal at P5-03 baseline creation under one extra strictly bounded product-root exception, without changing the controller's global five-round rule. | The P5-03 plan recorded four completed product roots. A closed diagnostic uniquely proved the field-mapping root and repair `2b067c1` advanced the unchanged Gate to the new baseline-create root, so it is the fifth repair under the controller definition. Recovery base `a1d8429` passed ordinary CI `30761151383`; diagnostic run `30761455482` proved only `P503_VERIFIER_POST_WORKSPACE_BASELINE_CREATE / RuntimeError / trace-f9c9295e07be5bec93aa8b6b05cc2c30`. The final unchanged Gate has not run and no P5-03 PASS exists. | Yes; the exception covers only the current baseline-create root, at most two additional diagnostic-only dispatches and one uniquely proved repair. Behavior-neutral diagnostics consume no product-root round. No Requirement, API, permission, Schema, ownership, lock, version, audit, idempotency, transaction order or PASS criterion may change. |
| 2026-08-03 | Stop at `BLOCKED_EXTERNAL` after the extra bounded P5-03 repair passed ordinary CI but the single final unchanged Gate exposed a non-unique response-contract failure. | Diagnostic runs `30776554186` and `30777405187` narrowed the server root to release lineage. FR-DS-006, its anchor, OpenAPI, real DocType fields, permissions and transaction invariants uniquely proved the comparison repair in `15abf26`; affected `167/167` and ordinary CI `30777828197` passed. Final workflow `30778190537` ran with diagnostic activation closed and returned only `P503_BASELINE_CREATE_RESPONSE_CONTRACT / RuntimeError / trace-062ce39fc49457a384bc1acba7afd785`. Multiple response predicates remain possible, while both diagnostic dispatches, the extra repair exception and final Gate are exhausted. | Yes; a new explicit bounded authorization may add only closed response-contract predicate diagnostics, require affected/full ordinary CI before at most one diagnostic Site, repair only a uniquely proved predicate, and reserve one final unchanged Gate. The global five-round rule and every Requirement/API/permission/Schema/ownership/lock/version/audit/idempotency/transaction/PASS invariant remain unchanged. |
| 2026-08-03 | Resume the existing Goal for one strictly bounded P5-03 baseline-create response-contract predicate diagnostic and at most one diagnostic-only controlled-Site dispatch. | The user supplied the exact authority anticipated by the prior blocker. The historical aggregate tuple remains non-unique; the new ladder is behavior-neutral and may expose only an allowlisted predicate code, validated exception type and exact trace ID. A verifier or fixture root follows normal controller classification; a uniquely proved product root receives one separate response-contract-only exception without changing the global five-round rule. | Yes; remove the diagnostic activation path before the reserved final unchanged Gate. No repair is allowed without unique proof and contract/DocType/permission/ownership/transaction cross-validation. If proof is absent or a frozen invariant must change, record one Hard Blocker and stop. |
| 2026-08-05 | Stop at `BLOCKED_EXTERNAL` before the P5-03 response-contract diagnostic dispatch because its mandatory complete ordinary CI is red and the exact failing subcommand is unavailable within the current non-disclosure authority. | Checkpoint `9d25e50` passed affected P5 Document `168/168`, tracked Python `883/883`, clean frontend generation/type/lint/`671/671`/coverage/build/brand and the fixed-Linux visual lane. Ordinary run `30980622113` failed twice only at the enclosing `bash scripts/verify.sh` step; the controlled Site was skipped. Full-log access and an external npm audit payload were not authorized, Dependabot alerts are disabled, and the offline zero-vulnerability cache cannot prove the runner result. Guessing a dependency or dispatching with a red prerequisite would violate the Gate. | Yes; grant one bounded read of only the failing ordinary-CI subcommand and, if applicable, package/GHSA/severity/first-patched-version metadata. After a uniquely proved ordinary-CI root is closed and complete CI passes, the existing unused `0/1` diagnostic dispatch authority resumes. No Requirement, API, permission, Schema, ownership, lock, version, audit, idempotency, transaction order or PASS criterion changes. |
| 2026-08-05 | Pass P5-03 Level 2 at exact product SHA `302b1e9` and activate only the P5-04 Requirement/domain audit. | The bounded response-contract recovery, exact persisted-release and Datetime boundary corrections, and verifier/fixture-only replay/lock corrections converged without weakening FR-DS-006 or any public contract, permission, ownership, transaction or PASS criterion. Ordinary CI `30990594281` passed, followed by final unchanged controlled-Site Gate `30991177478` with diagnostics closed and PASS artifact `8924223239`. Local affected backend `233/233`, frontend `162/162`, i18n `3,232` sources at direct `100%` `zh`/`zh-TW`, trace checks and original-resolution six-image UX review passed. | Yes before retained history by reverting the P5-03 slice. After history exists, preserve policies, baselines, members, dependencies, impacts, Gate/review history, audits and receipts; use the P5-03 route switch and a reviewed forward fix. P5-04 may own only NPI EBOM working revisions/comparison and may not infer formal ERPNext Item/MBOM/routing or production policies. |
| 2026-08-05 | Implement P5-04 as NPI-owned immutable EBOM content revisions with a separate guarded lifecycle and deterministic exact-revision comparison under explicit synthetic policy; retain all unknown production rules as scoped holds. | FR-DS-011/012 and the Phase 5 anchor require hierarchy, quantity, alternate/effectivity, review/release state and added/removed/quantity/substitution/attribute differences. Ownership fixes formal Item Code, stock UOM, MBOM, routing and execution in ERPNext, while production numbering, line identity, precision, vocabulary and authority are unavailable. A publish-once policy plus fixed synthetic fixtures delivers the generic technical mechanism without inventing a production default. | Yes before retained P5-04 history by removing the additive slice. After history exists, activate the independent P5-04 route switch and deploy a reviewed forward fix while retaining every policy, EBOM/revision/line, lifecycle/event, audit and receipt record; never delete or reopen a released revision. |
| 2026-08-05 | Pass the P5-04 repository/BFF/OpenAPI Level 1 checkpoint and activate only the Project EBOM frontend workspace. | Exact checkpoint `40e7b70` passed ordinary CI `31001529719`: complete repository, `939/939` Python, `671/671` frontend unit, `288/288` non-visual browser, direct `3,421`-source trilingual coverage, fixed-Linux `59/59`, zero-vulnerability audits and both secret lanes. The public boundary keeps exact policy/revision/lifecycle authority, actor-bound replay, closed schemas and two-explicit-revision comparison without formal ERPNext Item/MBOM ownership. | Yes before retained P5-04 history by reverting the additive repository/API slice. After history exists, activate only the P5-04 route switch and use a reviewed forward fix while preserving all immutable content/lifecycle/audit/receipt history. The frontend stage may consume but not rewrite the public contract or held production policies. |
| 2026-08-05 | Bind one P5-04 browser idempotency key to one explicit user submit across retry, and reconstruct every EBOM request from only its declared fields instead of forwarding structurally compatible caller objects. | FR-DS-011 lifecycle commands and the accepted repository contract require actor-bound replay/conflict and closed payloads. Task Diff Review proved that retry-time UUID generation could create a second intent and object spread could forward an undeclared field. Affected tests now prove key reuse, exact serialization and pre-transport rejection of invalid review/release constants. | Yes; this is a client-side safety implementation with no Schema or business-policy change. Reverting it would reopen duplicate-intent and undeclared-field risks and is therefore permitted only with an equivalent closed/idempotent client boundary. |
| 2026-08-05 | Close the P5-04 frontend stage at exact checkpoint `0c344fe` and activate only a bounded controlled-Site EBOM runtime candidate. | Complete ordinary CI `31008027534` passed repository, fixed-Linux `62/62`, complete browser and both secret lanes. The runtime candidate reuses only the fixed disposable P5 Project and a normal internal member, creates one explicit synthetic policy, proves immutable revisions/lifecycle/comparison/rollback/audit/route recovery/replay, and reduces failures to allowlisted stage code, validated type and exact trace ID. It creates no production policy or ERPNext object. | Yes before retained P5-04 history by reverting the harness. After history exists, preserve every policy, EBOM/revision/line, lifecycle/event, audit and receipt; disable only P5-04 routes and use a reviewed forward repair. P5-05 remains inactive until P5-04 Level 2 passes. |
| 2026-08-05 | Correct only the P5-04 controlled verifier's synthetic-policy provisioning boundary; keep the product's guarded policy controllers unchanged. | Exact-SHA ordinary CI `31011531101` passed. Controlled run `31013199095` then passed the unchanged P5-01/02/03 runtime and stopped when generic verifier REST CRUD met the intentional `ebom_policy_write()` guard. Code and runtime evidence uniquely prove a verifier/fixture defect, not a product root. The repair uses one allowlisted fixed-Bench fixture under the existing guarded admin context and changes no API, permission, Schema, ownership, transaction, policy or PASS rule. | Yes; before retained disposable evidence, revert the fixture-only checkpoint. The final controlled Gate recreates an ephemeral Site and cleans it completely. No product history or production policy is created, and no product-root repair round is consumed. |
| 2026-08-05 | Narrow the non-unique P5-04 fixed-Bench policy fixture with one behavior-neutral closed substage diagnostic before any further repair. | Repair checkpoint `cb314ff` passed ordinary CI `31014577854`; final unchanged workflow `31015391479` passed pinned Bench, Site, migrations, unchanged P5-01/02/03 runtime, route recovery and cleanup, then returned only `P504_RUNTIME_POLICY_FIXTURE / BenchFixtureError / trace-38a09ee9b80150e98daef921d5b01fd1`. The tuple cannot distinguish root/version build, insert, publish or persistence, so a repair is not uniquely proved. Closed substages and strict last-line/type/trace validation preserve non-disclosure and are diagnostic-only under the controller. | Yes; revert the diagnostic checkpoint without product-state impact. It changes no product API, controller, permission, Schema, ownership, transaction, policy or PASS rule. Require complete ordinary CI before one diagnostic dispatch; repair only the proven synthetic fixture substage and remove no validation to make the Gate pass. |
| 2026-08-05 | Stop at `BLOCKED_EXTERNAL` after the P5-04 diagnostic uniquely proves a policy-version publication product root but no P5-04 product-repair round remains authorized. | Checkpoint `217632f` passed complete ordinary CI `31016624361`; diagnostic workflow `31017098820` passed every predecessor/environment/cleanup boundary and returned only `P504_RUNTIME_POLICY_VERSION_PUBLISH_FIXTURE / ValidationError / trace-9d081239bf095af1a7f41eeaa65a0d9d`. Draft insert persists a server-owned hash, then published domain reconstruction rejects that exact prior-state hash before `_apply_policy()` reaches its intended prior-hash allowance. Five product-root rounds are exhausted; the extra user authority is P5-01-checkout-only and cannot be transferred. | Yes; the diagnostic checkpoint is behavior-neutral. One explicit additional P5-04 round may repair only exact prior-draft-hash handling, run affected tests/ordinary CI and one final unchanged controlled Gate. No Requirement, API, permission, Schema, ownership, transaction, policy authority or PASS criterion may change. |
| 2026-08-05 | Apply the explicitly authorized bounded P5-04 prior-draft-hash repair and retain the unchanged Gate. | Repair `d21d21a` treats only an exact persisted prior draft hash as server-owned during published reconstruction, leaves the original value for exact prior/new validation and retains tamper/non-draft/non-publish rejection. EBOM `58/58`, complete Python `954/954` and ordinary CI `31020190868` passed. | Yes; revert the three-file repair before retained P5-04 history, or use the P5-04 route switch and reviewed forward fix afterward. No public contract, permission, Schema, ownership or Gate criterion changed. |
| 2026-08-05 | Stop at a new `BLOCKED_EXTERNAL` after the authorized repair advances the final Gate to a non-unique EBOM create-stage failure. | Final controlled workflow `31020886002` passed policy publication, empty workspace, guest/unrelated authorization and the full P5-01/02/03 predecessor runtime, then emitted only `P504_RUNTIME_CREATE / HttpStatusError / trace-f92a1e065fe35759b261601244cca7d4`. That aggregate spans multiple verifier and product substages, so no unique repair is proven and the prior authorization is exhausted. | Yes; one explicit bounded authority may add closed response-neutral create substages, run affected/full ordinary CI, use at most one diagnostic Site, repair only a uniquely proven in-scope root and reserve one final unchanged Gate. |
| 2026-08-06 | Resume P5-04 under one bounded create-stage diagnostic/repair authority and activate only the first create request diagnostic. | The user explicitly asked to repair and continue the existing Goal after receiving the exact bounded authorization request. The diagnostic emits only an allowlisted substage, validated exception type and exact trace ID to local logs; HTTP responses and every Requirement/API/permission/Schema/ownership/transaction/idempotency/audit/PASS invariant remain unchanged. Local EBOM `62/62`, related regression `70/70` and complete Python `958/958` passed. | Yes; remove the verifier header activation before the reserved final unchanged Gate. At most one diagnostic Site and one uniquely proved in-scope repair are authorized. |
| 2026-08-06 | Repair only the P5-04 controlled fixture's synthetic namespace/key invariant and keep the domain contract unchanged. | Checkpoint `008e6ed` passed ordinary CI `31069567886`; the sole diagnostic workflow `31069924517` isolated `P504_CREATE_DOMAIN_BUILD / RequestValidationFailed / trace-79bcd3a2408c5f71bb8c0cad8bd9db21`. The fixture published namespace `synthetic_runtime` but sent key `synthetic_ebom_...`, while the accepted domain and tests require namespace `synthetic_ebom` and exact `namespace + "-"` key prefix. This uniquely proves a synthetic verifier/fixture precondition defect before transaction scope, not a product rule or persistence defect. | Yes; use one shared `synthetic_ebom` fixture constant, close diagnostic activation, run full/ordinary CI and only then the reserved final unchanged Gate. No Requirement, API, permission, Schema, ownership, transaction, idempotency, audit or PASS criterion changes. |
| 2026-08-06 | Stop at `BLOCKED_EXTERNAL` after the fixture repair advances the exhausted final Gate to a new non-unique remaining create-stage failure. | Repair `158ef02` passed complete local and ordinary CI `31070341154`. Final unchanged workflow `31070732986`, with diagnostic activation closed, no longer emitted the proven domain-build validation failure and instead returned only `P504_RUNTIME_CREATE / HttpStatusError / trace-462662eec74c5c4f9e3e5a07258f1a7b`. That aggregate cannot select one remaining transaction/response stage, while the diagnostic Site, fixture repair and final Gate allowances are all consumed. | Yes; one new explicit bounded authority may reactivate only the existing response-neutral create diagnostic, require affected/full ordinary CI, execute at most one diagnostic Site, repair only the uniquely proved remaining root and reserve one final unchanged Gate. Frozen invariants and global repair accounting remain unchanged. |
| 2026-08-06 | Resume P5-04 on exact base `c7edac8` under one new bounded remaining-create-stage diagnostic/repair authority. | The user supplied the exact authority requested by the prior blocker. Historical create-stage diagnostic/repair/Gate consumption remains immutable; this separate sequence starts at diagnostic `0/1`, uniquely proved repair `0/1` and reserved final unchanged Gate `0/1`. Only the existing response-neutral diagnostic is active on the first create request, and affected/full ordinary CI is mandatory before the sole diagnostic Site. | Yes; close activation before the final unchanged Gate. No Requirement, API, permission, Schema, ownership, transaction, idempotency, audit, PASS criterion or global repair-accounting rule may change. |
| 2026-08-06 | Repair only the P5-04 revision controller's incomplete exact-policy hydration projection and close create diagnostics. | Exact-SHA ordinary CI `31073500593` passed, then the sole diagnostic Site `31073915463` returned only `P504_CREATE_REVISION_INSERT / ValidationError / trace-9b23575185625a1998ac184bfefaa272` after all prior create stages passed. `require_exact_parent()` selects only expected plus explicit extra fields; the revision controller then hydrates `ebom_policy_value()` but omitted its required `policy_global_id` and `policy_version` from the selected row, producing the exact mapped validation failure. | Yes; select those two existing fields only, retain the same exact-parent predicates and domain revalidation, run affected/full ordinary CI and one final unchanged Gate with diagnostics closed. No contract, Schema, permission, ownership, transaction, idempotency, audit or PASS rule changes. |
| 2026-08-06 | Stop P5-04 at `BLOCKED_EXTERNAL` after the authorized remaining-create final Gate returns to an opaque aggregate create failure. | Repair `f4aba87` passed complete ordinary CI `31075372272`. Final unchanged workflow `31075730002` retained that SHA and passed repository, visual, predecessor runtime, migrations and cleanup, but with diagnostics correctly closed emitted only `P504_RUNTIME_CREATE / HttpStatusError / trace-6fa26f47b241558db7fdafa0b9c1a46e`. The log contains no server substage that proves recurrence of the revision insert or a later create failure, while diagnostic, repair and final-Gate allowances are all `1/1`. | Yes; preserve the failed Gate, do not retry or guess. A new explicit bounded authority is required before reactivating the existing response-neutral create diagnostic or applying another repair. Frozen invariants and PASS criteria remain unchanged. |
| 2026-08-06 | Resume P5-04 on exact base `16ed463` under a new bounded post-revision-create diagnostic/repair sequence. | The user requested that the problem be fixed after the prior Goal was blocked. The final Gate remains opaque, so direct product modification would still be speculative; the existing response-neutral substage diagnostic is the minimum safe evidence path. | Yes; permit diagnostic `0/1`, at most one uniquely proved repair `0/1` and final unchanged Gate `0/1`, with affected/full ordinary CI before each Site boundary and diagnostics closed before the final Gate. Frozen invariants and global repair accounting remain unchanged. |
| 2026-08-06 | Use the sole post-revision repair to convert the lifecycle controller's already-canonical revision ID from `str` to `UUID` at the pure-domain boundary. | Diagnostic Site `31080379082` uniquely emitted `P504_CREATE_LIFECYCLE_INSERT / ValidationError`; the preceding line controller passed the same exact revision/tenant/Project/hash predicate, and `EngineeringBomRevisionLifecycle._uuid` rejects non-`UUID` values before mapping through `ebom_domain_value` to that exact exception type. | Yes; the conversion is local to domain hydration, changes no persisted field or contract, and can be reverted before retained lifecycle history. Diagnostic activation is closed before ordinary CI and the final Gate. |
| 2026-08-06 | Stop P5-04 after final Gate `31082337133`; do not infer another repair from `P504_RUNTIME_CREATE / HttpStatusError`. | Exact repair SHA `6a4ba7c` passed complete ordinary CI, but the required unchanged Gate returned only an aggregate create-stage tuple with diagnostics closed. It cannot distinguish lifecycle recurrence from a later projection/audit/response/receipt root, and the explicit diagnostic/repair/final allowances are all consumed. | Yes; a new explicit bounded authority may reopen only the existing response-neutral diagnostic path while preserving every frozen invariant and historical run. |
| 2026-08-06 | Apply the user's standing Hard Blocker recovery authority and resume P5-04 without repeated authorization stops. | Prior repairs advanced the same create transaction one proven stage at a time, but diagnostics must be closed in every final Gate; each newly exposed downstream failure therefore returned only the aggregate code after its one-off counter was consumed. The standing authority removes that administrative loop while preserving serial proof: one existing response-neutral diagnostic Site, one uniquely proved repair and one final unchanged Gate per cycle, with ordinary CI before every Site. | Yes; the standing authority may be withdrawn. It does not alter Requirements, API, permission, Schema, ownership, transaction, idempotency, audit, PASS criteria or historical counters, and still pauses for business decisions, high-risk/destructive approval or external manual action. |
| 2026-08-06 | Repair only the missing immutable-audit flag inside existing authorized EBOM command and lifecycle scopes. | Exact diagnostic SHA `233b23f` passed ordinary CI `31084462702`; Site `31085013974` uniquely emitted `P504_CREATE_AUDIT_APPEND / PermissionError / trace-ee528c1626eb59c4ba40f1ffea1b86ce` after root, revision, line, lifecycle and root-projection stages passed. `NPI Audit Event.before_insert()` requires `npi_audit_append`; the inherited append writes directly, but EBOM scopes set only their domain flags. Passing peer command scopes set and restore both. | Yes; add/restore `npi_audit_append` only inside the existing EBOM command/lifecycle contexts and close diagnostics. This enables mandatory audit persistence without granting a role, DocPerm, generic CRUD, `ignore_permissions`, new transaction or altered audit content. |
| 2026-08-06 | Advance the standing recovery authority to one response-neutral submit-review transition diagnostic cycle after create succeeds. | Repair `1fda74a` passed ordinary CI `31086008989`; final workflow `31086562000` passed repository, visual, fixed Site/migrations, predecessor runtime and cleanup, advanced through create, then emitted only `P504_RUNTIME_SUBMIT_REVIEW / HttpStatusError / trace-1494387c76f6549899ce007d429ba163`. This proves the audit repair effective but cannot select one submit-review substage. The diagnostic adds one separate header scope only to submit-review and records one allowlisted stage/type/trace tuple without changing responses. | Yes; remove the one verifier activation before the final unchanged Gate. Create, review and release stay unactivated. Require affected/full ordinary CI before one diagnostic Site and repair only a uniquely proved root; preserve Requirement, API, permission, Schema, ownership, transaction, idempotency, audit and PASS invariants. |
| 2026-08-06 | Repair only the submit-review lifecycle projection's canonical last-event type boundary and close transition diagnostics. | Exact diagnostic SHA `f47f4ef` passed ordinary CI `31087964089`; Site `31088548041` uniquely emitted `P504_TRANSITION_LIFECYCLE_PROJECTION_SAVE / ValidationError / trace-15866486cf445bb0bac3dc35120d6318` after receipt and lifecycle-event insert. The controller canonicalizes and exact-parent-validates `last_event_global_id` as text, then passes it to a domain field that requires `UUID`; the same constructor already converts the revision ID. | Yes; convert only the already-validated non-null last-event ID to `UUID`, retain exact event/state/version validation and transaction order, remove runtime activation, then require affected/full ordinary CI and one final unchanged Gate. No Requirement, API, permission, Schema, ownership, idempotency, audit or PASS criterion changes. |
| 2026-08-06 | Accept P5-04 at Level 2 and automatically activate P5-05. | Repair `2c0734a` passed complete ordinary CI `31089637022`; the single final unchanged Gate `31090154694` passed repository, fixed Site/two migrations, unchanged P5-01/02/03 runtime, complete EBOM lifecycle, visual evidence and cleanup with diagnostics closed. Artifact `8963145655` records `result=PASS` for exact SHA `2c0734a`. | Yes; `FR-DS-011` and `FR-DS-012` advance to `TECHNICAL_VERIFIED`. Retain production EBOM/ERP facts as scoped holds and start only P5-05 for `FR-DS-013`. |
| 2026-08-06 | Replace the accepted generic Execution Request create seed with one operation-specific P5-05 EBOM Item/MBOM publish command. | The current caller-selected `operation + payload` schema and Phase 2 Outbox/Inbox foundation cannot bind an exact released EBOM, independent publish authority, immutable approval input, node mappings/results or no-fake-success behavior required by `FR-DS-013`. | Yes; use `publish_released_ebom_item_mbom`, derive closed node intents server-side, default to Mock `validated`, prohibit formal target IDs/`succeeded` and all Phase 5 dispatch, and leave real sandbox/production adapter, retry/replay/webhooks and reconciliation to Phase 8. |
| 2026-08-08 | Accept the P6-04 live manufacturing workspace checkpoint and activate only its controlled-runtime/Level-2 checkpoint. | Exact stable SHA `039b7f1` passes complete ordinary CI `31263974510`: `1,208` Python, `756` frontend unit, `326` non-visual E2E, `4,641` complete direct trilingual sources, statement coverage `80.03%`, both secret lanes and `82/82` fixed-Linux visuals. The `29` baseline changes came from reviewed CI actuals; no tolerance, assertion or case was weakened. | Yes; disable the independent P6-04 route switch and remove the live composition while retaining immutable plan/observation history. Formal Supplier/ERP success, supplier portal and manufacturing authority remain unavailable. |
| 2026-08-08 | Accept P6-04 at Level 2 and automatically activate only the bounded P6-05 Requirement/domain/existing-capability audit. | Exact checkpoint `5ca13ab` passes ordinary CI `31266800163` and final workflow `31267181068`; controlled job `93127118037` proves two immutable plan revisions, two milestone observations, released/unreleased design dependency enforcement, unavailable ERP/manufacturing authority, replay, conflicts, rollback, IDOR and independent route recovery. The two precursor failures were uniquely proven fixture contract defects and their repairs changed no product permission, API or authority. | Yes; disable only P6-04 routes/projections and use a reviewed forward repair while retaining immutable plans, observations, evidence, audits and receipts. Formal Supplier/ERP execution and Tooling lifecycle authority remain held; P6-05 may not invent lifecycle or production red semantics. |
| 2026-08-08 | Accept the bounded P6-05 Requirement/domain/existing-capability audit and activate only its domain/contract/additive-metadata foundation. | The repository has no live Tooling defect, process-layer, capacity-scenario, Trial or health aggregate. Existing Project Work cannot retain cavity/root-cause/verification truth, and deterministic Tooling/Trial pages are prototypes. The accepted requirements support immutable defect revisions, separated process fact layers and explicit `capacity.v1`; Phase 7 remains the only truthful source of Trial Actual, Approved Baseline and final G5/G6 integration, while ERP/IoT health inputs remain unavailable. | Yes; add only pure domains, closed schemas, three guarded append-only DocTypes and ownership/receipt truth. Keep routes, SPA, Trial/Gate/ERP/IoT writes, Tooling lifecycle commands, inferred tolerances and production exception colors inactive. Rollback is additive before retained rows and route-disable/forward-fix afterward. |
| 2026-08-08 | Accept P6-05 at Level 2 and automatically activate only the bounded P6-06 Requirement/domain/existing-capability audit. | Exact checkpoint `4e04eb4` passes ordinary CI `31280290398` and final workflow `31280296684`; controlled job `93160709186` proves immutable defect/profile/capacity successors, action/evidence/blocking separation, absent Trial Actual/Approved Baseline, deterministic recomputation, replay, conflicts, rollback, IDOR and independent route recovery. Four precursor failures were uniquely proved verifier/fixture contract boundaries and their repairs changed no product permission, API or authority. | Yes; disable only P6-05 routes/projections and use a reviewed forward repair while retaining immutable revisions, audits and receipts. P6-06 may audit immutable acceptance evidence and Mock/sandbox-ready asset request/projection conditions only; real ERPNext asset creation/update, target mapping confirmation, location/movement, maintenance, repair, spares, inventory and cost remain Phase 8. |
| 2026-08-08 | Accept the bounded P6-06 Requirement/domain/existing-capability audit and activate only its pure domain/contract/additive-metadata foundation. | Existing Tooling identities/evidence are reusable, but no approved acceptance policy, live Trial/official quality, Asset request, formal mapping or ERP reader exists. The generic Execution Request create seed was intentionally removed by P5-05, and Outbox/Inbox foundations prove no Asset operation. Exact controller checkpoint `943d1ea` passes ordinary CI `31281224456`. | Yes; add immutable evidence-presence revisions, NPI-only movement/spare/repair evidence, a fixed `create_or_update_tool_asset` Mock request that remains draft/unapproved/undispatched, and strict unavailable/read-only Asset projection conditions. Keep routes, rows, SPA, Trial/Gate/lifecycle/ERP execution and production access inactive until later checkpoints. |
| 2026-08-09 | Accept P6-06 at Level 2 and automatically activate only the bounded P6-07 Tooling List import audit. | Exact checkpoint `de7bef7` passes ordinary CI `31291977009` and final workflow `31292306716`; controlled job `93191451432` proves immutable acceptance succession, physical-Set binding, customer-owned repair authorization, Mock request preparation, replay, conflict, rollback, IDOR, no network/Outbox/target truth and independent route recovery. The serial repairs corrected exact verifier/fixture and bounded repository-interface roots without changing product authority. | Yes; retain every acceptance/request/audit row and disable only P6-06 routes through a reviewed forward fix. P6-07 may audit passive XLSX safety, immutable provenance, versioned 43-column mapping mechanics, preview and partial-result/correction/retry/rollback-denial truth; `DR-REC-007/008` continue to prohibit unapproved production mapping activation and destructive downstream rollback. |
| 2026-08-09 | Accept the bounded P6-07 Requirement/domain/existing-capability audit and activate only checkpoint 1 domain/passive-inspection/contract/additive-metadata work. | The repository has a reviewed passive archive/XML inspector, exact private File Revision and distinct Tooling identities, but no business-value reader, region detector, mapping activation, preview, batch/job/result, correction or rollback command. The 43-column CSV remains a proposal under `DR-REC-007`; exact controller checkpoint `25db3ae` passes ordinary CI `31292919974`, including repository `93193123207` and fixed-Linux visual `93193123198` at `88/88`. | Yes; follow `implementation/evidence/phase-6/p6-07-plan.md`. Checkpoint 1 may add product-owned passive inspection, pure immutable import domains, a deterministic visibly synthetic fixture, closed schemas, guarded DocTypes, receipt values and direct trilingual tests. Keep routes, business rows, worker, UI, production mapping, customer workbook, ERPNext contact and destructive rollback inactive. |
| 2026-08-09 | Accept P6-07 at Level 2 and automatically activate only the bounded P6-08 Requirement/domain/existing-capability audit. | Exact stable checkpoint `d8e4897` passes ordinary CI `31330677928` and unchanged final workflow `31330684809`; repository, `91/91` fixed-Linux visuals and controlled job `93288346195` prove the cumulative import path, cross-process replay, partial/retry/reconciliation, rollback allowed/denied, route recovery, redaction, no production mapping/ERP traffic and cleanup. Serial repairs corrected only exact controlled-runtime roots without weakening authority or PASS criteria. | Yes; retain all source, preview, confirmation, result, receipt, correction, reconciliation and audit rows and disable P6-07 routes/worker only through a reviewed forward fix. P6-08 may audit controlled selection/filter and authorized object-package export only; it must never become an arbitrary database dump or expose raw private File URLs. Production mapping, ERPNext and downstream destructive rollback holds remain. |
| 2026-08-09 | Accept the bounded P6-08 Requirement/domain/existing-capability audit and activate only checkpoint 1 domain/contract/additive-metadata work. | `UX-007`, the Phase 6 anchor and Tooling domain specification require ten Tooling views plus selection/filter export with separate authorization, redaction, audit, validity, language and immutable versions. The shared DenseGrid, fixed preference, Tooling repository and private binary patterns are reusable, but no Tooling list/export contract, authority, package artifact or action exists. Exact predecessor/controller checkpoint `d5d6064` passes ordinary CI `31331504738`. | Yes; follow `implementation/evidence/phase-6/p6-08-plan.md`. Checkpoint 1 may add pure ten-view/query/preference/package domains, safe localized CSV/ZIP rendering, closed contracts, guarded DocTypes, receipt values and direct tests. Keep routes, business rows, Files and SPA inactive; prohibit arbitrary queries/fields, raw private URLs, cross-Project data and ERP/lifecycle claims. |
| 2026-08-10 | Accept P6-08 checkpoint 1 and activate only checkpoint 2 repository/BFF/private-artifact/download work. | Exact stable checkpoint `5b15609` passes ordinary CI `31334024291`: repository `93296765481` proves `1,381` Python, `796` frontend unit and `343` non-visual E2E tests, complete `5,638`-source direct trilingual coverage, zero vulnerabilities and both secret lanes; visual `93296765409` passes `91/91`. The preceding failures uniquely isolated missing catalog sources, one mixed-language label and eighteen footer-only fingerprints; repairs changed no product authority, route, assertion, matrix, threshold or PASS rule. | Yes; activate only independently default-closed Project-first list/preference/create/download routes, conservative export authority, exact shared-Master containment, private immutable artifact/audit/receipt persistence and creator-bound one-hour hash-verified POST download. Keep live SPA, controlled Site, production ERPNext and Tooling/lifecycle authority inactive. |
| 2026-08-10 | Accept P6-08 checkpoint 2 and activate only checkpoint 3 dense trilingual Tooling List workspace. | Exact stable checkpoint `ac0a29c` passes ordinary CI `31336841275`: repository `93303992048` proves `1,405` tracked Python, `796` frontend unit and `343` non-visual E2E tests, `5,647` complete direct trilingual sources, statements `80.00%`, zero vulnerabilities and both secret lanes; visual `93303992034` passes `91/91`. The sole precursor failure was eighteen reviewed footer-only catalog fingerprints; repair changed no source component, assertion, matrix, threshold or PASS rule. | Yes; compose only the fixed P6-08 data source and DenseGrid section, ten views/paging/preferences/selection, selected-object navigation and one secondary reviewed Export/download action with honest trilingual states. Keep controlled Site, production ERPNext and Tooling/lifecycle authority inactive. |
| 2026-08-10 | Accept P6-08 checkpoint 3 and activate checkpoint 4 cumulative controlled runtime, Level 2 and Phase 6 Gate. | Exact stable checkpoint `82ebcaf` passes ordinary CI `31341354013`: repository `93315593607` proves `1,405` tracked Python, `809` frontend unit and `352` non-visual E2E tests, `5,753` complete direct trilingual sources, statements `80.07%`, zero vulnerabilities, install-script/brand/full verify and both secret lanes; visual `93315593576` passes `94/94`. Two bounded visual repair rounds promoted only reviewed Linux candidates and corrected one empty-state spacing defect plus missing workflow/artifact coverage without changing product authority, assertion, threshold or PASS rule. | Yes; activate only the cumulative disposable-Site proof through P6-08, P6-08 Level 2 trace reconciliation and Phase 6 Level 3 release gate. Keep production ERPNext, production mapping, arbitrary export, raw File URLs and Tooling/lifecycle authority inactive. |
| 2026-08-10 | Accept P6-08 at Level 2, close Phase 6 at Level 3 and automatically activate only P7-00 Trial/NPI requirement anchoring. | Exact product checkpoint `68f230f` passes ordinary CI `31355006189` and final unchanged workflow `31355555773`; repository, `94/94` fixed-Linux visuals and controlled job `93354448564` prove ten views, selection/filter packages, three languages, immutable private bytes/hashes, expiry, replay, IDOR/stale/conflict/generic-mutation denial, route recovery, two migrations, no ERP traffic and cleanup. Serial repairs corrected only evidence-proved runtime/verifier boundaries without weakening product authority or Gate criteria. | Yes; retain all Tooling/import/export/package/File/receipt/audit history and disable affected independent routes/workspaces only through reviewed forward fixes. P7-00 may allocate Trial/NPI requirements and authorities only; it must not create product code, invent Trial/Gate lifecycle or quality approval, contact ERPNext, publish an external Trial Summary event, or install production print policy. |
| 2026-08-10 | Accept P7-00 at Level 2 and activate only the bounded P7-01 `FR-TR-001` Trial Plan/Round audit. | The 282-row trace now allocates all twenty-five primary Trial/NPI requirements plus `UX-020` to P7-01..08, preserves the earlier `FR-PRN-002` and `FR-INT-015` truth and records identity, ownership, authority, migration, rollback and test boundaries. Exact predecessor controller SHA `e662684` passes ordinary CI `31356737236` with repository `93357718684` and fixed-Linux visual `93357718640` at `94/94`. | Yes; P7-01 may audit and then implement only distinct Trial Plan/Round identity and lifecycle foundations under Project-first authority. Input locking, evidence, quality/Gate authority, ERPNext, conclusions, readiness, Released Trial Summary, external projection and print policy remain inactive. |
| 2026-08-10 | Accept the bounded P7-01 audit and activate only checkpoint 1 pure domain/contract/additive metadata. | Exact P7-00 controller SHA `4865e0a` passes CI `31358008296`; repository `93361224683`, fixed-Linux visual `93361224744` at `94/94` and skipped controlled runtime establish the clean start. The audit proves no live Trial backend, identifies three unsafe unimplemented OpenAPI placeholders and freezes immutable Plan revisions, distinct planned Rounds/events, governed work links, fail-closed reservation truth and four checkpoints. | Yes; checkpoint 1 may add pure domains, corrected closed Project-first schemas, exact ownership, five guarded additive DocTypes and direct tests only. No route, business row, fixture, UI, lifecycle beyond planned-state rules, confirmed reservation, quality/Gate effect or ERP contact is active. |
| 2026-08-10 | Accept P7-01 at Level 2 and activate the independently ordered Delivery Pipeline Optimization audit while pausing P7-02. | Exact checkpoint `78efa3e` passes final unchanged workflow `31380834335`; repository `93430635765`, `97/97` fixed-Linux visuals and controlled Site `93430635851` prove immutable Plan succession, a distinct planned Round, governed task linkage, replay/conflict/rollback/IDOR, route recovery, two migrations, unavailable reservation truth and zero ERP/integration traffic. Serial repairs corrected only evidence-proved JSON persistence and closed verifier-payload boundaries. | Yes; retain all Plan/Round/link/receipt/audit history and disable P7-01 routes only through reviewed forward repair. `FR-TR-001` remains resource-reservation-held. Delivery Pipeline Optimization may change delivery mechanics only, must preserve every Gate and pass Level 3 before P7-02 resumes. |
| 2026-08-10 | Freeze and implement the Delivery Pipeline Optimization plan without reducing any Gate. | P7-01 evidence measures a roughly `10m24s` serial repository critical path, a `3m35s` visual job and a `4m46s` cumulative Site. A controlled dispatch repeats the already successful ordinary Gate; independent Python, frontend/E2E and secret checks are serialized; repeated Phase 6/P7 artifacts isolate eighteen global baseline changes to the valid footer catalog fingerprint. `implementation/CURRENT_TASK.json` freezes exact paths, checks, state and rollback before code. | Yes; split only complete independent lanes, permit Level 2 Site reuse only after exact-SHA successful PR-Gate verification, retain a complete Level 3 mode, assert the real catalog version before screenshot-only normalization and fail closed on task/controller/path drift. Revert these delivery-only commits before P7-02 if the final Level 3 Gate does not pass. |
| 2026-08-10 | Accept Delivery Pipeline Optimization at Level 3 and remove the ordered P7-02 hold. | Exact implementation SHA `22cb24d` passes ordinary PR CI `31388734891` and complete Level 3 `31392474781`; evidence SHA `fbac85b` passes CI `31393689222`. All four ordinary lanes, both secret boundaries, `97/97` fixed-Linux visuals, controlled runtime and the release-gate review pass without reducing product or Gate truth. | Yes; retain the optimized workflow and exact-SHA verifier. Resume only P7-02 under a newly frozen product-task manifest; no broader Phase 7 authority follows from the delivery change. |
| 2026-08-10 | Accept the bounded P7-02 audit and activate only checkpoint 1 domain/contract/additive metadata. | The audit reconciles `FR-TR-002/003/010`, `FR-NP-004/005` and `FR-TX-019` against P7-01, the controlled private-file pipeline and the disjoint process-data layers. It freezes exact immutable Round input locks, versioned manual Actuals, stable Sample Batch revisions and exact clean private evidence references while preserving one NPI owner and explicit unavailable external truth. | Yes; checkpoint 1 may add pure domains, closed contracts, ownership, four guarded additive DocTypes, translations and focused tests only. No live route, business row, file, fixture, UI, automatic machine import, formal ERPNext quality, conclusion, Gate, approval or release authority is active. |
| 2026-08-10 | Accept P7-02 checkpoint 1 and activate only checkpoint 2 repository/BFF/private-file/lifecycle behavior. | Exact stable SHA `37a4d9d` passes ordinary CI `31399227239`: `1,510` tracked Python, `822/822` frontend unit, `359/359` non-visual E2E, `6,130` complete direct trilingual sources, both secret boundaries and unchanged `97/97` fixed-Linux visuals. The boundary is additive and activates no route, business row, File write, lifecycle or UI. | Yes; checkpoint 2 may activate only Project-first execution reads and prepare/start/manual Actual/Sample/pending-upload/clean-evidence commands with exact containment, CSRF, actor-bound replay, one transaction, append-only audit/cleanup and an independent fail-closed switch. UI, runtime fixture, automatic import, ERPNext quality, conclusion/Gate/approval/release, Tooling lifecycle and reservation remain inactive. |
| 2026-08-10 | Accept P7-02 checkpoint 2 and activate only checkpoint 3 live Trial execution workspace. | Exact SHA `318f1c8` passes ordinary CI `31405749894`: `1,521` tracked Python, `822/822` frontend unit, `359/359` non-visual E2E, `6,139` complete direct trilingual sources, both secret boundaries and unchanged `97/97` fixed-Linux visuals. Repository/API/seam tests prove Project-first authorization, exact released-reference containment, planned/prepared/running commands, actor-bound replay, append-only Actual/Sample history, pending private upload, clean evidence and audited URL-free byte access. | Yes; checkpoint 3 may compose only the strict execution data source and dense trilingual locked-input/parameter/sample/evidence workspace with honest loading, empty, pending-scan, read-only, permission, validation, conflict, processing, retry and unavailable-import states plus affected Linux visuals. Controlled runtime, automatic import, ERPNext quality, conclusion/Gate/approval/release, Tooling lifecycle and reservation remain inactive. |
| 2026-08-11 | Accept P7-02 at Level 2 and activate only the bounded P7-03 audit. | Exact product SHA `3a26719` passes complete ordinary PR CI `31432120639` and optimized exact-SHA controlled Gate `31432837104`; together they prove `1,524` tracked Python, `832/832` unit, `365/365` E2E, `6,311` direct trilingual sources, `100/100` visuals, both secret lanes and cumulative disposable-Site execution through P7-02. Runtime repairs preserve byte integrity, Frappe storage ownership and canonical semantic replay. | Yes; retain immutable input/Actual/Sample/File/evidence/lifecycle/receipt/audit history and reconcile the six Requirements only to their truthful manual/foundation dispositions. P7-03 may audit and freeze Round/cavity defect lineage, responsible actions, target Round and independent verification, but no product code starts before the plan; automatic NCR, formal quality, Gate, Tooling lifecycle, conclusion, approval and release remain inactive. |
| 2026-08-11 | Accept the bounded P7-03 audit and activate only checkpoint 1 domain/contract/additive metadata. | Starting controller SHA `135d083` passes ordinary CI `31434848448`. The audit finds exact Trial Round/input/Sample/cavity/evidence foundations in P7-02 and one stable NPI Tooling defect identity in P6-05, but no Cavity Result, exact Trial successor, action target Round or independent verification. A parallel Trial defect would violate single-object ownership. | Yes; continue the same stable `defectGlobalId` across immutable P6/P7 successor stores with one cross-store tip, add exact cavity results and separate verification attempts, and activate only pure domains, closed contracts/ownership, three guarded additive DocTypes, translations and tests. Routes, rows, UI, runtime, NCR, formal quality, Gate, Tooling lifecycle, conclusion, approval and release remain inactive. |
| 2026-08-11 | Accept P7-03 checkpoint 1 and activate only checkpoint 2 repository/BFF/single-tip behavior. | Exact product SHA `42812c3` passes ordinary CI `31438191274`: complete repository, frontend/E2E and secret lanes plus the unchanged `100/100` fixed-Linux visual matrix. The additive boundary defines exact cavity results, one cross-store defect successor, exact action targets and independent verification without activating a route or business row. | Yes; checkpoint 2 may activate only Project-first quality reads and exact cavity-result/defect/verification commands with transactional P6-to-P7 single-tip enforcement, actor-bound replay, one transaction, append-only audit and an independent fail-closed switch. UI, runtime, NCR, formal quality, Gate/Tooling lifecycle, conclusion, approval and release remain inactive. |
