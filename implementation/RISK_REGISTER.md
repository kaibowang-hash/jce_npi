# Risk Register

| ID | Risk | Impact | Mitigation | Status |
|---|---|---|---|---|
| R-001 | A fresh Codespace entered Alpine recovery because the target Dockerfile inherited a Yarn APT source with an unavailable signing key. | Target-container creation and Phase 3 were blocked. | Sanitize both `/etc/apt/sources.list` and `/etc/apt/sources.list.d/*yarn*` before APT refresh, lock/verify all remote artifacts, then verify the rebuilt target dynamically. | Resolved — fresh target and dynamic gate passed 2026-07-21 |
| R-002 | Large V1.2 scope can hide trace gaps. | P0/P1 omission | Retain the immutable 229-ID DOCX extraction, checkpointed coverage crosswalk, exact original-Pack ID-set digest, current 282-ID typed trace, per-task anchors and Phase gates; preserve the historical 281-row R1-01 evidence. | Mitigated by R1-01 plus the append-only 2026-07-27 FR-UX-043 correction; remains under continuous trace review |
| R-003 | Siemens component defaults may violate square/neutral baseline. | UI gate failure | Keep Siemens imports behind the local adapter, generate company tokens, reject raw geometry/style violations and run computed-style plus exact zero-tolerance visual assertions. Shared catalog hash/copy changes require a global baseline regeneration rather than only new-page images. | Mitigated — R1-03 passed the clean 201-case zero-tolerance visual matrix after 195 existing and 12 new baselines were regenerated; the cumulative R1 bridge matrix remains mandatory |
| R-004 | Translation fallback can mask missing Chinese. | Language gate failure | Require direct complete `zh` and `zh-TW` catalogs, missing/placeholder/context/terminology checks, DOM/shadow-root mixed-language scans and normal-user runtime proof. | Mitigated — R1-03 passed 2,539 direct entries per Chinese locale, 244-case non-visual browser coverage and 201-case exact visual coverage on 2026-07-26; Phase 3 business UAT remains separately unsigned |
| R-005 | ERP retry/replay can duplicate execution. | External data corruption | Idempotency, inbox/outbox, expected version, reconciliation and fault tests | Open |
| R-006 | No production ERP access for final activation. | External go-live dependency | Mock plus sandbox-ready adapter and activation runbook | Accepted external |
| R-007 | A Codespaces clone may retain generated Git LFS hooks even though the repository has no LFS attributes or pointer objects. | Push fails when the unused `git-lfs` binary is absent. | Verify attributes and reachable history before removing only clone-local generated hooks; require approved attributes and reproducible installation before any future LFS adoption. | Mitigated — current clone residue removed |
| R-008 | A valid immutable base image can still reference time-sensitive external package repositories whose signing configuration later expires. | Reproducible container creation can fail before Feature or post-create execution. | Remove the unused Yarn APT source from both supported locations, forbid signature/trust bypasses and validate base/Feature/tool artifacts against official registries before delivery. | Mitigated — repaired build and dynamic proof passed |
| R-009 | Privilege elevation can sanitize the Node Feature PATH during lifecycle commands. | npm CLIs with `/usr/bin/env node` fail even though Node is installed for the remote user. | Install fixed npm tools through the verified writable remote-user prefix; statically reject `sudo npm`; rerun the exact lifecycle script in a fresh target. | Resolved in repair round 4 |
| R-010 | The main JavaScript asset is 1,091,572 B / 273,498 B gzip after P4-05, above Vite's visible 500 kB warning; lazy Project, live My Work and Gate routes are 74,308 / 17,458, 11,763 / 3,936 and 57,650 / 13,716 B raw/gzip. | Later shared catalogs and business modules could worsen startup and interaction performance. | Keep the warning enabled, measure every vertical slice, retain route/data-boundary code splitting, and perform a dedicated shared-shell/catalog loading review before production release. | Open — Phase 4 growth is measured debt; the warning and threshold remain unchanged |
| R-011 | Phase 3 business UAT and provenance-backed sanitized representative data are unavailable. | Technical fixtures could be mistaken for accepted usability or real operational evidence. | Keep acceptance at `TECHNICAL_PASS_PENDING_UAT` after the technical release gate; label fixtures, use the unsigned UAT script, and request all external evidence only through `REQUIRED_INPUTS.md`. | Open external — scoped; not a global blocker |
| R-012 | Prototype or unavailable notification, integration, telemetry and delivery states can be overclaimed as live service behavior. | False readiness, missed integration faults or invalid performance/localization conclusions. | Preserve explicit unavailable/prototype states and trace statuses; use the strict live Project/My Work BFF, server-resolved Due time zone and distinct System time zone, while leaving notification, integration-exception, telemetry ingestion and delivery to their owning phases. | Open — Project and My Work/time-zone boundaries are proven; remaining services stay explicitly bounded |
| R-013 | Pack trace allocation can be read as putting portfolio, portals, notifications and ERP-owned cost into Phase 4 although controller Phase 4/M3 is Project and Gate. | Scope creep or guessed ERP behavior could enter the first business slice, or the later portal decision could accidentally remove internal collaboration scope. | Use the `P4-00` requirement anchor and trace remap; deliver ERP creation/cost in Phase 8, retain internal supplier/customer evidence and notification foundations in V1.2, and mark only FR-CO-003/004 external login/identity/self-service portal surfaces `USER_APPROVED_POST_V1_2_DEFERRED` without changing their IDs, source or `REMAPPED_PHASE_9` status. | Mitigated — Phase 4 anchor passed 2026-07-22; external-only portal deferral approved 2026-08-26 with a closed future-release restoration trigger |
| R-014 | A session-language mutation could bypass CSRF, fail before the domain error boundary, or leave a partial preference update while returning a non-success response. | Cross-site preference changes, raw Frappe errors, false retry semantics, or inconsistent session state. | Deliver the CSRF token only through authenticated no-store bootstrap, strip caller-supplied browser tokens, require the trusted token on `PUT`, normalize pre-handler malformed/CSRF failures to Problem Details, validate exact fields, roll back every non-2xx domain result, and preserve the original request locale until save plus cache invalidation succeed. | Mitigated — 58 Python tests, 110 frontend tests, cache-failure rollback/request-locale proof, and loopback missing/wrong-CSRF, malformed/field, unchanged-state, trace, and cleanup checks passed 2026-07-22 |
| R-015 | Prototype telemetry could accept arbitrary routes or identifiers and leak object/customer context. | Privacy breach or misleading operating evidence. | Restrict name, outcome, timestamp, duration, context-switch count, and route to finite validated values; reject query, hash, identifier, traversal, encoded, and non-finite variants; keep the recorder in memory only. | Mitigated for the Phase 3 prototype — unit validation passed; no live ingestion or durable metric is claimed |
| R-016 | Missing production numbering, template, RACI/approval, WorkItem lifecycle, health, exception, invalidation and Project lifecycle policies could be disguised as product defaults. | Incorrect approvals, false health, invalid Gate decisions or incompatible production data. | Keep rules versioned and fail closed when absent; install no production template/control policy; use clearly synthetic acceptance fixtures; maintain every recorded Class-B hold in the Phase 4 anchor and `REQUIRED_INPUTS.md`. | Open external — scoped; the generic versioned Project/Gate/control infrastructure passed Phase 4 without inventing production rules |
| R-017 | Concurrent Project retries, duplicate business codes, partial template instantiation, standalone child mutation, or tenant/IDOR access could corrupt or disclose the first live aggregate. | Duplicate or incomplete Projects, rewritten history, or cross-project/cross-tenant disclosure. | Use tenant-scoped business-code reservation, first-write idempotency with rollback/reload on races, one atomic Project/Gate transaction, exact immutable template snapshots, guarded child/history DocTypes, strict CSRF/BFF fields, fail-closed per-Site tenant configuration, owner/System Manager authorization, audit and trace IDs. | Mitigated for P4-01 — domain contract permission concurrency migration and real Frappe runtime checks passed 2026-07-23 |
| R-018 | A Domain WorkItem cursor could be forged, validated before Project authorization, or cause a read request to auto-provision Site configuration; related records could share a Project ID but belong to another tenant. | Pagination manipulation, object-existence disclosure, unexpected configuration mutation, or cross-tenant association. | Sign every cursor field with an existing Site-bound domain-separated key, authorize before key/shape/signature checks, fail 503 without querying or writing when the key is unavailable, and compare both Project and tenant on tenant-bearing identities and references. | Mitigated for P4-02 — affected API/repository tests and fresh real Frappe runtime passed 2026-07-23 |
| R-019 | The authoritative temporal rule for disabled members' existing or future role and substitution relations is unavailable. | Stale responsibility or unintended assignment could be retained or removed if a convenience rule is invented. | Permit only identity-preserving non-expansive membership end-dating; retain the broader role/substitution decision as a Class-B hold in the Phase 4 anchor and external input request. | Open external — scoped; does not block safe-default-denied P4-04 work |
| R-020 | A raw or mutable Frappe File reference, equal-content URL reuse, fabricated scan state, or cross-Project revision could be mistaken for controlled Gate evidence. | Unauthorized file access, mutable audit history, false cleanliness, or evidence attached to the wrong Project/tenant. | Persist and revalidate the exact private Frappe File identity, content hash, name, URL identity, size, SHA-256 and revision; expose only URL-free metadata; accept scanner state only through the controlled flag; require exact same-Project/same-tenant source resolution and append-only references. | Mitigated for P4-03 — focused controller/repository/security tests and fresh real runtime passed 2026-07-24; production scanner/provider and normal-user file routes remain future reviewed scope |
| R-021 | P4-04 reviewer writes need a narrow normal-user transport permission without turning `System Manager`, a Project role, or a RACI assignment into approval authority and without using `ignore_permissions`. | An unsafe shortcut could grant approval power, expose Desk CRUD, or deadlock every legitimate review. | Keep `NPI API User` non-Desk and unassigned; use controlled DocPerms only with the command repository, flags, generic-CRUD denial, tenant/Project isolation, frozen policy authority and runtime proof. Policy assignment remains the sole business authority. | Resolved for P4-04 — permission/controller tests and the complete live runtime prove generic CRUD/history denial and exact frozen authority; no production authority map is installed |
| R-022 | A large P4-04 implementation checkpoint with a live API/UI could be mistaken for an accepted Gate review service before its required Gates run. | Migration/runtime compatibility, security review, build or release checks could be promoted as PASS. | Keep P4-04 `IN_PROGRESS`; record the exact passed 72-case browser and 23-case visual matrices separately from pending coverage/build/audit, migration/runtime, Level 2 and triggered Level 3; prohibit P4-05 activation until they pass. | Resolved 2026-07-25 — complete migration/runtime, 204-case browser, 170-case exact visual, full repository, security, trace, and independent Level-3 evidence passed before P4-05 activation |
| R-023 | A browser reload, actor change, route change, or lost response during a Gate Review command could produce duplicate execution, discard the only reconciliation key, or show success in the wrong Gate context. | Users could act on stale authority or a false success while the server committed a command. | Coordinate one actor/Gate command, persist only a bounded non-sensitive receipt marker, retain it for uncertain outcomes, reconcile against its original Gate, wait for an old-actor command to settle, never auto-replay, and reload authoritative state without optimistic success. | Resolved for P4-04 — unit/component and complete browser matrices cover receipt loss, actor/route changes, processing, conflict, retryable/final failure, one alert, and authoritative reload without optimistic success |
| R-024 | The local controlled development, MariaDB or Redis containers can remain stopped behind a stale Docker OCI task after a host/runtime interruption. | Final migration, real Frappe verification or browser evidence cannot execute, and stale historical runtime evidence could be misreported as current. | Treat preflight failure as no product result; resolve exact container/task/process ownership; preserve named containers and volumes; never reset the database; then require healthy services, focused replay of the interrupted case and fresh complete affected Gates. | Mitigated recurrent risk — on 2026-07-27 exact-ID starts restored the preserved services without deletion/reset; deterministic stale-projection reconciliation and the complete runtime Gate passed; after a later three-container browser interruption the exact crashed case passed, the clean 251-case matrix passed, and final development/service health was confirmed |
| R-025 | A newly published transitive npm vulnerability can invalidate an earlier zero-finding audit, while its only patch may require a newer Node engine. | A release could ship a known denial-of-service path or become stranded on an end-of-life toolchain. | Fail the current Gate, follow ADR-011, pin Active LTS Node/npm, upgrade only vulnerable parent tool families, require `brace-expansion@5.0.8`, exact install-script approval, clean `npm ci`, both audits and fresh-target plus full Level 3 evidence. | Resolved 2026-07-25 — exact Node 24/npm 11 fresh-target proof, clean strict install, no pending scripts, patched dependency tree, both zero-finding audits, and the complete Level-3 Gate passed |
| R-026 | An authenticated upstream-metadata request can scope its first-hop header correctly but leak the token when the HTTP client copies `Authorization` across a redirect. | A malicious or compromised redirect could disclose the repository's GitHub credential outside the approved API origin. | Add credentials only for exact HTTPS `api.github.com` with default port and no userinfo; fail closed before constructing any authenticated redirect outside that origin; test cross-host, downgrade, non-default-port, and lookalike redirects. | Resolved 2026-07-25 — independent reproduction returned the Gate to implementation; 19 focused tests and the final 417-test Level-3 rerun passed |
| R-027 | A derived My Work assignment index can become stale, retain terminal work, or be mistaken for authorization. | A user could see another actor's responsibility, follow an invalid target, or miss current work. | Treat the index only as a locator; refresh it in exact source transactions; revalidate source, tenant, Project, actor assignment, membership, capability, terminal state and route at query time; fail closed on malformed sources; provide atomic idempotent rebuild and route-disable recovery. | Mitigated for Phase 4 — 296-source/184-row/127-active rebuild, injected rollback, terminal/reassignment deactivation, malformed-source handling, operational-error propagation, IDOR tests and complete live runtime passed |
| R-028 | A controlled-document implementation can collapse business revision identity into a mutable Frappe File, trust a raw private URL, fabricate scan/preview state, or let an expired share bypass Project authorization. | Released evidence could be overwritten, confidential files disclosed, or unsafe content represented as releasable. | Keep Controlled Document, Document Revision and exact File Revision identities separate; authorize before resolving protected objects; compute/revalidate hashes server-side; retain scanner/provider truth; expose no stable raw URL; audit downloads; make grants expiring/revocable and non-authorizing until the approved external identity policy exists. | Mitigated through P5-02 — exact release-time private identity/bytes/hash/format/size/scanner truth and deletion protection passed complete normal CI and controlled runtime; external identity/retrieval remains held |
| R-029 | A generic or Mock Execution Request can be mistaken for completed ERP Item/MBOM publication, especially after partial node success. | False formal mappings, duplicate Items/BOMs, or NPI overwrite of ERP-owned manufacturing truth. | P5-05 must use operation-specific exact-version contracts, Mock-default/no-production-host enforcement, actor-bound idempotency, payload hash, approval evidence, per-node result/mapping truth and no aggregate success for partial outcomes; Phase 8 owns real adapter/retry/replay/reconciliation. | Open — scoped Phase 5 foundation; real ERP acceptance remains Phase 8 |
| R-030 | Alias IDs, normalized IDs and addendum IDs can be double-counted, silently dropped, or used to relabel historical Gate evidence. | False coverage or duplicate implementation could pass a Gate. | Use `PACK_CANONICAL`, `DOCX_RECONCILED` and `ADDENDUM_DIRECT`; verify the current 173/95/14 distribution and exact pre-Pack ID digest; link aliases through `canonical_ids`; never backfill historical PASS evidence. | Mitigated by R1-01 machine checks and append-only FR-UX-043 coverage; ongoing |
| R-031 | Customer Tooling XLSX can contain macros/XLM, external links, archive collisions, entity expansion, corrupt members, formula errors, ambiguous regions/images, sensitive values and unapproved column semantics. | Code execution, resource exhaustion, data leakage, guessed relationships, false capacity/import results or unsafe rollback. | Use the bounded passive `xlsx-tooling-import` inspector, immutable hash/provenance, eight-step preview pipeline, raw retention, human confirmation, idempotent partial truth, correction artifacts and scoped DR-REC-007/008 holds. Never execute formulas or contact production ERPNext. | Active P6-07 implementation risk; bounded audit plan recorded and checkpoint 1 productizes the R1-01 parser safety foundation |
| R-032 | Supplied brand marks could be used outside their CSV scope, recolored, treated as a new palette, or activated in the wrong delivery phase. | Brand/legal misuse, industrial UI regression or technical-identity migration churn. | Verify exact brand file set/hashes, self-contained SVGs and bounded PNG dimensions; centralize display adapters; keep logo colors a narrow asset exception; retain teal/neutral UI tokens and stable internal codes; activate `Core.png` only in Phase 8/M7-09. | Mitigated for R1-02 by exact asset/context/build guards and trilingual visual evidence; Phase 8/M7-09 `Core.png` activation risk remains open |
| R-033 | Exact Tooling Requirement, Revision and physical Set state machines could be invented during reconciliation. | Wrong lifecycle, authorization, skip or terminal behavior becomes embedded before business approval. | Freeze only distinct identities and non-collapse invariants; keep exact states/transitions/authorities under DR-REC-010 and the Phase 6 requirement anchor. | Open scoped decision; does not block identity/provenance/parser work |
| R-034 | A generic preference endpoint, caller-selected user/key, unconfirmed client state, route-label create command or unsafe return target could turn Shell personalization into cross-user mutation, false authority or open redirect. | Preference leakage, CSRF exposure, false saved state, unauthorized creation or context escape. | Bind the actor to the authenticated Frappe session and one fixed namespaced key; require trusted CSRF and exact boolean payloads; retain last confirmed state and re-bootstrap before retry; expose quick-create only after server capability and recheck at destination; allow only normalized approved internal return routes. | Mitigated for R1-03 — Python/frontend/runtime user-isolation, failure-reconciliation, capability, adversarial return-target and independent security reviews passed; no generic preference surface exists |
| R-035 | Grid personalization or published-view foundations could accept caller-selected identity/schema, stretch persisted widths, drop queued edits, count Unicode inconsistently, restore inaccessible filters, permit nested evidence mutation/cycles, corrupt revision lineage, or infer publisher authority. | Cross-user preference leakage, false saved state, hidden work, unstable geometry/hashes, rewritten shared-view history or unauthorized publication. | Use one fixed actor/tenant-bound BFF; closed ordered grid/view/column/filter schemas; exact pixel/ARIA geometry; immutable version-confirmed FIFO writes with bounded same-view coalescing; Unicode code-point limits; Project-access sanitization; authoritative reload/rollback; recursive JSON copy/freeze and cycle rejection; immutable lineage; generic-CRUD denial; and fail-closed publisher/export/bulk capabilities. | Mitigated for R1-04 — final backend/frontend/runtime/browser and independent re-review passed with no unresolved finding; the exact UX-028 publisher-authority policy remains an explicit scoped hold |
| R-036 | A generic or caller-selected pane preference, stale drag or write completion, responsive-breakpoint persistence, or collapse that hides focus could mutate another actor's layout, resurrect obsolete geometry, overwrite a deliberate desktop preference or strand keyboard users. | Cross-actor preference leakage, false confirmed state, unstable pane geometry or inaccessible hidden focus. | For FR-UX-040, use one fixed authenticated My Work inspector route with server-owned actor, pane identity, schema and storage key; require trusted CSRF and a closed bounded payload; keep drag changes as bounded preview with one release commit and capture-loss cancellation; serialize generation-bound writes, ignore stale completion, restore the last confirmed state on failure and require explicit reload; keep responsive stacking presentation-only; preserve selection/filter/scroll and move focus to the visible expand or recovery action. | Mitigated for FR-UX-040 by the R1-05 Stage 1 contract, runtime, unit, browser, accessibility, trilingual and zero-tolerance visual evidence; FR-UX-041 and FR-UX-043 passed their independent Stage 2 and Stage 3 Gates |
| R-037 | A field attachment control could fabricate upload or scanner success, retry an unconfirmed write, downgrade a registered revision to a replaceable local file, hide the active file identity, overclaim privacy/permission/capability truth or expose a mutable/private file URL as authority. | Operators could act on the wrong file, duplicate a server write, replace immutable evidence, disclose protected content or trust unsafe/unverified data. | Use a discriminated fail-closed state machine with injected actual transport, monotonic `none`/`unconfirmed`/`registered` write truth, runtime result validation, visible file identity, no registered detach/delete/replacement path, exact URL-free revision/hash/scan facts, explicit unavailable privacy/policy/capability values and non-clean scan capability blocking; require future upload/scanner/classification/retention/permission work to pass its own API and security Gate. | Mitigated for FR-UX-041 Stage 2 by unit/browser/accessibility/trilingual/visual and independent review; production upload, scanner, classification, retention and permission policy remain future controlled scope |
| R-038 | Compact icon actions can hide meaning, import an unapproved vendor design language, remove keyboard/focus/disabled behavior or make color/icon shape the only signal. | Operators can invoke the wrong command, miss an unavailable path or lose accessible action discovery. | Require a closed repository-owned allowlist and fail-closed eligibility policy; permit icon-only only for familiar low-risk context-clear secondary actions; retain translated accessible name/title/focus tooltip, keyboard/focus/disabled paths and visible labels for primary, high-risk or ambiguous actions; ban Primer/Octicons/react-icons imports and dependencies. | Mitigated for FR-UX-043 by policy/adapter units, boundary audit, affected browser/accessibility cases and reviewed trilingual Linux visuals |
| R-039 | Hosted CI images and third-party actions can drift: required tools may disappear, anonymous registry metadata can rate-limit, historical browser rendering can change with package/font updates, or a scanner can stop before scanning when a new token requirement appears. | A valid change can be blocked by infrastructure, or worse, a required Gate can be reported without executing its actual verifier. | Install required tools explicitly; use only the repository-scoped read-only token at exact fail-closed origins; pin the canonical visual container by digest; retain bounded artifacts; scope task visuals to reviewed affected surfaces without rewriting unrelated historical baselines; scan both the action range and complete PR branch; allow only individually reviewed historical synthetic finding fingerprints; assert every CI security/metadata/visual step and the exact ignore set in the repository verifier. | Mitigated through final P5-01 runs `30610355829` and `30610747931`; exact pins, fixed disposable identity guards, bounded artifacts, current/history secret scans and cleanup all passed |
| R-040 | A generic/client-only undo or self-approved prototype could report reversal without a server command, hide an irreversible boundary, fabricate business approval or silently enable unapproved bulk behavior. | Rewritten state/audit history, unauthorized recovery, false success or implementation before product acceptance. | Limit the prototype candidate to the current actor's closed-view My Work grid reset; keep business bulk capability false; require a new authorized/idempotent server command, exact version/hash/expiry/consumption checks and append-only audit only after an actual Product Owner approval tied to the prototype; classify all unapproved/high-risk actions as ineligible with visible forward recovery. | Mitigated for Stage 1 — prototype/no-mutation truth and the approval manifest are verified; Stage 2 remains fail-closed pending actual Product Owner approval |
| R-041 | A partial 1440 visual subset or renderer-driven bulk baseline rewrite could leave P0 pages/languages ungoverned while appearing to satisfy UX-035/036. | Layout, density, square-geometry or language regressions can enter CI without evidence, and accepted historical baselines can be rewritten without source justification. | Enforce the exact current six-page × three-language 1440×900 cross-product from one P0 registry, retain all 1366/1920/state/zoom evidence, use the fixed-digest Linux renderer with exact comparison and bounded diff artifacts, and require original-resolution review for each source-driven baseline change. | Mitigated for the current P0 registry by R1-06 Stage 3 — exact 18-image fixed-Linux governance, bounded artifacts and original-resolution review passed; future P0 registry expansion must add its governed cross-product |
| R-042 | Static verifier tests or mocked browser routes can be mistaken for a real controlled Frappe migration, transaction, private-file and replay result when the fixed disposable runtime is unavailable. | P5-01 could be promoted with unproven DocType synchronization, audit persistence, scanner observation, IDOR denial or binary integrity. | Keep P5-01 incomplete until the fixed physical repository Bench, exact disposable Site/database/user guards, two migrations, fresh and second-process replay, route-disable/recovery, bounded cleanup and no production connection all pass. Record every failed run as no runtime result and preserve all prior passing evidence. | Resolved for P5-01 by final controlled-Site workflow `30610747931`; exact nine-DocType migrations, transaction/private-file/scanner/audit/IDOR/replay/route-recovery proof and cleanup passed |
| R-043 | Canonical ISO UTC timestamp text can be written directly into Frappe `Datetime` database fields, while the fixed Frappe persistence path leaves existing strings unconverted and expects a different storage representation. | Policy publication or later Document, revision, lock, share and idempotency writes can fail with HTTP `500`; a narrow one-field workaround can hide the same shared defect until later in the Gate. | Separate canonical API/snapshot UTC formatting from one Frappe-compatible storage adapter; apply it to every affected P5 Document `Datetime` field; test aware/naive round trips, storage format and immutable comparisons; expose only sanitized server failure metadata; rerun affected checks, normal CI and the unchanged controlled-Site Gate. | Mitigated by `7aa14ed` — normal CI passed and controlled run `30573778175` passed the formerly failing policy publication and reached document checkout |
| R-044 | A sanitized runtime diagnostic can cover one setup operation but omit later command boundaries, leaving an ephemeral HTTP `500` impossible to attribute after mandatory cleanup. | Repairs may guess at transaction steps, rerun without evidence, weaken immutable/audit/idempotency requirements or exhaust bounded dispatches without identifying the root. | Reuse validated exception types plus explicit allowlisted transaction-stage codes at every checkout command assertion; exclude traceback, raw exception message, request, cookie and credential values; distinguish receipt insert, lock-event insert, projection save, audit append, response build and receipt seal before changing product code. | Mitigated for the outer transaction stage by checkpoint `954bd0d`, normal CI `30600587269` and dispatch `30600943765`, which safely proved `ValidationError / DOCUMENT_CHECKOUT_PROJECTION_SAVE`; final Gate `30601980685` disproved the exact-event selector hypothesis |
| R-045 | A checkout transaction-stage code can isolate the outer Frappe `document.save()` call while multiple closed validations and hooks inside that call still share the same `ValidationError`. | A plausible selector repair can pass unit/normal CI yet fail the controlled Site, consuming the final Gate without proving the inner root; further guessing can corrupt lock/version/audit truth. | Forward-revert the disproven candidate; retain the validated `code / exceptionType / traceId` boundary; require a separately authorized closed allowlist for identity/policy hydration, domain reconstruction, optimistic-version validation, exact lock-projection validation and later save hooks; repair only one proven substage and rerun affected checks, normal CI and one unchanged controlled-Site Gate. | Mitigated by checkpoint `57b4314`, diagnostic run `30604964265` and repair `7dc4dc0`, which proved and closed the revision substage without weakening non-empty successor checks; final Gate `30605683679` advanced to the new R-046 boundary |
| R-046 | A post-checkout document revision/upload request can cross private File creation, immutable File Revision hydration, Document Revision/association persistence, document projection, audit, response and receipt sealing while a generic `PdfStreamError` identifies none of those boundaries. | Treating a filename signature as a valid PDF, weakening controlled-file validation or changing a plausible transaction step could manufacture a Gate result, corrupt immutable file/revision truth or consume another controlled run without proving the root. | Retain the exact `code / exceptionType / traceId` non-disclosure boundary; use closed revision/upload transaction-stage codes; repair only uniquely proven product roots; require valid fixture preconditions and the unchanged final Gate without changing file-integrity, permission, version, audit, idempotency or transaction rules. | Resolved for P5-01 — run `30608055245` proved the private-file validation boundary; a structurally valid JavaScript-free PDF fixture preserved product validation, and final controlled workflow `30610747931` passed |
| R-047 | Frappe GET query values arrive as strings even when an OpenAPI field is an integer, so reusing a JSON-body-only type parser can reject a valid reverse relationship filter at runtime while mocked calls pass. | A valid typed relationship query can return a false validation failure, and broad coercion could admit signs, leading zeros, decimals, whitespace, booleans or overflow. | Keep body and query parsers separate; normalize only canonical decimal query text to the bounded positive integer domain; reject every noncanonical form; cover transport, parser, API and real controlled-Site behavior without changing the public schema. | Resolved by `5a9cd3d`; focused strict-parser tests, complete normal CI `30610355829` and final controlled-Site workflow `30610747931` passed |
| R-048 | Production review roles, approval counts, delegation, confirmation strength, replacement rules, effective dates, and scanner-provider details are not approved. | Inferring roles or weakening integrity could create false approval/release authority or diverge from V1.2. | Use a separate immutable Project-scoped release-policy version with exact synthetic user bindings and only fail-closed integrity and replacement rules; install no production policy, call records authenticated electronic confirmations rather than regulated signatures, and keep dependent production behavior unavailable. | Mitigated for the P5-02 mechanism — exact synthetic policy, independent authority, immutable confirmation and fail-closed integrity passed Level 2; production facts remain held |
| R-049 | Updating the immutable P5-01 revision snapshot to display review/release state would rewrite retained history and couple release failure to content identity. | Released content could appear overwritten, historical hashes could drift, or rollback could reopen a retained revision. | Keep the P5-01 revision/file association immutable; append review/confirmation/lifecycle events and advance only a separate guarded lifecycle projection with exact optimistic versions. | Resolved for P5-02 — append-only lifecycle/review/confirmation history, guarded projection, rollback and controlled-runtime replay passed at `f088d70` |
| R-050 | Protecting only `NPI File Revision` does not prevent deletion of the underlying released Frappe `File`. | A released revision could retain metadata while its binary disappears. | Add a Frappe `File.on_trash` server hook that fails closed when an exact referring File Revision is released, retain the existing dependency hook, and prove deletion rejection on the controlled Site. | Resolved for P5-02 — exact released association/hash resolution and controlled-Site Frappe `File` deletion rejection passed in workflow `30661586342` |
| R-051 | A server-generated draft release-policy snapshot can be mistaken for caller-owned immutable input when publishing the same policy version. | Legal draft-to-published transition can fail despite unchanged caller intent, or a broad workaround could admit tampered policy snapshots. | Accept only the exact prior server-owned draft snapshot/hash during the guarded transition, recompute the canonical published snapshot, continue rejecting caller tampering, and retain publish-once immutability. | Resolved by `c2b69d0`; focused checks, complete CI and final controlled runtime passed without changing the public policy contract |
| R-052 | A runtime verifier can compare a logical File Document ID directly with a Controlled Document ID even though their exact relation is stored through `NPI Document Revision File`. | Product behavior can pass while a synthetic precondition reports repeated false blockers or targets the wrong File Revision. | Resolve the immutable association through exact Document Revision/File link plus expected hash, assert released/tamper preconditions explicitly, and statically reject remaining direct cross-namespace comparisons. | Resolved by `7040dbc` and `f088d70`; final scan and controlled-Site workflow `30661586342` passed |
| R-053 | Production baseline authority, G2/G5/G6/ECN contents, retention/replacement rules and the document-to-downstream dependency matrix are not approved. | A convenience default could create unauthorized packages, false completeness or invalid impact decisions. | Require a separate exact Project-scoped published baseline policy with explicit synthetic actor bindings, install no production policy/content map, accept only explicit exact dependency registration and keep unknown relationships unavailable. | Open scoped production hold; P5-03 generic immutable infrastructure passed Level 2 without installing or inferring these facts |
| R-054 | Treating a mutable/latest revision, raw URL or unreleased File as baseline evidence can silently change a prior package or Gate decision. | Historical evidence can drift or unsafe content can appear approved. | Server-resolve only exact currently released Document Revision lifecycle/release snapshot and every private File Revision/hash/scan fact, canonically hash the complete member set, reject overwrite/delete and attach only the immutable baseline hash through Gate evidence. | Mitigated for P5-03 — exact create/attach, replay/conflict, migration and controlled runtime passed at `302b1e9` |
| R-055 | Successor creation can over-invalidate unregistered objects, partially append impact history or invent a second review authority. | Unrelated Gates can be blocked, prior decisions can be obscured or revisions can commit without matching impact truth. | Create dependencies only during explicit exact-baseline Gate attachment; append deterministic old/new lineage atomically during successor creation; include it in the existing Gate input hash and reuse the existing frozen Gate Review invalidation/successor cycle. | Mitigated for P5-03 — registered/unregistered successor, atomic impact lineage and existing Gate Review invalidation/successor resolution passed controlled Gate `30991177478` |
| R-056 | A broad baseline-create diagnostic or speculative repair could disclose protected runtime material, consume evidence without proving a root, or change the frozen transaction contract. | Sensitive data could leak, an incorrect repair could mask the real defect, or P5-03 could drift from FR-DS-006. | Permit only closed allowlisted stage codes, validated exception types and exact trace IDs; exclude exception text, traceback, request/response, Cookie, credentials, business data and storage paths; require affected tests plus complete ordinary CI before every diagnostic dispatch; cross-validate a uniquely proved root against the Requirement anchor, OpenAPI, real DocType fields, permissions and transaction invariants; close diagnostics before the final unchanged Gate. | Mitigated — historical safe diagnostic evidence is retained; exact-SHA final Gate `30991177478` passed with diagnostic activation closed |
| R-057 | A successful baseline create can still fail the controlled verifier at the aggregate response-contract stage, where several exact identity, policy, member, file, privacy and replay predicates share one `RuntimeError`. | Guessing one response field could weaken FR-DS-006, conceal a server-response defect, spend another uncontrolled Site run or falsely mark P5-03 complete. | Retain the closed `code / exceptionType / traceId` result; use only a behavior-neutral allowlisted predicate ladder; require affected and complete ordinary CI; repair only uniquely proved predicates; remove diagnostic activation before the final unchanged Gate; preserve the exact OpenAPI response, immutable member/file facts, privacy boundary, idempotency header and unchanged PASS criteria. | Resolved for P5-03 — ordinary CI `30990594281` and final unchanged controlled-Site Gate `30991177478` passed exact SHA `302b1e9`; Level 2 evidence is retained |
| R-058 | A time-varying online dependency audit or another hidden `verify.sh` subcommand can fail complete ordinary CI while the non-disclosure boundary permits only structured step metadata. | A diagnostic dispatch could run without its mandatory green prerequisite, or an unproven dependency upgrade could introduce scope, license, lockfile or security drift. | Keep the controlled Site skipped while ordinary CI is red; prove a bounded root without lowering or skipping audit; apply only the required lockfile correction; rerun complete ordinary CI before any diagnostic dispatch. | Resolved for the P5-03 sequence — the audited transitive lockfile correction restored ordinary CI; exact-SHA run `30990594281` passed `verify.sh`, both audit scopes and the complete Gate without exposing raw logs |
| R-059 | Production EBOM numbering, stable line identity, quantity precision, UOM, alternate/effectivity semantics, attribute set, review/release authority and formal Item conversion are not approved. | A convenience default could become an unauthorized production policy, create unstable comparison keys, misuse stock UOM or move formal Item/MBOM ownership into NPI One. | Install no production policy; require one exact published versioned policy; allow only visibly synthetic namespaces, caller-supplied stable line keys, fixed deterministic fixtures and independent exact authority bindings; keep formal Item Code, stock UOM, MBOM, routing and execution owned by ERPNext. | Mitigated for the P5-04 technical mechanism by exact Level 2 checkpoint `2c0734a`, ordinary CI `31089637022` and final Gate `31090154694`; production facts remain an open scoped hold |
| R-060 | Comparing an EBOM against a mutable latest pointer, matching by description/position or overwriting a released revision can make prior review and downstream publish preparation drift. | Added/removed/quantity/substitution/attribute results could change retrospectively, conceal actual changes or feed the wrong structure to P5-05. | Freeze immutable canonically hashed revision/line snapshots; match only exact same-EBOM stable line keys; compare two explicit revision IDs; return deterministically sorted typed changes; keep lifecycle/events separate and deny update/delete of released history. | Resolved for P5-04 — immutable explicit revisions, deterministic typed comparison, delete guards and final controlled Gate `31090154694` passed at exact SHA `2c0734a` |
| R-061 | A fixed-Bench policy fixture can collapse root construction, root insert, version construction, version insert, publish and persistence into one `BenchFixtureError`. | A plausible fixture edit could be guessed from local code, weaken the guarded policy boundary or consume another controlled run without proving which operation failed. | Emit only closed fixture substages, a validated exception type and the deterministic exact trace; accept only the exact allowlisted final child line and discard all other stderr. Require complete ordinary CI before one diagnostic dispatch, then repair only a uniquely proven synthetic fixture operation and retain an unchanged final Gate. | Mitigated — checkpoint `217632f`, ordinary CI `31016624361` and diagnostic workflow `31017098820` safely proved `P504_RUNTIME_POLICY_VERSION_PUBLISH_FIXTURE / ValidationError` while all earlier fixture and predecessor stages passed. |
| R-062 | A policy draft's server-owned canonical snapshot hash can be treated as caller-supplied published-state truth before the controller reaches its exact-prior-hash transition allowance. | Legal draft-to-published publication fails; clearing hashes broadly could instead admit tampering or weaken publish-once immutability. | During published domain reconstruction, omit only a value that exactly equals the persisted prior draft hash; leave the document value intact for `_apply_policy()` to accept only exact prior/new canonical hashes; retain tamper, unrelated-hash and already-published rejection tests. Require affected checks, complete ordinary CI and one final unchanged controlled-Site Gate. | Mitigated — bounded repair `d21d21a` passed EBOM `58/58`, complete Python `954/954` and ordinary CI `31020190868`; controlled Gate `31020886002` passed policy publication and advanced to the new EBOM create stage. |
| R-063 | The aggregate `P504_RUNTIME_CREATE` stage covers policy load/authority, idempotency, root/revision/line/lifecycle persistence, projection save, audit, response and receipt sealing. | Guessing from one `HttpStatusError` could weaken an invariant, spend uncontrolled Site runs or falsely classify a verifier failure as product behavior. | Preserve the exact failed Gate; add only closed response-neutral create substages, validate exception type and trace, require affected/full ordinary CI before at most one diagnostic Site, and repair only one uniquely proven in-scope root before one final unchanged Gate. | Mitigated — checkpoint `233b23f`, ordinary CI `31084462702` and Site `31085013974` proved `P504_CREATE_AUDIT_APPEND / PermissionError`; repair `1fda74a` added only the existing internal audit flag to authorized EBOM scopes, passed ordinary CI `31086008989` and advanced final workflow `31086562000` beyond create to submit-review. |
| R-064 | The aggregate `P504_RUNTIME_SUBMIT_REVIEW` stage covers request parsing, exact policy/authority and root/revision/lifecycle checks, idempotency, transition construction, receipt/event/projection/audit persistence, response build and receipt sealing. | Guessing from one HTTP failure could weaken review authority, transaction atomicity, immutable event/audit truth or replay guarantees, while repeated final Gates would expose only the same outer boundary. | Add one separate response-neutral transition diagnostic scope activated only on the controlled submit-review request; allow only a closed stage code, validated exception type and exact trace ID; require affected/full ordinary CI before one diagnostic Site; repair only a root uniquely proved by the tuple plus controller/DocType/domain cross-validation; close activation before one final unchanged Gate. | Resolved — repair `2c0734a` passed ordinary CI `31089637022`; final unchanged controlled-Site Gate `31090154694` passed exact SHA with diagnostic activation closed and complete P5-04 lifecycle truth |
| R-065 | A generic caller-selected execution operation and open payload can let the browser invent ERP writes or omit the exact released EBOM/approval identity. | Requests could be authorized against mutable input, violate field ownership or be replayed with a different business meaning. | Replace the accepted create seed with one closed `publish_released_ebom_item_mbom` command; server-resolve exact released revision/policy/lifecycle/release evidence and node intents; bind actor, idempotency and canonical payload hash; keep unrelated operations separately typed. | Active P5-05 implementation risk; audited plan in `implementation/evidence/phase-5/p5-05-plan.md` |
| R-066 | Mock validation or request persistence can be presented as ERPNext completion and populate invented Item/BOM identifiers. | Users may act on fake formal master data or believe manufacturing execution succeeded. | Permit only Mock in Phase 5; terminate at `validated`; forbid formal target IDs and `succeeded`; keep source/mode/state visible; create no Outbox dispatch; reserve target confirmation for Phase 8. | Active P5-05 implementation risk; no production or sandbox network access authorized |
| R-067 | Aggregate request status can hide partial node success, timeout-after-possible-commit or stale mapping conflicts. | Unsafe retries can duplicate target objects, overwrite immutable mappings or silently report a complete MBOM. | Persist per-node immutable input/mapping observation and append-only result truth; derive aggregate state; represent uncertain/stale/conflict explicitly; expose retry eligibility but disable dispatch in Phase 5. | Active P5-05 implementation risk; all required fault cases must pass no-network tests before Level 2 |
| R-068 | A P6-04 controlled-runtime fixture could accidentally seed a formal Supplier or successful ERP projection, or treat released design evidence as Tooling manufacturing authority. | The Gate could manufacture external execution truth, move ERPNext-owned fields into NPI One or bypass `DR-REC-010`. | Use only visibly synthetic NPI-owned plans and internal observations; keep the production ERP reader absent; assert the exact unavailable projection and unavailable manufacturing authorization; perform no outbound network access; prove the independent P6-04 route switch and retain all external/lifecycle holds. | Closed for P6-04 — exact-SHA controlled Gate `31267181068` and artifact `9024542728` prove unavailable ERP/manufacturing authority, synthetic internal plans/observations, exact released/unreleased dependency handling, independent route recovery and no external mutation |
| R-069 | A Tooling defect severity or prototype status could be translated into an invented Gate blocker or Tooling lifecycle transition. | G5/G6 may be blocked or cleared without an approved Trial/Gate policy, while `DR-REC-010` could be bypassed through a convenience status. | Keep severity and explicit blocking intent separate; implement only the Pack Tooling-defect sequence; append immutable revisions; do not create Domain Work Items, mutate Gates or touch Requirement/Revision/Set lifecycle; leave final Trial/G5/G6 integration to Phase 7. | Mitigated for P6-05 — exact separation and no-Gate/lifecycle controlled runtime passed `31280296684`; Phase 7 policy remains a scoped hold |
| R-070 | Customer Standard values can be copied into Trial Actual or Approved Process Baseline slots, or a caller can claim approval without exact Trial evidence. | Unmeasured or unapproved process data could appear authoritative and feed acceptance/capacity decisions. | Use disjoint layer types and immutable source/context hashes; expose a live command only for Customer Standard; keep Trial Actual `not_measured` and Approved Baseline `unavailable` until Phase 7 supplies exact Trial/approval readers; accept no caller availability/status flag. | Mitigated for P6-05 — disjoint live Standard and explicit absent actual/baseline passed Level 2; Phase 7 remains authoritative for their creation |
| R-071 | Capacity output can hide assumed hours/days/OEE/yield/cycle/set counts, use stale physical-Set truth or import spreadsheet results such as `#REF!`. | Users may rely on a non-reproducible bottleneck, monthly capacity or gap and create unnecessary Tooling or miss real demand. | Version every input and result; publish `capacity.v1`, unit conversion and decimal rule in the contract; require exact Part/Applicability/provenance and explicit set assumptions; derive all outputs server-side; never accept a caller result or default business input. | Resolved for P6-05 — complete explicit inputs/results and deterministic successor recomputation passed controlled Gate `31280296684` |
| R-072 | Missing ERP/IoT observations, calibration and maintenance policy can be represented as zero shot count, a healthy score or preventive recommendation. | An unavailable source may appear safe and suppress real maintenance or safety work. | Use a closed unavailable production projection; expose separate missing source/calibration/scoring-policy reasons; install no adapter, score, threshold, warning or advice in P6-05. | Mitigated for P6-05 — explicit unavailable health truth passed Level 2; Phase 8 retains target-confirmed source observations and policy |
| R-073 | A complete acceptance checklist or System Manager evidence command can be shown as approved Tooling acceptance despite `DR-REC-010` and missing Trial/quality authority. | An unapproved mold could appear released for Asset creation, production or Gate progress. | Store only immutable evidence-presence dispositions; keep category coverage, business approval, lifecycle and target execution separate; force approval to explicit unavailable and forbid a caller approval flag. | Mitigated for P6-06 — immutable evidence and explicit unavailable approval/lifecycle truth passed Level 2; Phase 7/8 retain their authorities |
| R-074 | A locally validated Mock request can be presented as an ERPNext Asset creation/update or can populate a synthetic formal Asset ID. | Users could act on a nonexistent formal Asset, duplicate a target mapping or believe dispatch succeeded. | Fix the operation to `create_or_update_tool_asset`; keep formal request `draft`, input `validated_mock`, approval `unavailable`, dispatch `prohibited`, result `not_requested`; create no Outbox/network/target ID and reserve confirmation for Phase 8. | Mitigated for P6-06 — no-network/no-Outbox/no-target truth and explicit Mock axes passed controlled Gate `31292306716` |
| R-075 | Tooling Master, Requirement or planned quantity can be mapped directly to a formal Asset, collapsing copied physical molds or allowing competing mappings. | Physical custody, maintenance and cost can attach to the wrong mold; reconciliation can silently overwrite target truth. | Make one physical Tooling Set the sole mapping subject; freeze zero-or-one mapping cardinality; require authenticated target confirmation and exact prior observation for every mapping; keep mapping unavailable in Phase 6. | Mitigated for P6-06 — exact physical-Set binding and unavailable zero-or-one mapping condition passed Level 2; formal confirmation remains Phase 8 |
| R-076 | NPI-owned movement, spare or repair evidence can be mistaken for actual ERP movement, inventory, supplier, repair transaction or cost truth. | Location, stock and maintenance decisions could rely on local recommendations or paperwork rather than formal execution. | Keep immutable NPI evidence/recommendations in separate sections; expose ERP execution/projection as unavailable and read-only; require customer-authorization evidence for customer-owned repairs; install no ERP mutation or inferred result. | Mitigated for P6-06 — immutable NPI evidence, customer authorization and unavailable ERP projections passed Level 2; target execution remains Phase 8 |
| R-077 | The reviewed 43-column proposal or a synthetic fixture mapping can be mistaken for approved production customer semantics. | Ambiguous numeric columns, A/B/C grades or customer aliases could create authoritative facts under guessed meanings. | Persist immutable proposal revisions; require a server-side activation bound to exact customer/template/source signature; install no production activation; scope controlled activation to the named synthetic fixture and keep `DR-REC-007` visible. | Mitigated for P6-07 — controlled Gate `31330684809` activated only the exact visibly synthetic fixture; production mapping approval remains held |
| R-078 | A workbook's formatting, blank cells, merged areas, prior rows or nearby image can be used to invent Tooling relationships. | Parts can bind to the wrong shared Tooling, cavity, insert or image while preview appears valid. | Detect regions from bounded content/structure evidence; retain raw values and confidence; represent candidates only; require Project-authorized confirmation for every ambiguous relation/image before execution. | Resolved for P6-07 — position-independent detection and explicit ambiguous relationship/image confirmation passed the controlled Gate |
| R-079 | An aggregate asynchronous status can hide failed/unconfirmed rows or retry already successful mutations. | Users may see false success, duplicate Tooling objects or silently incomplete imports. | Persist immutable per-row/per-field results; derive terminal status; seal actor-bound idempotency; retry only failed eligible rows against exact target versions and make unconfirmed rows non-success. | Resolved for P6-07 — durable partial truth, five results per scenario and failed-row-only retry with no successful-row duplication passed the controlled Gate |
| R-080 | Raw confidential workbook values can leak through normal logs, trace/audit summaries or correction exports. | Customer design, pricing or identifier data may be exposed outside Project/file authorization. | Keep ordinary logs structural and hashed; return raw details only on authorized batch surfaces; allowlist/redact correction columns; audit downloads and test logs/artifacts for raw fixture sentinels. | Mitigated for P6-07 — structural logs, authorized allowlisted correction downloads and artifact sentinel scans passed; customer workbooks remain unauthorized |
| R-081 | Rollback can delete or overwrite an object changed or used after import. | Downstream evidence, revisions or execution references may become orphaned or historically false. | Permit reversal only for exact unchanged objects created solely by the batch with zero downstream references; deny updates and downstream-used objects durably; require forward correction otherwise. | Mitigated for P6-07 — allowed unchanged batch-created unused targets and durable downstream-use denial passed; broader destructive rollback remains held under `DR-REC-008` |
| R-082 | Reopening unchecked XLSX members for business-value parsing can bypass the passive inspector's archive/XML limits. | A file that passed one scan could trigger traversal, entity expansion or resource exhaustion in a second parser. | Productize one prevalidated archive manifest and bounded XML reader; business detection may consume only validated members under the same byte/element/depth limits and never execute formulas or fetch relationships. | Resolved for P6-07 — one prevalidated bounded member reader passed focused safety tests and cumulative controlled runtime without formula or relationship execution |
| R-083 | A caller-selected DocType, field list or filter expression can turn P6-08 into an arbitrary database export. | Hidden, unrelated or privileged fields can leave the Project boundary and the product BFF. | Use fixed P6-08 routes, ten code-owned views, closed search/sort/group values, one allowlisted package schema and exact-key rejection; never call Frappe Desk export/report APIs. | Mitigated for P6-08 — closed query/package controls, generic-mutation denial and cumulative controlled Gate `31355555773` passed |
| R-084 | A shared Tooling Master can expose Applicability or Set facts belonging to another Project/customer. | An authorized Project user may receive cross-Project relationship, identity or confidential operational truth. | Authorize the selected Project before every secondary identifier and build only a Project-relative aggregate/package; revalidate every included reference and omit unrelated relationships. | Mitigated for P6-08 — Project-first shared-Master containment, cross-Project denial and final controlled Gate passed |
| R-085 | Selected rows or a filtered result can change between display and export. | The package may silently contain newer, removed or additional Tooling objects than the actor reviewed. | Require exact selected Master snapshot hashes or the complete server querySnapshotHash; recompute under authorization and fail stale/conflict rather than silently refresh. | Mitigated for P6-08 — exact selection/query snapshots, stale/conflict denial and fresh-process replay passed controlled Gate `31355555773` |
| R-086 | Spreadsheet formula prefixes or confidential Tooling values can leak through CSV, manifest, logs or audit summaries. | Opening an export can execute a formula, and customer, repair, custody, cost, evidence or raw workbook truth can escape. | Fixed allowlist, leading `= + - @` neutralization, explicit omission manifest, structural hash/count logs and sentinel scans; include no private File URL/content. | Mitigated for P6-08 — three localized packages, formula neutralization, omission manifest, URL/path absence and raw-log sentinel scans passed the controlled Gate |
| R-087 | A filtered export can consume unbounded memory/CPU or create an oversized private artifact. | One request can degrade the Site or produce an unauditable data dump. | Bound the source collection, require `1..100` Masters, fix three package members and byte limits, use deterministic non-recursive rendering and reject broad filters. | Mitigated for the bounded P6-08 mechanism — fixed three-member packages and `1..100` source limits passed; representative production-scale performance remains external |
| R-088 | A raw File URL, non-creator replay or expired link can bypass package download authorization. | A confidential package can remain reachable after Project access or intended validity ends. | Return no raw URL; require creator-bound CSRF POST, current Project/export authority, exact package/File hash, attachment security headers and a fixed one-hour validity check. | Mitigated for P6-08 — creator/Project/hash/expiry reauthorization, security headers, no raw URL and exact one-hour denial passed the controlled Gate |
| R-089 | Localized CSV headers can be mistaken for stable machine keys or rerendered after a user language change. | Automated consumers can misread columns and audit cannot reproduce the delivered artifact. | Freeze Frappe language at creation; keep stable schema/keys in manifest, localized CSV/readme as separate members and immutable member/package hashes. | Mitigated for P6-08 — immutable `en`/`zh`/`zh-TW` packages, stable manifest keys and cross-process exact-byte replay passed |
| R-090 | Page selection can be confused with the complete filtered result. | Users may export only a visible page while believing all matches were included, or unintentionally export all matches. | Make `selection` and `filtered` mutually exclusive modes, show reviewed count/mode, keep selection across pages explicit and compute the full filtered result independently of cursor/page size. | Mitigated for P6-08 — mutually exclusive selection/filter packages, reviewed counts, stable paging and complete-filter server proof passed Level 2 |
| R-091 | The deterministic Trial page prototype can be mistaken for a live audited Trial aggregate. | Local values, local-only photos or an in-memory conclusion could be presented as durable operational truth. | Treat the prototype as zero backend capability; require independent routes, Project-first permissions, immutable audit/receipt truth and honest loading/error/unsupported states before any technical verification claim. | Active Phase 7 risk; P7-00 records the zero-capability audit |
| R-092 | Trial Plan, Trial Round and locked Round inputs can collapse into one mutable record or a latest-value pointer. | Later edits could rewrite what was planned or used and make comparisons, conclusions and approvals unreproducible. | Keep Plan and Round identities distinct; append immutable Round/input versions bound to exact Project, Tooling, product, material, parameter, inspection and evidence hashes; fail stale conflicts. | Active Phase 7 risk; P7-01/02 checkpoint plans must freeze the lifecycle before code |
| R-093 | NPI evidence can be presented as ERPNext formal Quality Inspection/NCR approval or Tooling acceptance. | Users may treat collaboration evidence as official manufacturing/quality release authority. | Keep NPI evidence and formal ERP projections separate, label unavailable target truth, accept no caller-selected official result and perform no production contact. | Active Phase 7 risk; ERPNext ownership and `DR-REC-009` remain held |
| R-094 | A readiness percentage can hide mandatory blockers or mutate a Gate automatically. | A visually high score could imply release readiness despite missing evidence or failed mandatory checks. | Version the checklist policy and evidence snapshot, derive the score server-side, make blockers dominant and never mutate/close a Gate without separately approved authority. | Mitigated for P7-05 at Level 2: exact product SHA `418b3aa`, ordinary CI `31777229867` and controlled Gate `31777985302` pass. Runtime artifact `9210730456` (`sha256:e018a02bc3005670879822c3ca2ec348136b4f36db50feb7ac7398c395ba4372`) proves exact immutable sources, server-derived score, dominant P0 blockers and Gate-input-only separation on a disposable Site with zero Gate/ERP/downstream mutation. P7-06 handover/observation and every later Gate authority remain separate and fail closed. |
| R-095 | Releasing the NPI-side Trial Summary can be confused with publishing the exact external event/projection or granting formal approval/print authority. | Phase 7 could falsely claim downstream ERP/JCE receipt, duplicate a future integration event, or present technical evidence/PDF as signature, production acceptance or G7 authority. | Create only an append-only NPI-owned technical summary from the exact current decided conclusion, retain a bounded URL-free projection/redaction manifest, reuse controlled-print mechanics without a production mapping, and keep event/payload/consumer/receipt under `DR-REC-009` plus form/signer/retention/copy under `DR-REC-003/004`. | Mitigated for P7-07 at Level 2: exact SHA `dda9c13`, ordinary CI `31887451908` and controlled Gate `31887990384` prove exact immutable approved/rejected summary succession, retained summary-v1 controlled PDF, Project-first authority, replay/conflict/rollback/redaction, zero external effects and cleanup. External event/projection/receipt, production form/signature/retention/copy and formal release/G7 authority remain scoped holds. |
| R-096 | Mobile field actions can bypass desktop BFF permissions or expose raw private File URLs. | Small-screen convenience could weaken authority, audit, attachment containment or download validity. | Reuse the same BFF commands/capabilities, Project-first reauthorization, private attachment APIs, audit and conflict rules; change layout only, never authority. | Mitigated for live Trial at P7-08 checkpoint 2 (`290c66f`, CI `31894667043`): camera selection remains local until explicit bounded private upload, scan review/apply submits nothing, clean binding and defect persistence use the unchanged commands, and no raw File URL appears. Gate integration and final Level 3 remain required. |
| R-097 | A fast Level 2 controlled dispatch could reuse a stale, foreign, partial or failed ordinary run. | The Site could execute without the exact SHA, complete branch history, frontend, repository, visual or secret evidence required before it. | Accept only a positive integer run ID from the same repository; query through an exact-origin read-only token; require the same full SHA, `pull_request` event, current workflow, completed success and successful `repository`, `frontend`, `secret_scan` and `visual` jobs; persist the attestation and fail closed on every mismatch/network error. | Mitigated — redirect-origin tests, ordinary CI `31388734891` and exact-SHA Level 3 `31392474781` pass; every future reuse remains fail-closed |
| R-098 | Splitting the serial repository job could silently drop a command or allow one parallel lane to mask another failure. | Delivery may appear faster by weakening Python, frontend/E2E, audit, trace, visual or secret coverage. | Keep the former commands in explicit repository/frontend lanes; retain current-tree and full-branch secret jobs; make all four ordinary jobs independently required; validate workflow structure in repository tests; run full Level 3 on the final SHA. | Mitigated — repository, frontend, secret and visual lanes plus complete controlled runtime pass ordinary CI `31388734891` and Level 3 `31392474781` |
| R-099 | Stabilizing catalog pixels in the P0 matrix could hide a real catalog/version failure. | A stale or missing generated translation catalog might no longer be visible in screenshots. | Keep catalog generation and direct trilingual integrity unchanged; require the live footer code to equal the generated `catalogVersion` before replacing only screenshot pixels with the registry-owned stable token; change no product DOM, baseline, case or tolerance. | Mitigated — catalog assertion, complete direct trilingual checks and unchanged `97/97` fixed-Linux Level 3 matrix pass at `22cb24d` |
| R-100 | A Round input lock could point to the latest material, Tooling, product or parameter definition instead of the exact version used. | Later edits could silently rewrite execution history and make Trial comparison, evidence and approval unreproducible. | Persist immutable exact identifiers, versions and hashes in append-only lock revisions; validate containment and stale state server-side; never resolve an existing lock to latest. | Mitigated at P7-02 Level 2 (`3a26719`, CI `31432120639`, controlled Gate `31432837104`) with exact-version/hash runtime proof |
| R-101 | Customer Standard, an unmeasured field or a caller-supplied result could be stored as Trial Actual or Approved Process Baseline. | Users could see invented measured or approved process truth and make unsafe quality decisions. | Keep all three layers disjoint; require explicit measured/not-measured state, unit, source and timestamps; allow only manual acquisition in P7-02 and derive no Actual from Standard or missing values. | Manual Trial Actual boundary mitigated at P7-02 Level 2; automatic acquisition and baseline approval remain explicit scoped holds |
| R-102 | A pending, infected, public or mismatched File could be bound as Trial/Sample evidence or exposed through a raw URL. | Confidential or unsafe evidence could leak, and the retained Round could cite bytes that were never reviewed. | Reuse bounded private upload; bind only the exact clean private File Revision/hash; separate upload from evidence bind; require Project-first audited byte access and return no raw URL. | Mitigated at P7-02 Level 2 with controlled pending/clean/private/live, exact-byte/hash, audited URL-free access and route recovery proof |
| R-103 | Sample identity, cavity, material, quantity, packaging or destination could be overwritten or collapsed into the Round. | Physical feedback and later quality evidence could attach to the wrong sample or lose reproducible lineage. | Give every Sample Batch a stable identity and append-only revisions bound to exact Round/input/material/cavity context; reject stale successors and preserve explicit feedback/evidence links. | Mitigated at P7-02 Level 2 with stable identity, two-cavity preservation, immutable successors and controlled replay/rollback proof |
| R-104 | A placeholder machine adapter or local flag could present manual entries as automatically imported machine data. | Acquisition confidence and audit provenance would be false, while production integration was never authorized. | Expose only explicit manual source/time/confirmation; keep `machine_import=unavailable`; install no adapter, credential, network call, Outbox or inferred measured value. | Active scoped hold under `FR-TR-010`; P7-02 Level 2 proves the unavailable boundary and zero integration traffic |
| R-105 | A Trial defect could lose its exact Round, physical cavity or input-lock lineage and be shown as a generic Tooling issue. | Corrective action and later comparison could target the wrong cavity or silently use a later Tooling state. | Bind immutable defect revisions to exact Project/Round/input-lock/Tooling Revision/Set/cavity identities and reject latest-value substitution or cross-context references. | Mitigated at P7-03 Level 2 (`102de35`, ordinary CI `31459395711`, controlled Gate `31459974578`) with exact cavity lineage, cross-store single-tip and cross-Round runtime proof |
| R-106 | Closing an action or recording verification could automatically create an NCR, mutate a Gate or advance Tooling lifecycle. | NPI collaboration truth could be mistaken for formal ERP quality or release authority. | Keep action and independent verification append-only and separately authorized; expose formal NCR/quality/Gate/Tooling effects as unavailable unless a later approved command owns them. | Mitigated at P7-03 Level 2: controlled runtime proves independent verification, explicit close/reopen, zero ERP/network/Outbox traffic and no Gate or Tooling lifecycle mutation |
| R-107 | A Round comparison, conclusion or approval reference could resolve latest values, hide critical gaps or present evidence/external status as approval. | Users could approve the wrong versions, accept an incomplete Trial or infer formal ERP/customer/Gate authority that does not exist. | Compare exact immutable source revisions; derive critical blockers server-side; snapshot conclusions append-only; keep evidence presence, internal/customer/deviation references and formal ERP quality as distinct policy-bound or unavailable facts. | Mitigated at P7-04 Level 2 (`02781c0`, ordinary CI `31488890426`, controlled Gate `31489609774`) with exact multi-Round sources, blocker/reference/conclusion succession, replay/rollback/IDOR/route recovery and zero external effects |
| R-108 | A production-transition acknowledgement could be treated as an electronic signature, approval or G7 decision, or Trial/readiness/capacity evidence could be promoted to a production actual. | The system could claim formal bilateral handover, stable production or Gate authority without the receiving organization, actual SOP, external operational facts or approved policy. | Keep acknowledgement fact-only and distinct from signature, approval and G7; bind every transition to an explicit no-default `ProductionTransitionPolicyVersion`; keep Trial, readiness and capacity sources distinct from external production yield/complaint/cycle/Tooling actuals and fail closed when formal authority, actual SOP or stability policy is unavailable. | Mitigated for P7-06 at Level 2: exact product SHA `563fff5`, ordinary CI `31828878511` and controlled Gate `31829617671` pass. Runtime artifact `9230370526` (`sha256:0b68c53e2abea2ba11957134977b68ef507e9b22cc4bbd5e450718832fd573a0`) proves exact current source tips, immutable package/acknowledgement/observation history, all five identity-free unavailable providers, Project-first actor separation, replay/route recovery/redaction and zero Gate/Project/Work/Tooling/ERP/downstream effects. Formal receiving authority, external actuals, stability policy and G7 remain held. |
| R-109 | A responsive Trial or Gate surface, camera selector or scanned value could create mobile-only authority, auto-submit a command, expose a raw private File URL or hide critical engineering truth. | Field operators could mutate unauthorized state, attach unsafe evidence, approve stale inputs or mistake a reduced layout for complete engineering review. | Keep the identical Project-first BFF capability/permission/CSRF/idempotency/conflict/audit boundary on every viewport; separate scan review/apply/normal command; use only bounded private pending upload and exact clean bind; retain critical identity/version/blocker/held-authority summaries and move only enumerated complex tables behind an explicit same-link desktop handoff. | Mitigated at P7-08 Level 2 and Phase 7 Level 3 (`3111402`; ordinary `31898840279`; Level 3 `31899480493`): same-authority Trial/Gate phone paths, no-submit scan, URL-free private evidence, direct trilingual/accessibility and desktop engineering handoff all pass. |
| R-110 | A queued, accepted or timed-out ERPNext request can be presented as target success. | Users may rely on a nonexistent Item, BOM, Asset or quality result and duplicate execution on retry. | Separate approval, request, attempt, observed result and reconciliation; Mock emits no formal success/target ID; timeout after possible commit is uncertain and must reconcile before redispatch. | Mitigated for the P8-03 Item slice at exact SHA `c11d97c` and Level 3 `32480568505`: immutable pre-call attempts, closed classification and recovery refuse post-boundary redispatch; the inspector keeps request/result/mapping conflict separate and renders formal Item identity only from current authoritative mapping. Mock/synthetic/HTTP acceptance/timeout cannot report success. MBOM/Asset/quality operations remain separately held. |
| R-111 | Duplicate, reordered or restarted webhook/worker processing can overwrite newer ERP truth or repeat NPI effects. | Master/status projections can regress and one external event can create duplicate Project or mapping records. | Verify signature/replay window, land durable Inbox first, bind event ID to payload hash, process asynchronously with version ordering and prove duplicate/reorder/restart/cross-process replay. | Mitigated for P8-01 at Level 3 `31926087732` and for P8-02 at exact SHA `260ed2e`, Level 3 `31944941030`: concurrent signed events, live/expired leases, exact source binding and final cross-process replay converge to one draft without rewriting durable truth. |
| R-112 | Sandbox configuration can silently fall back to a production ERPNext host or leak credentials/payloads. | Autonomous tests or retries could contact production, expose secrets or mutate formal records. | Mock default; explicit allowlisted non-production host and operation scope; reject production hosts; keep credentials outside code/payload/audit/log; never use production as a validation fallback. | Mitigated for P8-01 at Level 3 `31926087732`, P8-02 at Level 3 `31944941030` and the P8-03 adapter boundary at `1a2c5be` / CI `31956908978`: default-disable, explicit non-production host/operation/redirect checks, injected secret resolution, bounded timeouts, redaction and network-free synthetic proof pass. No Sandbox or production profile is installed. |
| R-113 | Generic replay, reconciliation or DocType-write tooling can bypass operation authority and field ownership. | Operators could alter an immutable request, overwrite ERP-owned truth or perform an unreviewed cross-system mutation. | Use operation-specific commands, current Project/operation authority, immutable payload/idempotency, audited attempts and forward-only reconciliation; expose no generic write_doc or database path. | Anchored at P8-00 Level 2; active through P8-07, which must fail closed and require no database access for normal operations. |
| R-114 | A tenant-wide ERP master/status observation can be resolved before Project scope or reused under a foreign NPI context. | Customer, Supplier, cost, quality or Asset truth can leak across Projects even when the source ID exists. | Authorize exact tenant and Project before every secondary identity; bind a guarded head to one server-resolved NPI scope; revalidate containment per row and return the same unavailable result for missing, foreign and ambiguous IDs. | Mitigated for P8-01 at exact SHA `b938926` and Level 3 `31926087732`: server and client Project containment, identical internal absent/foreign `404`, bounded external redaction and exact consumer-scope membership pass on the disposable Site. |
| R-115 | Opaque ERP versions or equal modified timestamps can be guessed into an order. | A reordered or conflicting event can overwrite newer target truth and hide the conflict. | Compare only exact source modified time under the operation contract; retain older observations as superseded, treat equal-time different-version/hash as conflict, never use lexical version ordering and lock the exact head before advance. | Mitigated for P8-01 at exact SHA `b938926` and Level 3 `31926087732`: twenty-five observations retain one superseded and one conflicted disposition without lexical version ordering or backward head movement; same/cross-process replay leaves counts and heads unchanged. |
| R-116 | A missing freshness/EAC/quality policy can be replaced by a convenient default. | The UI can label stale data fresh, invent forecast-at-completion or turn an unmapped ERP status into a Gate pass/fail. | No installed freshness policy yields `unknown`; preserve raw ERP status/cost codes; keep budget/EAC and quality/Gate interpretation in separately approved versioned policies and display unavailable instead of inference. | Mitigated for P8-01 at exact SHA `b938926` and Level 3 `31926087732`: direct trilingual Project/Tooling UI and disposable runtime withhold formal values under unavailable/stale/unknown/synthetic/conflict truth, preserve source codes and install no freshness/EAC/quality policy. Future policy tasks remain separately held. |
| R-117 | Parsing JSON or trusting caller fields before exact raw-body authentication can let reserialization, duplicate keys or a forged tenant/source bypass the signed boundary. | An attacker could land unverified payloads, select authority or create an incorrectly scoped Project. | Bind HMAC-SHA256 to exact method/path/key/timestamp/request/raw bytes; enforce size/content/encoding first; verify the five-minute window and key profile before duplicate-key-free closed parsing; server-resolve tenant and policy and audit failures without raw body or secrets. | Mitigated for P8-02 at exact SHA `260ed2e` and Level 3 `31944941030`: fixed-route raw authentication, bad/stale/key-rotation rejection, closed parsing, safe failure audit and redaction pass on the disposable Site. |
| R-118 | Two event IDs or restarted workers for one submitted ERP document can race through actor idempotency and create two Project drafts or an orphan binding. | Duplicate Projects, business-code reservations or Gate shells can be retained for one quotation/order. | Reserve one tenant/profile/type/source binding, track the highest received version, serialize workers on that row, derive Project idempotency only from the server source key and commit Project/binding/Inbox/audit atomically under a reclaimable claim lease. | Mitigated for P8-02 at exact SHA `260ed2e` and Level 3 `31944941030`: distinct concurrent event IDs, Frappe unique-field races, expired-claim recovery and separate-process replay retain exactly one draft, one binding and two Gate shells. |
| R-119 | A convenient default owner, template, Project type, business-code transform or raw Site-config key can turn missing production mapping into hidden authority or credential leakage. | The wrong tenant/team/workflow could be created and a production HMAC secret could enter code, database, audit or logs. | Install no profile/policy/key; require exact versioned non-production server configuration and opaque secret resolver; accept only source-document-ID code mode without silent transform; fail final when owner/template/code is unavailable and use only a disposable synthetic policy in tests. | Mitigated for the P8-02 slice at exact SHA `260ed2e` and Level 3 `31944941030`: scoped actor/owner/published template, source-document code, runtime-only synthetic configuration, secret redaction and zero production traffic pass; no production mapping is installed. |
| R-120 | Treating an EBOM line UUID or repeated opaque engineering ID as a global Item identity can duplicate target work or map unrelated Projects to one formal Item. | Several occurrences could create several Items, divergent content could be chosen arbitrarily, or one Project could leak/reuse another Project's mapping. | Group all exact released occurrences, reject divergent Item-master fields, exclude MBOM facts and key the stream by tenant + Project + case-preserved engineering ID until an explicit cross-Project identity is approved. | Mitigated for the P8-03 Item slice at exact SHA `c11d97c` and Level 3 `32480568505` by deterministic complete grouping, divergent-field rejection, Item/MBOM separation, exact source/profile/expected-version impact, Project-first containment and cumulative replay/legacy proof. Stale or conflicting expectations create no request/Outbox, and old P5 baselines remain unchanged. |
| R-121 | Legacy or generic Outbox/support rows can be promoted into executable work without immutable operation, tenant, Project, profile, source, lease or attempt evidence. | A worker could send an ambiguous payload, duplicate a target call, bypass operation authority or erase uncertain history. | Add a guarded version-1 Item envelope and immutable request/attempt/result records; never promote legacy rows; commit request plus Outbox before enqueue; persist a pre-call attempt and prohibit redispatch after the adapter boundary without reconciliation. | Mitigated for P8-03 at exact SHA `c11d97c` and Level 3 `32480568505`: guarded version-1-only claims, active/retained guard truth, immutable attempts/idempotency, crossed-boundary uncertainty/no redispatch, terminal replay and strict legacy read-only/non-claimable behavior pass. Generic retry/replay/reconciliation remains P8-07. |
| R-122 | Reusing the Item request or a latest-value EBOM projection for MBOM execution can collapse separate approval, node hierarchy, Item-mapping readiness, partial target results and submitted-BOM immutability. | A BOM could reference stale or unmapped Items, hide a partial/uncertain target outcome, overwrite a submitted BOM or claim success for a different released structure. | Frozen P8-04 plan defines a separate `publish_released_mbom` command over exact released topology, exact current authenticated Item mappings, immutable Item/MBOM mapping-set expectations, per-node results, submitted blocking and forward-only repair. | Mitigated for the P8-04 technical slice at exact SHA `ca72dece`, ordinary CI `32651139504` and Level 3 `32651903846`: exact request-bound projection, bounded leases, immutable attempts, default-disabled/network-free adapters, per-node truth, submitted protection and authoritative exact mapping CAS pass. Actual ERPNext projection/method, submitted-successor and Sandbox/production facts remain scoped holds. |
| R-123 | Collapsing Tooling development or NPI acceptance evidence into an ERP Asset record can create duplicate formal assets, treat acceptance as ERP approval or let stale location/maintenance truth drive execution. | One physical tool could map to several Assets, NPI evidence could claim a formal ERP approval, or an unverified update could overwrite ERP-owned Asset/location/maintenance state. | P8-05 freezes one physical Tooling Set to zero-or-one formal Asset mapping, separate v2 create/update operations and idempotency, exact immutable source/mapping expectations, acceptance-evidence versus business-approval separation, authenticated complete-result compare-and-set and P8-01-only read-only Asset/location/maintenance projection ownership. | Mitigated for the P8-05 technical slice at exact SHA `f9c3580`, ordinary CI `32937395289` and Level 3 `32938622250`: operation-specific request/Outbox/attempt/result truth, mapping CAS, partial/uncertain withholding, default-disabled network-free Synthetic proof and strict read-only projection/UI pass. Production/Sandbox Asset method, fields, location/maintenance, business approval and formal mapping remain scoped holds. |
| R-124 | Linking NPI Trial, issue, readiness or controlled-report evidence directly to formal ERP Quality Inspection, NCR or CAPA truth can turn incomplete evidence into an ERP approval or pass. | A failed, unavailable or stale target result could be displayed as approved quality truth, or an NPI record could mutate an ERP-owned quality document without authority. | The frozen P8-06 audit requires exact Project/source containment, exact current P8-01 observation/head identity, an NPI-only immutable link, source-specific existing authority and failed/partial/uncertain/unavailable truth. Formal Quality Inspection/NCR/CAPA identity, lifecycle, approval, submission and result remain ERPNext-owned and read-only; raw codes have no installed pass/Gate policy. | Audit-plan SHA `b3cf6ac` passes ordinary CI `32946799144`. Checkpoint 1 is bounded after its separate transition CI to pure domain/config/validation, three guarded zero-row support DocTypes, ownership/OpenAPI components, i18n and tests; it has no route, row creation, event, Outbox, worker, adapter, runtime, UI or network. Current ERPNext DocType/method/field/lifecycle/approval/status-policy/Sandbox facts remain scoped Class-B holds, and production target contact is prohibited. |
