# P7-07 Checkpoint 3 — Live Released Trial Summary Workspace

Recorded: `2026-08-15T09:40:46Z`

Decision: `PASS — CHECKPOINT 3`

Exact product checkpoint:
`9a2ed86fb3780d5d8cdcda023a76d647d384ca63`

Ordinary CI:
`31877039560` (`PASS`)

## 1. Bounded outcome

Checkpoint 3 delivers only the strict P7-07 browser boundary in the existing
live Trial workspace:

- strict Released Trial Summary history/current loading and server-response
  validation through the Project-first BFF routes;
- a dense current/history workspace that preserves exact revision,
  decided-conclusion, source-manifest, bounded fact, redaction and authority
  truth rather than substituting latest mutable Trial values;
- reviewed retain/revise commands with immutable predecessor history,
  actor-bound command replay and accepted-command refresh-failure protection;
- exact controlled-print availability and action reuse without adding a
  generic print behavior, production form mapping or browser-print shortcut;
- loading, empty, read-only, permission, validation, conflict, processing,
  retry, unavailable mapping and accepted-refresh-failure states; and
- direct English, Simplified-Chinese and Traditional-Chinese content,
  keyboard/focus/Axe coverage and fixed-Linux governed visual evidence.

This workspace exposes an NPI-owned technical summary. It does not create an
external event/projection/receipt, customer approval, formal ERP quality
result, production acceptance, G7/Gate decision, Project/Work Item/Tooling
lifecycle mutation or production ERP/print authority.

## 2. Exact-SHA verification evidence

Final ordinary pull-request CI `31877039560` passed exact product SHA
`9a2ed86fb3780d5d8cdcda023a76d647d384ca63`:

- repository `94994234564`: `1,905/1,905` tracked Python tests plus repository,
  current-task and reconciliation verification PASS;
- frontend `94994234549`: `58/58` files, `913/913` unit tests, `408/408`
  non-visual E2E tests, `7,439` literal English sources with `100%` direct
  `zh` and `zh-TW` coverage, statements `80.31%`, branches `80.25%`,
  functions `82.90%`, lines `82.98%`, production build and zero
  vulnerabilities;
- secret scan `94994234566`: current-task guard reports `50` committed paths,
  the complete branch scan covers `470` commits and both secret lanes report
  no leaks; and
- visual `94994234575`: `115/115` fixed-Linux cases PASS, including the new
  P7-07 English `1366x768`, Simplified Chinese `1440x900` and Traditional
  Chinese `1920x1080` cases.

Visual artifact `9245060184`, `r1-06-linux-visual-evidence`, contains `88`
files, has size `13,175,382` bytes and digest
`sha256:a6d88366adb22c9ef6404caf32895a8a77ee8b7bb1230db0b48d7e48f4e7515c`.
Gitleaks artifact `9245015977` has digest
`sha256:b4e2913a4d46fb79f004ba8aa94eba83c8ece4f7bbb3e738d6579c0a7ab7839f`.
Controlled preflight `94995433086` and runtime `94995433200` correctly skip
because checkpoint 3 installs no runtime fixture or synthetic mapping and
makes no Level 2 claim.

The reviewed P7-07 Linux snapshots hash as follows:

- English:
  `e37956f7c27bc002a8acf4a1b13c444803933c8632c7cd8b812561b9647dfaa5`;
- Simplified Chinese:
  `2c85f506978407d6276dbada0c7665fe326ade883523c2b59380522fc2624679`;
- Traditional Chinese:
  `cae152f5f51feaf22a47f38b1d78d32635d3c9335a9d5743e38b0d40837f85d7`.

## 3. Diagnostic closure and visual review

Diagnostic ordinary CI `31876670734` passed repository, frontend and secret
lanes. Its visual lane failed only because adding a new global Trial section
anchor changed three pre-existing P7-01 snapshots; artifact `9244966942`,
digest
`sha256:7512612b6f5dcd0e39854a16a607eb493452592497564801ffab7ff92ad6cb8e`,
showed that all three new P7-07 language cases already matched their reviewed
Linux baselines. The extra global anchor was outside the frozen checkpoint
contract and the P7-01 baseline paths were outside the task allowlist.

Forward fix `9a2ed86` removed only that four-line global anchor registration.
The Released Summary section, title, history, inspector, commands and tests
remain present. No product assertion, visual threshold, snapshot tolerance,
translation rule, route switch, permission check or authority boundary was
removed or weakened. Final exact-SHA CI then passed the full `115/115` matrix.

Focused local evidence before final CI included `48/48` adapter/page unit
tests, `33/33` page tests after the visual correction, `9/9` new non-visual
P7-07 E2E tests, `3/3` reviewed Darwin candidates and `3/3` reviewed Linux
baselines. TypeScript, complete frontend verification, i18n audit, controller
tests, current-task validation, reconciliation tests and `git diff --check`
also passed. Darwin snapshots and generated local reports remain untracked and
are not accepted release evidence.

## 4. Task-scope review

The checkpoint-3 product range
`4109ebee34e3d03e4071184aae031d6bcff79231..9a2ed86fb3780d5d8cdcda023a76d647d384ca63`
contains `15` files, `3,548` insertions and `3` deletions. Every committed path
belongs to the strict browser data source, existing-Trial workspace, direct
translation catalog, focused unit/E2E fixtures, governed visual workflow or
three reviewed Linux baselines and is within `implementation/CURRENT_TASK.json`.
No user-owned dirty file, Darwin snapshot, generated report or local
development-prerequisite experiment is in the product range.

## 5. Security, rollback and transition

The browser preserves Project-first authorization and displays only
server-owned bounded projection truth. It never accepts translated labels as
contract values, never constructs a private File URL, retains an idempotency
key across conflict review and never reports an accepted command as failed
merely because its follow-up refresh failed. Scrollable fact/source regions
remain keyboard reachable and every status has text rather than color-only
meaning.

After retained summary or controlled-output history, rollback disables only
the independent P7-07 routes and Released Summary workspace and uses reviewed
forward repair. It never deletes, rewrites, renumbers, rehashes or silently
re-renders retained summaries, receipts, audits, controlled-print snapshots,
outputs, access events or private File identities.

Checkpoint 3 passes. Standing continuous-delivery authority activates only
checkpoint 4: cumulative disposable-Site runtime, a disposable synthetic
mapping, exact controlled PDF/replay/conflict/rollback/redaction proof,
Requirement trace, Task Diff Review and the P7-07 Level 2 Gate. This checkpoint
does not claim controlled runtime, task Level 2 completion or the Phase 7
Level 3 Gate. The latest complete Level 3 remains workflow `31392474781` at
exact SHA `22cb24d42174a5b75f475127ac3aa9fee5a08606`.
