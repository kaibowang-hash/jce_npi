# P7-02 Domain, Contract and Metadata Checkpoint

Recorded: `2026-08-10T14:48:18Z`

Status:
`PASS — EXACT INPUT LOCK, MANUAL ACTUAL, SAMPLE REVISION AND PRIVATE EVIDENCE DOMAIN`

Primary requirements: `FR-TR-002`, `FR-TR-003`, `FR-TR-010`, `FR-NP-004`,
`FR-NP-005` and the `FR-TX-019` foundation.

Exact stable checkpoint:
`37a4d9d49c3842813bdd3b54574893c0d403144d`

Product and bounded catalog commits:

- `f407550` — pure immutable input-lock, manual Trial Actual, Sample Batch and
  evidence-reference domains; closed future contract and ownership; four
  guarded additive DocTypes; direct tests and translations; and
- `37a4d9d` — regenerate the React catalog and replace four prohibited Latin
  residues in the new Chinese translations without changing domain or
  contract behavior.

## Delivered boundary

- Established immutable, exact-version Round input-lock revisions over the
  Project, Plan revision, Round, product/design, Part, Tooling Revision,
  physical Set/binding/cavity/process-chain, inspection document, material
  observation, parameter definitions and controlled references.
- Established immutable manual Trial Actual revisions with one explicit
  `measured` or `not_measured` entry for every locked parameter definition,
  exact units, source, observation time and predecessor/hash lineage.
- Established stable Sample Batch identity with immutable successor revisions
  for exact Round/input/material/cavity, quantity, packaging, destination and
  feedback truth.
- Established an immutable evidence-reference aggregate that accepts only an
  exact clean, private, live File Revision and persists hash/size/MIME truth
  without a File URL.
- Added four UUID-based, create-only guarded DocTypes, exact metadata
  validation, additive ownership declarations and complete direct Simplified
  and Traditional Chinese translations.
- Replaced the former unimplemented execution placeholders with closed future
  OpenAPI schemas for the exact Project-first checkpoint-2 boundary. The
  schemas expose manual acquisition and explicit unavailable machine import;
  they grant no ERPNext quality, conclusion, approval, Gate or release effect.

## Deliberately inactive

- This checkpoint activates no handler, BFF route, repository command,
  lifecycle transition, business row, File write, runtime fixture or UI.
- `planned -> prepared -> running` remains specified but inactive until the
  checkpoint-2 repository/BFF transaction, authorization and rollback tests
  pass.
- Automatic acquisition, production ERPNext access, Item/Batch verification,
  resource reservation, formal Quality Inspection/NCR, Trial conclusion,
  approval, Gate mutation, readiness and Released Trial Summary remain
  unavailable.
- Existing P7-01 Plan/Round history and the controlled private-file subsystem
  remain the only retained live predecessors; no second File system or
  Standard-to-Actual copy was introduced.

## Changed-files to affected-tests

| Change surface | Direct evidence |
|---|---|
| pure execution domains | exact identity/version/hash, immutable successor, closed parameter set, measured/not-measured, stable sample identity and URL-free clean-private evidence invariants |
| metadata/controllers | four exact additive DocTypes, canonical snapshot fields, UUID identity, create-only permissions and generic update/delete/rename denial |
| OpenAPI/ownership | closed future Project-first commands, exact NPI/source ownership and absence of external or approval effects |
| translations/generated catalog | generated-catalog equality, `6,130` direct English sources and `100%` `zh`/`zh-TW` coverage |
| checkpoint guard | current-task path scope, YAML parsing, reconciliation, P0 governance and clean diff |

## Local and exact-SHA CI evidence

- Focused P7-02 domain/metadata/contract checks passed `42/42`; the metadata
  subset passed `8/8`, and OpenAPI/data-ownership YAML parsed successfully.
- The clean-checkout ordinary CI run `31399227239` passed exact SHA
  `37a4d9d49c3842813bdd3b54574893c0d403144d`:
  - repository job `93489719835`: PASS with `1,510` tracked Python tests and
    the complete reconciliation/P0-governance checks;
  - frontend job `93489719816`: PASS with `54/54` files, `822/822` unit tests,
    `359/359` non-visual E2E tests, generated-catalog equality, complete direct
    trilingual coverage, build, dependency and production-brand audits;
  - secret-scan job `93489719819`: PASS for the task guard, current tree and
    complete pull-request branch history; and
  - visual job `93489719881`: PASS at the unchanged `97/97` fixed-Linux
    governed matrix.
- Controlled runtime correctly skipped because this checkpoint intentionally
  activated no live persistence or route boundary.
- Precursor run `31398601324` passed repository scope and exposed only the
  stale generated catalog. Commit `37a4d9d` corrected that generated artifact
  and the four translation residues; it changed no product invariant, test,
  baseline, threshold or Gate criterion.

## Review, rollback and next checkpoint

Task Diff Review confirms that checkpoint 1 is additive and creates no
runtime behavior. Before retained P7-02 rows exist, this foundation can be
reverted and a disposable Site migrated fresh. After checkpoint 2 activation,
rollback must disable only the independent execution routes/workspace and use
reviewed forward repair; immutable input, actual, sample, file, receipt,
lifecycle and audit truth must never be deleted or rewritten.

Checkpoint 1 is PASS. Checkpoint 2 alone is active: implement the Project-first
repository/BFF/private-file boundary, exact `planned -> prepared -> running`
transitions, actor-bound idempotency, one-transaction audit/rollback and the
independent fail-closed route switch. UI and controlled runtime remain later
checkpoints.
