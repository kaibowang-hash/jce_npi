# P7-04 Checkpoint 3 — Live Trial Review Workspace

Recorded: `2026-08-11T09:27:57Z`

Decision: `PASS — CHECKPOINT 3`

Exact product checkpoint:
`0b7d3c762fd340d546be316cf7915a7fb31390fb`

Ordinary CI:
`31476917719` (`PASS`)

## 1. Bounded outcome

Checkpoint 3 delivers only the strict P7-04 frontend boundary:

- exact Project/Plan/Round/policy/comparison/reference/conclusion loading
  through the fixed Project-first BFF aggregate;
- chronological comparison over exact immutable Round, input-lock, Actual,
  cavity-result and defect source tuples without a latest-value substitute;
- controlled internal-quality, internal-sample, customer-evidence and
  deviation-or-waiver reference state that never presents evidence as
  approval;
- server-derived blockers, immutable conclusion submission, independent
  approve/reject decision and controlled reopen history;
- explicit `unavailable` formal ERP quality, cycle, yield and customer
  signature truth plus proposal-only Gate/NPI effects; and
- one dense industrial English, Simplified-Chinese and Traditional-Chinese
  workspace covering loading, empty, read-only, permission, validation,
  conflict, processing, retry and unavailable-effect states.

The preview is localized-neutral summary input, not the P7-07 Released Trial
Summary. The browser creates no formal ERP quality result, customer signature,
Gate/Tooling/Work Item mutation, readiness decision, release, external event or
production print.

## 2. Exact-SHA verification evidence

Final ordinary pull-request CI `31476917719` passed the exact product SHA:

- repository `93732624387`: `1,601/1,601` tracked Python tests and the complete
  repository verification PASS;
- frontend `93732624360`: `54/54` files, `853/853` unit tests, `378/378`
  non-visual E2E, `6,770` direct English sources with `100%` direct `zh` and
  `zh-TW` coverage, statements `80.05%`, branches `80.18%`, functions
  `82.26%`, lines `82.75%`, production build and zero vulnerabilities;
- secret scan `93732624335`: current-task guard plus current-tree and complete
  pull-request-history Gitleaks PASS; and
- visual `93732624345`: expanded `106/106` fixed-Linux governed matrix PASS,
  including P7-04 English `1366x768`, Simplified Chinese `1440x900` and
  Traditional Chinese `1920x1080` cases.

Visual artifact `9095701660`, `r1-06-linux-visual-evidence`, contains 79 files
and has digest
`sha256:f0218eafc63b976f672140e0830c0491f9d691b5769c33c55caa480219262617`.
Gitleaks artifact `9095595590` has digest
`sha256:e4107514045904f3a28db706bc5b72002706af5805fc4e6dd67603ea51f5d101`.
Controlled runtime correctly skipped because checkpoint 3 does not authorize a
runtime fixture or Level 2 claim.

## 3. Diagnostic closure and visual review

Diagnostic CI `31475485414` exposed two bounded evidence failures rather than
a product-authority defect: complete frontend statements were `79.58%`
(`10,045/12,622`) against the unchanged `80%` threshold, and the fixed-Linux
visual job passed `94` cases while producing twelve expected Trial-surface
candidates. Nine candidates were P7-01/P7-02/P7-03 consequences of the shared
Trial navigation and inspector truth; three were the new P7-04 language cases.

All twelve Linux actuals from artifact `9095162279` (digest
`sha256:1b89b58438c60452e5c4e9deca7d66326e99ca9554129ffb1ba8a4aae07e809e`)
were inspected in their exact viewport, language and scale. Only those reviewed
Linux candidates were promoted. Five focused unit cases then covered conflict
replay, chronological exact comparison, the full controlled-reference tuple,
immutable proposal-only conclusion submission and exact reopen. No assertion,
case, threshold, tolerance, translation requirement or product authority was
removed or weakened.

Local focused Trial review checks passed `30/30`; the final complete local unit
run passed `853/853` with statements above the unchanged threshold. TypeScript,
ESLint, Prettier, current-task validation, JSON validation and
`git diff --check` also passed. Darwin snapshots and generated local evidence
directories remain untracked and are not accepted evidence.

## 4. Task-scope review

The checkpoint-3 range
`b65415f8789be3b24c8f3ab8be0a85a5f5f636b3..0b7d3c762fd340d546be316cf7915a7fb31390fb`
contains `35` files, `5,471` insertions and `311` deletions across the controller
transition, product workspace and bounded test/visual repair commits. Every
committed path is inside `implementation/CURRENT_TASK.json` and belongs to the
strict data source, dense Trial review workspace, direct translations/catalog,
focused fixtures/tests, governed visual matrix, reviewed Linux baselines or
checkpoint control evidence. No user-owned dirty file, Darwin snapshot,
generated local report or development-prerequisite experiment is in the range.

## 5. Security, rollback and transition

The UI preserves Project-first authorization, exact policy/authority and
source hashes, actor-bound idempotency, immutable server blockers and
transactional command truth. Conflict retry retains the original idempotency
key and never implies success. Cycle/yield/formal quality/customer approval
remain textually unavailable when no exact provider exists, and proposed
effects never execute.

After retained history, rollback disables only the independent P7-04 routes
and review workspace and uses reviewed forward repair; it never deletes or
rewrites comparison, reference, conclusion, lifecycle, receipt or audit truth.

Checkpoint 3 passes. Standing continuous-delivery authority activates only
checkpoint 4 cumulative disposable-Site runtime, exact-SHA controlled dispatch,
Requirement trace, Task Diff Review and Level 2. This checkpoint does not claim
controlled runtime, Level 2 task completion or the Phase 7 Level 3 Gate.
