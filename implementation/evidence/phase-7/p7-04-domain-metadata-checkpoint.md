# P7-04 Domain and Additive Metadata Checkpoint

Recorded: `2026-08-11`

Status: `PASS — CHECKPOINT 1`

Exact product checkpoint:
`8e676acaebb08efbe8f322d7abeba894770f86c6`

Ordinary PR CI:
`31465224626` (`success`)

## Delivered boundary

Checkpoint 1 adds the pure immutable foundations required by the frozen
`p7-04-plan.md`:

- deterministic comparisons across at least two exact same-Project/same-Plan
  Round source tuples, including exact input, Actual, cavity and defect tips;
- explicit `measured`, `not_measured` and `unavailable` truth without zero
  imputation, unsafe unit deltas or latest-value substitution;
- distinct immutable controlled-review-reference successors whose evidence
  presence never becomes approval;
- exact published conclusion-policy versions and server-derived blockers;
- immutable conclusion submit, approve-or-reject decision and controlled
  reopen successors; and
- a localized-neutral one-page summary input with every external effect held
  as `unavailable` or `proposal_only`.

Closed OpenAPI schemas and ownership entries describe this boundary without
opening a P7-04 route. Four guarded append-only DocTypes retain only canonical
snapshots and exact hashes: conclusion policy version, Round comparison
snapshot, review reference revision and conclusion revision. Metadata creates
no business row, production policy, authority binding, fixture or ERP adapter.
All new Link targets are proven to exist in the repository. Generic update and
delete remain denied.

The command receipt registry recognizes only the frozen future operations and
targets. It does not execute lifecycle or business behavior in checkpoint 1.

## Verification evidence

Focused local verification passes:

- `29` domain, contract and metadata tests;
- generated catalog equality;
- direct i18n audit at `6,664` literal English sources with `100%` `zh` and
  `zh-TW` coverage; and
- `git diff --check`.

Exact-SHA ordinary CI `31465224626` passes:

- repository job `93696644312`: `1,586` tracked Python tests and repository
  verification;
- frontend job `93696644266`: `843/843` unit tests, `371/371` non-visual E2E
  tests and the `6,664`-source complete direct trilingual audit;
- secret job `93696644315`: current-tree and complete pull-request history
  boundaries; and
- visual job `93696644330`: unchanged `103/103` fixed-Linux governed visual
  baselines.

The controlled Site job correctly skips because checkpoint 1 opens no route,
business row or runtime fixture.

## Holds and next authorized checkpoint

Cycle/yield and formal ERP quality remain explicitly unavailable unless an
exact governed source or later authorized adapter exists. No customer
signature, Gate, Tooling, Work Item, readiness, release, external projection
or production-print authority is implied.

Only checkpoint 2 is authorized next: Project-first review reads and exact
begin-analysis/comparison/reference/conclusion/decision/reopen commands with a
fail-closed published policy/authority boundary, server blockers, actor-bound
idempotency, one transaction, append-only audit and an independently default-
closed P7-04 switch. UI and controlled runtime fixtures remain inactive.

Rollback before retained runtime rows is commit reversion plus disposable-Site
migration. After any retained history exists, rollback is route/workspace
disable plus reviewed forward repair; exact sources and decisions are never
rewritten.
