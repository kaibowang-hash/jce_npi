# P7-07 Repository, BFF and Source Adapter Checkpoint

Recorded: `2026-08-15`

Status: `PASS — CHECKPOINT 2`

Exact product checkpoint:
`b6a50b9c1fb6bd38bc7cb1099c8744d57e4e96e6`

Ordinary PR CI:
`31874165243` (`success`; `pull_request`; exact product SHA)

## Delivered boundary

Checkpoint 2 activates only the repository, closed BFF and exact controlled-
print source-adapter boundary authorized by the frozen `p7-07-plan.md`:

- exact server enumeration of the frozen Project, Plan, Round, locked-input,
  Actual, Sample, cavity, Trial and Tooling defect, action, verification,
  comparison, controlled-reference and decided-conclusion graph;
- Project-first history/current reads, Project/Round/summary-stream locks and
  exact revalidation of the unique current `approved` or `rejected`
  conclusion;
- append-only retain and revise commands with exact predecessor/current-tip
  checks, actor-bound idempotency and sealed replay;
- receipt, immutable row, audit, workspace projection and receipt seal in one
  transaction, with rollback on every failure;
- three closed BFF routes behind the independent default-closed
  `npi_p7_07_routes_disabled` switch, with authentication before parsing,
  CSRF on commands, exact technical-role authority and response validation;
  and
- exact `released_trial_summary` registration in the existing P5-06 source
  registry, resolving only the persisted summary revision, Project, tenant,
  version and projection hash and remaining unavailable when no independently
  published mapping matches.

The Tooling-defect source kind represents the already frozen complete P7-04
defect tips in the Released Summary graph. It is read-only source truth and
does not grant P7-07 authority to create, revise, close, reopen or otherwise
mutate Tooling lifecycle data.

Checkpoint 2 adds no frontend action, runtime fixture, production mapping,
Print Format, PDF output, external event or consumer projection. It performs
no Gate, Project, Work Item, Tooling lifecycle, Trial-source, ERPNext/JCE,
integration, Outbox/Inbox or provider mutation.

## Verification evidence

Focused and complete local verification passes:

- `39/39` focused P7-07 domain, metadata, contract, repository, API and source-
  registry tests;
- `1,911/1,911` complete Python tests in the current local working tree and
  complete repository verification;
- current-task and V1.2 reconciliation verification; and
- OpenAPI YAML, generated catalog, compilation and `git diff --check`.

Exact-SHA ordinary PR CI `31874165243` passes all required lanes:

- repository job `94987257323`: `1,905/1,905` tracked Python tests plus
  prototype, reconciliation and repository verification;
- frontend job `94987257376`: `58/58` files, `908/908` unit tests,
  `399/399` non-visual E2E tests, `7,364` direct trilingual sources, coverage
  `80.39%/80.26%/83.05%/83.03%` and zero vulnerabilities;
- secret job `94987257391`: `467` full-history commits scanned and no leak;
  and
- visual job `94987257304`: unchanged complete `112/112` fixed-Linux governed
  visual matrix.

Visual artifact `9244313048` has digest
`sha256:088c577eba4aed946a7fd20f822f379769da11ca91d3b229cd743bf810769e07`.
Gitleaks artifact `9244270139` has digest
`sha256:c6a37ba80211c6a52dd7dbb07b599f10e9c52f8490f4db827a06c0a889f3b7d2`.
Controlled preflight `94988374172` and runtime `94988374331` correctly skip
because checkpoint 2 installs no runtime fixture or synthetic mapping.

This is checkpoint 2 PASS only, not P7-07 Level 2 or Level 3. The latest
complete Level 3 remains workflow `31392474781` at exact SHA
`22cb24d42174a5b75f475127ac3aa9fee5a08606`.

## Holds and next authorized checkpoint

`DR-REC-003`, `DR-REC-004` and `DR-REC-009` remain unchanged. A decided Trial
conclusion and its technical summary, QR, hash or controlled output are not
customer approval, signature, production acceptance, G7, Gate or external
delivery truth. Production mapping, form owner, signer, retention, browser
print, numbered copies, exact external event/payload/consumer/redaction route
and target receipt remain unavailable.

Only checkpoint 3 is authorized next: a dense trilingual Released Summary
section in the existing live Trial workspace, strict browser data source,
exact current/history inspection, source/redaction/authority truth, reviewed
retain/revise flow, reused controlled-print action and the complete honest
state, accessibility, i18n, E2E and governed visual evidence defined by the
frozen plan. It may add no runtime fixture, production mapping, generic print
behavior, external authority or downstream mutation.

Rollback before retained runtime rows is commit reversion plus disposable-Site
migration. After retained summary or controlled-output history exists,
rollback disables only the independent P7-07 routes and workspace and uses a
reviewed forward repair. Exact history is never deleted, rewritten,
renumbered, rehashed or rerendered.
