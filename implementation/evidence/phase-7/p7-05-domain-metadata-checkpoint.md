# P7-05 Domain and Additive Metadata Checkpoint

Recorded: `2026-08-11`

Status: `PASS — CHECKPOINT 1`

Product commit:
`60da5cf1f437edbd7cfd0ebbc6ab4f099124d584`

Final exact checkpoint:
`c75956c4ef14677fe29a27f67f622a6c9f1fc8d1`

Ordinary PR CI:
`31496046593` (`success`)

## Delivered boundary

Checkpoint 1 adds only the pure immutable foundations authorized by the frozen
`p7-05-plan.md`:

- reusable versioned readiness-template drafts and immutable published
  versions with explicit Project-type, exact Customer-reference and NPI-owned
  industry selectors;
- an independently frozen Project readiness instance with exact Project,
  template, Gate, category, item, owner, due-date and predecessor identities;
- closed confirmation, exact-evidence and exact-source-result completion rules;
- exact supported NPI source references and identity-free `unavailable` truth
  for formal ERP material, quality, Run-at-rate, HR and supplier providers;
- `readiness-score.v1` category and total basis points derived only from exact
  applicable positive weights with half-even rounding; and
- separately retained incomplete-P0, failed-mandatory-quality and required-
  source-unavailable blockers whose dominance cannot be suppressed by a high
  percentage or a caller-supplied state.

Published template versions are immutable. Instance successors preserve the
frozen policy and revise exactly one applicable item against the exact current
predecessor. Non-applicability is derived from the frozen selector and cannot
be selected later to hide a blocker. The domain exposes no Gate transition,
decision, pass or close method.

Closed OpenAPI schemas and ownership entries describe this boundary without
opening a route. Four guarded additive DocTypes retain the template root,
template version, immutable instance revision and future actor-bound command
receipt. Metadata installs no template, instance, authority, fixture, ERP
adapter or other business row. Generic update/delete remains denied outside
the guarded command context.

## Verification evidence

Focused verification passes:

- `24/24` readiness domain, contract and metadata tests;
- `1,639/1,639` complete tracked Python tests;
- JSON, Python compile, prototype-approval, V1.2 reconciliation, P0 visual-
  governance and prohibited-pattern checks;
- generated React catalog equality and direct i18n audit at `6,866` literal
  English sources with `100%` `zh` and `zh-TW` coverage; and
- TypeScript, lint, coverage, production build and `git diff --check`.

The first product CI correctly failed because the new Frappe catalogs had not
yet been synchronized to the generated React catalog. Repair `c75956c` ran the
repository generator and replaced six unapproved retained `ID`/`JSON` tokens
in Chinese translations with direct Chinese terms. It changed no scanner,
allowlist, threshold, behavior or test.

Exact-SHA ordinary CI `31496046593` passes:

- repository job `93793900208`: complete repository verification and `1,639`
  tracked Python tests;
- frontend job `93793900200`: complete generation/type/lint/coverage/build/
  audit, `853/853` unit tests, `378/378` non-visual E2E tests, `6,866` direct
  trilingual sources and zero vulnerabilities;
- secret job `93793900081`: current-tree and complete pull-request history
  boundaries; and
- visual job `93793900326`: unchanged `106/106` fixed-Linux governed visual
  baselines.

The controlled Site job correctly skips because checkpoint 1 opens no route,
business row, Gate-input behavior or runtime fixture.

## Holds and next authorized checkpoint

Formal ERP material, quality, production, Run-at-rate, HR and supplier truth
remains unavailable. Tooling capacity is a governed scenario, not production
actual. No score, confirmation or evidence becomes Gate approval. No Gate,
Work Item, risk, Tooling lifecycle, handover, release, external projection or
print effect is authorized.

Only checkpoint 2 is authorized next: internal-admin template commands,
Project-first instance read/initialize/revise behavior, exact source resolvers,
actor-bound replay, one transaction and append-only audit, an independently
default-closed P7-05 switch, and bounded inclusion of current readiness P0
blockers plus one exact readiness-revision dependency in the existing Gate
review input. It may not decide, pass, close, reopen or otherwise mutate a
Gate. UI and controlled runtime fixtures remain inactive.

Rollback before retained runtime rows is commit reversion plus disposable-Site
migration. After any retained template or instance history exists, rollback is
route/workspace/Gate-input disable plus reviewed forward repair; exact history
and derived snapshots are never rewritten.
