# P7-07 Domain and Additive Metadata Checkpoint

Recorded: `2026-08-15`

Status: `PASS — CHECKPOINT 1`

Exact product checkpoint:
`684c6833a4e0c2732ce55cfc1883fa805f07dd97`

Ordinary PR CI:
`31872006649` (`success`; `pull_request`; exact product SHA)

## Delivered boundary

Checkpoint 1 adds only the pure immutable foundations authorized by the frozen
`p7-07-plan.md`:

- one append-only Released Trial Summary revision domain per exact Project and
  Trial Round, with exact predecessor lineage and a successor allowed only for
  a newly decided `approved` or `rejected` Trial conclusion;
- canonical complete source manifests retaining exact source kind, global ID,
  version and snapshot hash without latest-value substitution;
- a closed localized-neutral presentation projection whose fact groups, fact
  fields, value states, values, units and ordered source references are exact,
  source-bound and recursively free of private locators and sensitive fields;
- a strict `524288`-byte UTF-8 projection boundary that rejects overflow and
  never truncates, samples or fabricates truth;
- a closed server-owned redaction manifest and explicit unavailable external
  effects for customer approval, external projection, formal signature, Gate
  decision and production acceptance;
- closed OpenAPI schemas and explicit ownership entries without an active path;
  and
- one guarded append-only `NPI Released Trial Summary Revision` DocType plus
  only `released_trial_summary.retain` and
  `released_trial_summary.revise` on the existing actor-bound Trial receipt.

Metadata installs no summary row, source adapter, controlled-print mapping,
Print Format, File, PDF, event, projection, provider, endpoint, credential,
fixture or other business default. Generic writes remain denied outside the
guarded Trial command context, and retained history cannot be updated, renamed,
deleted, exported, printed or emailed through generic DocType behavior.

The exact historical P7-07 plan fingerprint is allowlisted as one reviewed
Gitleaks false positive and mirrored in the strict verifier and its unit test;
the allowlist remains an exact closed fingerprint inventory.

## Verification evidence

Focused and complete local verification passes:

- `20/20` focused P7-07 domain, metadata and contract tests;
- `57/57` affected P7-07, Trial metadata/review metadata and strict Gitleaks
  verifier tests;
- `1,899/1,899` complete Python tests in the current local working tree and
  complete repository verification;
- current-task, V1.2 reconciliation, JSON/YAML, metadata, generated-catalog,
  prohibited-pattern and whitespace checks; and
- direct i18n audit at `7,360` literal English sources with `100%` `zh` and
  `zh-TW` coverage and no unapproved retained Latin token.

Exact-SHA ordinary pull-request CI `31872006649` passes:

- repository job `94982017438`: `1,893` tracked Python tests plus complete
  reconciliation and repository verification;
- frontend job `94982017474`: `58/58` files, `908/908` unit tests,
  `399/399` non-visual E2E tests, generation/type/lint/coverage/build/audit,
  `7,360` direct trilingual sources, coverage
  `80.36%/80.24%/83.05%/83.00%` and zero vulnerabilities;
- secret job `94982017414`: all `28` committed P7-07 task paths, the exact
  `26`-commit first-parent pull-request range and `465`-commit branch history
  with no leak; and
- visual job `94982017419`: unchanged complete `112/112` fixed-Linux governed
  visual matrix.

Visual artifact `9243731265` has digest
`sha256:8ee405cb06c832ac6f698bfbc7e09007408673684f92a55ffdf70473497348d9`.
Gitleaks artifact `9243685598` has digest
`sha256:dd85e880d7ecc671abde0abed07e2d06cc08a7d00e15cdd51db110ac12e6d849`.
Controlled preflight `94983213717` and runtime `94983213982` correctly skip
because checkpoint 1 opens no route, business row, source adapter or runtime
fixture.

Independent scope, domain, contract and metadata review reports no P0/P1
finding. This is checkpoint 1 PASS only, not P7-07 Level 2 or Level 3.

## Holds and next authorized checkpoint

An approved or rejected Trial conclusion and its technical summary remain
technical facts, not customer approval, signature, production acceptance, G7
or Gate truth. External event identity, payload, consumer redaction, routing,
delivery, projection and target receipt remain held under `DR-REC-009`.
Production form mapping, signer, retention, browser-print and numbered-copy
policy remain held under `DR-REC-003` and `DR-REC-004`. Gate, Project, Work
Item, Tooling, ERP, Outbox, provider and production-print mutation remain held.

Only checkpoint 2 is authorized next: exact source graph loaders; Project,
Round and summary-stream locks; Project-first history/current reads; retain and
revise commands; exact current decided-conclusion revalidation; actor-bound
sealed replay; one transaction; append-only audit; the independent default-
closed `npi_p7_07_routes_disabled` boundary; and registration of the exact
`released_trial_summary` controlled-print source adapter. It may not add a
frontend action, runtime fixture, production mapping, external event/projection
contract, PDF authority, Gate/Project/Work Item/Tooling/ERP/Outbox mutation or
formal release semantics.

Rollback before retained runtime rows is commit reversion plus disposable-Site
migration. After retained summary, receipt, audit, controlled-print snapshot,
output or private File history exists, rollback disables only the independent
P7-07 routes and Trial workspace and uses a reviewed forward repair. Exact
history is never deleted, rewritten, renumbered, rehashed or rerendered.
