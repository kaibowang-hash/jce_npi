# P7-02 Live Trial Execution Workspace Checkpoint

Recorded: `2026-08-11T05:35:00Z`

Status:
`PASS — LEVEL 1 LIVE WORKSPACE, TRILINGUAL, ACCESSIBILITY AND VISUAL EVIDENCE`

Primary requirements:
`FR-TR-002`, `FR-TR-003`, `FR-TR-010`, `FR-NP-004`, `FR-NP-005` and
`FR-TX-019` foundation.

Exact stable product checkpoint:
`9f264d7afb80087300b2da5ecbb5b784094a606b`

Primary product commit:
`913e124`

## Delivered boundary

- Added one strict Trial execution data source over the independently
  default-closed Project-first execution read and prepare/start/manual Actual/
  Sample/upload/evidence/content commands. Responses are accepted only when
  exact Project, Round, lock, revision, Sample, evidence, File, capability,
  receipt and replay identities match the reviewed request.
- Extended the selected-Project Trial workspace with a dense locked-input,
  parameter, Sample Batch and controlled-evidence work area. Immutable input
  versions, measured/not-measured truth, units, sources, timestamps, cavity
  lineage, packaging/destination/feedback and private-file scan truth remain
  visible without collapsing into a latest-value summary.
- Loading, empty, pending scan, no permission, read-only, route unavailable,
  validation, conflict, processing, retryable failure and success are explicit.
  Automatic machine import remains visibly unavailable and cannot be selected.
- The work area preserves one primary action in the active context, compact
  industrial tables/panels, translated keyboard paths, focus behavior and
  non-color-only status expression.
- All new visible sources are literal English and have direct Simplified- and
  Traditional-Chinese Frappe CSV translations. The stable checkpoint contains
  `6,299` governed sources at 100% direct `zh`/`zh-TW` coverage.

## Security and no-fake-success proof

- The browser cannot submit tenant, actor, current Round lifecycle, input/file
  hash, scan/privacy state, derived completeness, machine-import truth, ERP
  verification, approval, Gate effect or raw private URL.
- Project authorization and exact Round/reference/File containment remain
  server-side. Capability hiding in the UI is presentation only.
- Pending/infected/failed/public/mismatched File states do not expose an
  evidence success path. Evidence access remains audited and byte-returning,
  with no redirect or stable bearer URL.
- Replay is shown only from the sealed server response. Validation, conflict
  and retry failures never advance the visible immutable history or claim a
  command completed.
- No conclusion, submission, approval, formal Quality Inspection/NCR, Gate,
  Tooling lifecycle, reservation, external projection or production print
  authority was added.

## Changed-files -> affected-tests

| Change surface | Direct evidence |
| --- | --- |
| strict Trial execution data source | focused schema, identity, command, replay, conflict and error tests |
| dense execution workspace | component tests for locked inputs, Actuals, Samples, evidence and complete state matrix |
| Trial planning composition | regression tests proving the existing Plan/Round workspace remains intact |
| trilingual UI and accessibility | direct catalog audit, mixed-language scan, keyboard/Axe and tooltip/accessibility checks |
| affected visuals | direct P7-02 English, Simplified-Chinese and Traditional-Chinese cases plus complete governed Linux matrix |
| complete checkpoint | exact-SHA repository, frontend/E2E, visual and both secret lanes in CI below |

## Exact-SHA ordinary CI

Ordinary CI run `31418866090` passed exact stable checkpoint `9f264d7`:

- repository `93554463838`: `1,522/1,522` tracked Python tests and complete
  repository verification PASS;
- frontend `93554463683`: `54/54` files and `832/832` unit tests,
  `365/365` non-visual E2E, `6,299` direct trilingual sources and zero
  dependency vulnerabilities;
- visual `93554463773`: `100/100` fixed-Linux governed cases PASS, including
  all three direct P7-02 locales;
- secret scan `93554463846`: current-task guard plus current-tree and complete
  pull-request-history Gitleaks PASS with no leaks; and
- controlled runtime correctly skipped because checkpoint 4 was not active.

## Review, rollback and next checkpoint

The checkpoint range after repository/BFF checkpoint `318f1c8` through
`9f264d7` contains only the bounded execution data source/workspace, direct
tests, trilingual catalogs, explicit visual-impact declaration and two
evidence-proved browser-byte compatibility repairs. No test, locale, viewport,
threshold, accepted baseline or PASS criterion was removed or weakened.

User-owned dirty files, Darwin snapshots, local evidence reports and untracked
development prerequisites were not staged. After retained Trial history,
rollback disables only the independent P7-02 execution routes/workspace and
uses a reviewed forward repair; it never deletes or rewrites input locks,
Actuals, Samples, Files, evidence, lifecycle events, receipts or audits.

Checkpoint 3 is PASS, not the P7-02 Task Gate. Standing transition authority
activates only checkpoint 4: cumulative disposable-Site runtime and Level 2.
