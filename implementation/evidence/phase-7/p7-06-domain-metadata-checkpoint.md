# P7-06 Domain and Additive Metadata Checkpoint

Recorded: `2026-08-14`

Status: `PASS — CHECKPOINT 1`

Exact product checkpoint:
`d078f063c35fb7a0f7b8d74c634e17d5ff238cb1`

Ordinary PR CI:
`31789635452` (`success`; `pull_request`; exact product SHA)

## Delivered boundary

Checkpoint 1 adds only the pure immutable foundations authorized by the frozen
`p7-06-plan.md`:

- guarded optimistic Production Transition policy drafts and immutable
  published versions with explicit Project applicability and no installed
  business default;
- ordered sender and receiving acknowledgement slots, receiving-group keys,
  handover object requirements, unresolved-action rules and mandatory
  observation-source/conclusion rules owned by the exact published policy;
- the user-approved Scheme A manifest contract: the browser submits only the
  exact published-policy `requirementKey`, source kind, ID and expected
  version; each requirement freezes accepted kinds, minimum count and
  `manifestRole`, which the server injects; and one exact kind/ID tuple belongs
  to one requirement and counts once;
- append-only handover package revisions over exact Project and source
  identities, versions and hashes, plus independent actor-slot acknowledgement
  facts whose query-derived `fullyAcknowledged` state never rewrites a package
  snapshot and never transfers to a successor;
- a separate append-only observation-period stream whose exact references
  freeze kind, ID, version, hash and `context` or `retrospective` usage without
  handover `requirementKey` or `manifestRole`; and
- the five mandatory actual-SOP, first-batch-yield, customer-complaint,
  production-cycle-time and Tooling-stability providers as server-fixed,
  identity-free `unavailable`, yielding only `not_evaluable` and performing no
  network work.

Closed OpenAPI schemas and explicit ownership entries describe eleven
operations and nine future commands without opening a route. Six guarded
additive DocTypes retain the policy root/version, immutable package revision,
independent acknowledgement, immutable observation revision and future
actor-bound command receipt. Metadata installs no policy, organization
mapping, package, acknowledgement, provider, adapter, endpoint, credential,
fixture, Gate binding or other business row. Generic writes remain denied
outside the guarded repository contexts, and published/snapshot history is
immutable.

## Verification evidence

Focused and complete local verification passes:

- `52/52` focused Production Transition domain, metadata and contract tests;
- all `219/219` OpenAPI consumer tests;
- `1,802/1,802` complete Python tests in the current local working tree and
  repository verification;
- current-task, V1.2 reconciliation, JSON/YAML, metadata, generated-catalog,
  prohibited-pattern and whitespace checks; and
- direct i18n audit at `7,193` literal English sources with `100%` `zh` and
  `zh-TW` coverage.

Exact-SHA ordinary pull-request CI `31789635452` passes:

- repository job `94733409400`: `1,796` tracked Python tests plus complete
  reconciliation and repository verification;
- frontend job `94733409390`: `56` files, `881/881` unit tests, `388/388`
  non-visual E2E tests, complete generation/type/lint/coverage/build/audit and
  `7,193` direct trilingual sources with complete `zh`/`zh-TW` coverage;
- secret job `94733409403`: all `41` committed P7-06 task paths, the complete
  `25`-commit pull-request range and `453`-commit branch history with no leak;
  and
- visual job `94733409444`: unchanged `109/109` fixed-Linux governed visual
  baselines.

Visual artifact `9215015413` has digest
`sha256:af255a03accfb6d1e64f29f896bdf2f9d2ce4fdb466ef613dbb43f6476ec2c54`.
Gitleaks artifact `9214911743` has digest
`sha256:7d4bab48c53b553e2e479a19cbdae92cfffba5a5b9c7d9c68a267e6044401c43`.
Controlled preflight `94736007237` and runtime `94736007523` correctly skip
because checkpoint 1 opens no route, business row, Gate behavior or runtime
fixture.

Independent scope, domain, contract and metadata audits report no P0/P1
finding. This is checkpoint 1 PASS only, not P7-06 Level 2 or Level 3.

## Holds and next authorized checkpoint

An acknowledgement remains a fact, not an electronic signature, approval,
formal production acceptance or G7 decision. Trial, readiness and capacity
evidence remains NPI context and never becomes an external production actual.
Formal receiving-organization and bilateral authority, actual SOP, external
yield/complaint/cycle/Tooling actuals, stability policy and authority, G7 close,
Gate/Project/Work Item/Tooling mutation, production ERPNext, network, Outbox,
release, external projection and production print remain held.

Only checkpoint 2 is authorized next: internal-admin policy commands,
Project-first production-transition reads and exact package, acknowledgement
and observation commands; closed canonical source resolution; actor-bound
idempotency; one transaction; append-only audit; and the independently
default-closed `npi_p7_06_routes_disabled` boundary. It may not add a Gate
input or evidence hook, mutate a Gate, Project, Work Item or Tooling object,
contact an external provider, activate UI or add a runtime fixture.

Rollback before retained runtime rows is commit reversion plus disposable-Site
migration. After retained policy, package, acknowledgement, observation,
receipt or audit history exists, rollback disables only the independent P7-06
routes and Project workspace and uses a reviewed forward repair. Exact history
is never deleted, rewritten, renumbered or copied to simulate reversal.
