# P6-03 Domain, Contract and Metadata Checkpoint

Recorded: `2026-08-08T04:53:01Z`

Status:
`PASS — LEVEL 1 DOMAIN, CLOSED CONTRACT AND GUARDED METADATA`

Requirements:
`FR-TX-004..008`, `FR-TL-002`, `FR-TL-003`, `FR-TL-006`

Exact stable checkpoint:
`96f201cc933ad1544acc62cfc4006445688aebdb`

## Delivered boundary

- Added pure immutable Tooling Revision, closed unit-bearing specification,
  cavity, insert/changeover, external-identity, controlled Part specification,
  ordered process-chain and one-time physical-Set source-binding invariants.
- Added four guarded additive DocTypes: `NPI Tooling Revision`, `NPI Part
  Controlled Specification`, `NPI Tooling Process Chain Revision` and `NPI
  Tooling Set Revision Binding`. They create no business row or default and
  deny generic mutation, deletion, export and print.
- Added exact NPI One ownership rows, four closed command receipt
  operation/target pairs and closed OpenAPI schemas. Existing P6-01/P6-02
  paths retain their meanings and no P6-03 repository or route is active.
- Added literal English sources and direct `zh`/`zh-TW` coverage. The generated
  catalog contains `4,312` governed sources at 100% direct coverage.

## Deliberately unavailable

- No repository, BFF route, frontend command, controlled-Site runtime, business
  row, production policy, fixture, mapping, adapter, credential or external
  mutation is activated by this checkpoint.
- `DR-REC-010` continues to hold exact lifecycle states, transitions, release,
  supersession and authorities. Creating a Revision does not approve it or
  authorize manufacturing or Trial use.
- Formal Supplier, ERP Asset/location/execution, combined Trial results,
  automatic impact action and production Tooling-list mapping remain later
  capabilities. `supplier_reference` is provenance only.
- The Set-source binding is initial and append-only. It never rewrites an
  existing P6-02 Set snapshot, intake or evidence record.
- This is checkpoint 1, not the P6-03 Level 2 Task Gate. Repository/BFF, live
  workspace and controlled runtime evidence remain required.

## Local affected and regression evidence

- focused P6-03/P6-01/P6-02 Tooling revision/domain/metadata/contract tests:
  `43/43` PASS;
- P0 visual-governance verifier and its direct unit suite: PASS (`18` governed
  Linux baselines at `1440x900`, `7/7` verifier tests);
- byte comparison against the failed exact-SHA artifact: all `18/18` repaired
  Linux baseline targets match their reviewed actuals;
- OpenAPI, ownership YAML, four DocType JSON files, generated catalog,
  compilation, full reconciliation and `git diff --check`: PASS; and
- complete clean exact-SHA CI evidence below supersedes the bounded local
  regression for the final checkpoint decision.

The workspace contains pre-existing user-owned development and Darwin visual
files. They were preserved and excluded from both checkpoint commits.

## Changed-files -> affected-tests

| Change surface | Direct evidence |
|---|---|
| `tooling/revision_domain.py` | lineage/hash, closed/unit-bearing specifications, cavity count/mapping, insert validation, external identity effectivity, controlled Part specification, process-chain order/acyclicity and Set-binding cases in `test_phase6_tooling_revision_domain.py` |
| four guarded DocTypes/controllers and receipt values | exact fields/options, immutability, generic CRUD/delete denial, no lifecycle/ERP/private-URL invention and additive metadata assertions in `test_phase6_tooling_metadata.py` |
| OpenAPI and data ownership | closed schemas, ownership, inactive-route boundary and no fake release/Supplier/ERP/Trial claims in `test_phase6_tooling_contract.py` |
| translation catalogs/generated catalog | generation plus complete direct `zh`/`zh-TW` i18n audit |
| shared footer fingerprint baselines | exact eighteen fixed-Linux screenshots and P0 visual-governance verifier |

## Exact-SHA CI and bounded visual repair

Product commit `35229fa` ran ordinary CI `31225090693`. Its repository job
passed all product, contract, i18n, non-visual E2E and secret checks, while the
visual job passed `58/76` and failed only the eighteen durable P0 screenshots.
Artifact `9011854268` proved every changed pixel was confined to the bottom
status-bar catalog fingerprint: English rows changed only within
`x=567..676, y=882..891`; Chinese rows only within
`x=504..612, y=882..891`. The product workspace had no pixel delta.

The eighteen reviewed Linux actuals were copied byte-for-byte only to their
matching fixed-Linux targets in isolated repair commit
`96f201cc933ad1544acc62cfc4006445688aebdb`. No component, state, assertion,
matrix, threshold or PASS rule changed; user-owned Darwin screenshots were
not staged.

Final ordinary CI `31239993150` passed at exact stable checkpoint `96f201c`:

- repository job `93059251734`: PASS — `1,154/1,154` Python tests,
  `738/738` frontend unit tests, `315/315` non-visual E2E, `4,312` literal
  English sources with 100% direct `zh`/`zh-TW`, zero dependency
  vulnerabilities and no secret leaks;
- visual job `93059251780`: PASS — `76/76` fixed-Linux cases;
- controlled runtime job `93059252121`: correctly skipped;
- visual artifact `9016729205`, digest
  `sha256:72300de78862d7e79a10694d0d7518e82b65e21fafa0992f64c8d3a54d144abc`;
  and
- Gitleaks artifact `9016791693`, digest
  `sha256:f178ae11312410a8e97594c39518242b79d04457238054dfbac6a7d702345433`.

## Review, rollback and next checkpoint

The checkpoint is additive, creates no business rows and activates no route.
Before retained rows exist, a disposable environment may restore the starting
checkpoint and migrate fresh. After retained rows exist, rollback disables
only P6-03 routes/projections and uses a reviewed forward repair while
preserving every immutable Revision, specification, cavity, insert, external
identity, chain, binding, audit and receipt.

Checkpoint 1 is PASS. P6-03 remains in progress. Autopilot next implements
only checkpoint 2: Project-first bounded Revision, controlled-Part-
specification, process-chain and Set-binding reads/commands; exact containment,
effectivity/current-tip checks; System Manager-only mutations; actor-bound
idempotency; one transaction; append-only audit; an independent fail-closed
route switch; and exact API/IDOR tests. The live SPA and controlled-Site
runtime remain inactive until that checkpoint passes affected checks and
complete ordinary CI.
