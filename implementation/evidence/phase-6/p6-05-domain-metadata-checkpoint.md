# P6-05 Domain, Contract and Metadata Checkpoint

Recorded: `2026-08-08T18:03:44Z`

Status:
`PASS — LEVEL 1 DOMAIN, CLOSED CONTRACT AND GUARDED METADATA`

Requirements:
`FR-TX-009`, `FR-TX-010`, `FR-TX-011`, `FR-TX-019`, `FR-TX-020`,
`FR-TL-009`, `FR-TL-010` foundation, `FR-TL-017` foundation and
`FR-TL-018` foundation

Exact stable checkpoint:
`4f5270b444c343c88cdab7a45aaf33c5c164d759`

## Delivered boundary

- Added an immutable Tooling-defect revision domain with direct predecessor
  and hash lineage, the frozen sequential state machine, explicit severity and
  independent blocking intent, exact Tooling Revision/cavity/detection
  context, root-cause truth, Project-member responsibility, retained actions
  and clean private File Revision evidence.
- Added immutable process-profile revisions whose Customer Standard, Trial
  Actual and Approved Baseline layers cannot collapse. Exact released-Document
  and Tooling-Revision source contexts, typed metrics, explicit comparison
  rules and the textual `not_measured`, `unavailable`, `within_tolerance` and
  `outside_tolerance` results retain exact source and rule hashes.
- Added immutable Capacity Scenario revisions with every hours/days/OEE/yield/
  cycle/cavity/usage/set/demand input, exact Part/Applicability/Set provenance,
  the contract-visible `capacity.v1` formula and deterministic
  `decimal-6-half-even` part, assembly, bottleneck and gap results.
- Added an explicit read-only unavailable health projection. It fabricates no
  shot count, calibration, maintenance, score, warning or advice.
- Added three guarded append-only DocTypes, three command-receipt operation/
  target pairs, exact ownership rows and closed OpenAPI component schemas. No
  P6-05 route, repository command, projection adapter or live UI is active at
  this checkpoint.
- Added literal English sources with complete direct `zh` and `zh-TW`
  translations. The generated catalog contains `4,795` governed sources.

## Deliberately unavailable

- Trial Actual remains `not_measured`; Approved Process Baseline and exact
  Trial identity/approval evidence remain `unavailable` until Phase 7. No
  caller field can manufacture either fact and no Trial row is created.
- Severity never sets blocking, and an explicit blocking intent does not
  create a Domain Work Item, mutate a Gate, pass G5/G6 or transition a Tooling
  Requirement/Revision/Set lifecycle.
- Production exception-color semantics remain held by `DR-REC-002`; textual
  comparison truth does not claim an approved red/error policy.
- ERPNext/IoT shot count, calibration, maintenance, actual operating history
  and health policy remain unavailable. No endpoint, credential, adapter,
  Webhook, retry, replay, write or target-confirmed result was added.
- No route, business row, default tolerance, capacity assumption, fixture,
  policy, mapping or controlled-Site execution is activated by checkpoint 1.
  Repository/BFF, live workspace and runtime evidence remain required.

## Local affected and regression evidence

- focused Phase 6 Tooling domain/metadata/contract regression: `152/152` PASS;
- complete local Python discovery: `1,239/1,239` PASS, including six
  pre-existing user-owned untracked local-prerequisite tests; the clean tracked
  exact-SHA CI independently passed `1,233/1,233` below;
- frontend unit suite: `756/756` PASS;
- catalog generation/check, TypeScript, formatting, i18n audit, OpenAPI/YAML/
  JSON parsing, Python compilation, reconciliation and `git diff --check`:
  PASS; and
- P0 visual-governance verifier: PASS at `18/18` Linux baselines; its direct
  unit suite passed `7/7`.

All pre-existing user-owned development files, Darwin screenshots, local
evidence and `implementation/LAST_RUN.md` were preserved and excluded from the
checkpoint commits.

## Changed-files -> affected-tests

| Change surface | Direct evidence |
|---|---|
| `tooling/engineering_controls_domain.py` | defect state/action/evidence succession, non-collapsed process layers, comparison boundary/unit/zero cases, explicit capacity formula/precision/ties/successors and unavailable health in `test_phase6_tooling_engineering_controls_domain.py` |
| three guarded DocTypes/controllers and receipt values | exact fields/options, parent/hash/member/cavity/release/File/provenance containment, append-only immutability and generic CRUD/delete/export/print denial in `test_phase6_tooling_engineering_controls_metadata.py` plus cumulative Tooling metadata regression |
| OpenAPI and data ownership | closed foundation schemas, read-only server result, formula description, separated ownership and inactive-route/no-fake-Trial/no-ERP-write boundary in `test_phase6_tooling_engineering_controls_contract.py` |
| translation catalogs/generated catalog | generation plus `4,795` literal English sources at complete direct `zh`/`zh-TW` coverage and mixed-language audit |
| governed footer fingerprints | exact eighteen reviewed CI actuals copied byte-for-byte to only their Linux baseline targets and complete `82/82` fixed-Linux CI |

## Exact-SHA CI and bounded visual repair

Product commit `ae501c3523b2346c57a12c6c5964bd64edfb102e` ran ordinary CI
`31269767038`. Repository job `93133640483` passed the complete product,
contract, i18n, non-visual E2E and secret checks. Visual job `93133640517`
passed `64/82` and failed exactly the eighteen durable P0 screenshots after
the additional translated sources changed the bottom status-bar catalog
fingerprint; controlled job `93133640939` correctly skipped.

Artifact `9025279088`, digest
`sha256:1f72a3497c08c9b8ec344630e43c54c508f811f72076c96993a89e08e637ea66`,
contained exactly eighteen actual/diff pairs. The five non-Trial workspaces in
each language had exact-pixel deltas confined to the footer catalog text:
English `x=560..677, y=882..892`; Simplified and Traditional Chinese
`x=496..614, y=882..892`. Trial retained the same catalog delta plus
threshold-below pixels at the extreme lower-right control edge; inspection of
the full expected, actual and Playwright diff images found no business-region
component, layout, copy or state change.

Isolated repair commit
`4f5270b444c343c88cdab7a45aaf33c5c164d759` copied only those eighteen
reviewed CI actuals byte-for-byte to their exact governed Linux targets. It
changed no component, state, assertion, matrix, threshold or PASS rule and
staged no user-owned or Darwin file.

Final ordinary CI `31270566049` passed exact stable checkpoint `4f5270b`:

- repository job `93135659056`: PASS — `1,233/1,233` tracked Python tests,
  `756/756` frontend unit tests, `326/326` non-visual E2E, `4,795` literal
  English sources with 100% direct `zh`/`zh-TW`, statements `80.03%`, zero
  dependency vulnerabilities and both current/history secret lanes;
- visual job `93135659034`: PASS — `82/82` fixed-Linux cases;
- controlled runtime job `93135659341`: correctly skipped;
- visual artifact `9025498587`, digest
  `sha256:d3837e6067d9c1d75b8801997dc2351434e00b54684e02b501783a8a44c097b7`;
  and
- Gitleaks artifact `9025565511`, digest
  `sha256:51ed2b4b149212cc5599f65b9ce81a643b4b35c9e1064d7fa0f61c0d4f9841d5`.

## Review, rollback and next checkpoint

The checkpoint is additive, creates no business rows and activates no route.
Before retained rows exist, a disposable environment may restore the starting
checkpoint and migrate fresh. After retained history exists, rollback disables
only later P6-05 routes/projections and uses a reviewed forward repair while
preserving every immutable defect, action/evidence, process profile, capacity
scenario, audit and receipt. It never rewrites Trial, Gate, Tooling lifecycle
or ERPNext truth.

Checkpoint 1 is PASS. P6-05 remains in progress. Autopilot next implements
only checkpoint 2: one Project-first bounded engineering-controls read and
three narrow append commands; exact Master/Revision/member/context/File/Part/
Applicability/Set containment; System Manager-only mutation; actor-bound
idempotency; one transaction; append-only audit; explicit unavailable Trial
and health truth; an independent fail-closed route switch; and exact API,
permission, IDOR, replay, conflict, rollback, no-fake-actual and no-ERP-write
tests. The live SPA and controlled-Site runtime remain inactive until that
checkpoint passes affected checks and complete ordinary CI.
