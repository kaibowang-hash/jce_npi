# P6-02 Live Physical Tooling Set Workspace Checkpoint

Date: `2026-08-07`

Status: `PASS — CHECKPOINT 3`; this is not the P6-02 Level 2 Task Gate.

## Delivered boundary

Product commit `3b62046d73c694b13f300ca72d7d761f8d76738e` adds only the
server-backed physical Tooling Set and arrival-intake workspace already closed
by checkpoint 2:

- strict exact-response validators and a same-origin browser data source for
  Set collection/detail, Set creation, versioned arrival intake and append-only
  evidence attachment;
- a dense Project/Master-scoped repository, arrival-intake history and truth
  inspector with server-capability-driven actions and independent dirty-state
  reporting;
- customer-owned custody, transport, accessories, the frozen five-category
  inspection and explicit difference/customer-confirmation truth;
- a Project-scoped controlled-document picker that accepts only an existing
  clean File Revision with available integrity and sends its exact global ID,
  never a raw URL;
- explicit loading, empty, unavailable/no-permission, read-only, validation,
  stale/conflict, evidence-conflict, processing and safe-retry states; and
- literal English source copy, direct `zh`/`zh-TW`, keyboard/focus labels,
  non-color-only state and fixed-viewport overflow/accessibility evidence.

Source Tooling Revision, formal Supplier, lifecycle, ERPNext location/Asset,
customer login/signature and every upload/release/overwrite/delete/exposure or
external mutation remain explicitly unavailable. No production policy,
mapping, adapter, endpoint, credential or dependency was installed.

## Local affected and full checks

- strict Tooling data-source/live-page/workspace unit checks: `17/17` PASS;
- complete frontend unit suite: `41` files and `738/738` PASS, with aggregate
  statements `81.26%`, branches `79.74%`, functions `83.81%` and lines
  `83.39%`;
- complete current-workspace Python suite: `1,145/1,145` PASS;
- affected P6-01/P6-02 browser matrix: `12/12` non-visual and `8/8` local
  fixed-Darwin visual cases PASS;
- generated-source check, TypeScript, ESLint, Prettier, Stylelint, boundary and
  industrial UI audits: PASS;
- i18n audit: `4,211` literal English sources with direct `100%` `zh` and
  `100%` `zh-TW` coverage; and
- production build passed. The local brand post-check was blocked only by the
  pre-existing untracked user file
  `frontend/public/images/npi-one-project-management-sketch.png`; the clean
  exact-SHA repository job passed the same brand guard. The user file was not
  changed or staged.

## Evidence-only ordinary-CI repair sequence

Initial exact-SHA ordinary CI `31213774878` ran product commit `3b62046`.
Repository `92982448302` passed `verify.sh`, complete non-visual E2E and both
secret lanes. Visual `92982448359` passed `50` of `73` cases and failed only:

- five P6-01 fixed-Linux views whose governed Tooling surface now includes the
  P6-02 entry point and no longer falsely lists physical Set as unavailable;
  and
- eighteen durable P0 views whose only change was the rendered catalog
  fingerprint after adding the direct P6-02 translations.

Artifact `9007770198`, digest
`sha256:f71a5a5e6489c4c296956cdb75b835a9578e033c82c68eea8e9474be093082cc`,
contained exactly those `23` stable actuals. Review confirmed the Tooling views
remain square, dense and explicit, while the P0 deltas remain confined to the
catalog footer. Controlled job `92982448993` correctly skipped.

Governance/baseline commit `26685ff229a2960a12677a58dda3f20738389eb0`
copies only those `23` byte-exact Linux actuals and adds the new P6-02 visual
spec and artifact paths to the existing governed visual job. It changes no
assertion, threshold or PASS rule. Ordinary CI `31214796127` then passed
repository `92985692894`; visual `92985692814` passed every prior `73` case and
failed only the three newly governed P6-02 cases because their Linux baselines
did not exist. Artifact `9008140984`, digest
`sha256:77af8f873636df087154b02d423126a1eb3f96956b31c7c5249b4b97852c2a62`,
contained exactly those three actual PNGs. Controlled job `92985693561`
correctly skipped.

Baseline commit `b1df79f7cd225154060534e1bc558a593b9fe31f` adds only those
three byte-exact Linux images. Final unchanged exact-SHA ordinary CI
`31215596601` is PASS:

- repository `92988260703`: `1,139/1,139` clean-tree Python tests,
  `738/738` frontend unit tests, `315/315` non-visual E2E, zero-vulnerability
  audit, current-tree Gitleaks and complete PR-history Gitleaks PASS;
- visual `92988260754`: expanded fixed-Linux matrix `76/76` PASS;
- visual artifact `9008421671`, digest
  `sha256:0023f78a46b48728fe87fe28e415989faa451c1eeabb89907a75d4d7d020214d`;
- Gitleaks artifact `9008567686`, digest
  `sha256:bd3662c8d2a5526a590253eb96d51789890884ebae0cb16a78f3e71e0458d90b`;
  and
- controlled job `92988261221` correctly skipped.

## Task Diff Review and rollback

The checkpoint changes only the Tooling browser data source/live page/new Set
workspace, direct translations/generated catalog, local industrial styles,
focused unit/E2E tests, governed visual workflow coverage and artifact-proved
baselines. It consumes the frozen checkpoint-2 BFF without changing public
semantics, permissions, DocTypes, ownership, transaction, idempotency, audit,
baseline thresholds or PASS criteria. Every unrelated dirty or untracked user
file was excluded from all commits.

Before retained use, rollback may revert the three workspace commits. After
retained use, the checkpoint-2 independent route switch remains the safe
forward-only disable path; no Set, Intake, Difference, File Revision reference,
audit or idempotency row is deleted or rewritten.

## Decision and next boundary

Checkpoint 3 passes. The next unfinished boundary is checkpoint 4 only:
disposable-Site migration and controlled Set/intake/evidence/replay/rollback/
IDOR/route-disable proof, followed by complete ordinary CI and the P6-02 Level
2 Task Gate. The controlled Site may run only after its verifier and workflow
changes pass affected checks and complete ordinary CI. Diagnostics remain
closed unless a later exact controlled failure uniquely requires one governed
diagnostic cycle.
