# P8-01 Checkpoint 3 — Dense Trilingual Projection Truth

Recorded: `2026-08-16T06:22:00Z`

Decision: `PASS — CHECKPOINT 3`

Exact product implementation:
`9026477d806bb2af39dcbfd7a1c900c17285aa5a`

Exact final checkpoint:
`71bd18a610b685894ab2ed84df4a51a4306eacae`

Final ordinary pull-request CI:
`31913915708` (`PASS`)

## 1. Bounded outcome

Checkpoint 3 delivers only the frozen read-only projection presentation:

- a strict closed frontend data source for the seven projection kinds, exact
  Project containment, source/scope identity, bounds, ordering and explicit
  availability, freshness, synthetic, superseded and conflict truth;
- one dense Project projection table with keyboard-selectable rows and a
  docked read-only inspector, without an edit, refresh, replay or target
  execution action;
- exact current formal cost truth in the existing Tooling Manufacturing
  workspace only when the snapshot is available, fresh, authoritative and the
  applied current head;
- exact current formal Asset truth in the existing Tooling acceptance
  workspace only when the snapshot also matches the physical Set membership;
- honest loading, empty, denied, read-only, unavailable, stale, unknown,
  synthetic, conflict and error states, with formal values withheld whenever
  current target truth is not proven; and
- direct English, Simplified Chinese and Traditional Chinese copy, keyboard,
  focus, Axe, mixed-language and fixed-Linux governed visual coverage.

The UI receives only the NPI BFF response. It exposes no raw target error,
credential or caller-selected target identity, invents no freshness/EAC/
quality rule and does not turn a Mock, synthetic or transport result into ERP
truth. No webhook, target write, retry/DLQ/replay/reconciliation operation,
Trial Summary event, JCE Core behavior or production ERPNext/JCE contact is
introduced.

## 2. Diagnostic CI and bounded forward repair

Diagnostic pull-request CI `31913049429` ran at exact product SHA
`9026477d806bb2af39dcbfd7a1c900c17285aa5a`:

- repository `95080878077`, frontend `95080878111` and secret scan
  `95080878088` passed;
- visual `95080878067` passed `116/119` cases and failed only the retained
  P6-06 English, `zh` and `zh-TW` acceptance-Asset snapshots; and
- controlled lanes skipped as required for ordinary checkpoint CI.

Artifact `9254236401`, digest
`sha256:645ab591eb6376146786ecab0737fe673e12ca4784f1a5c4de2f469a274eb702`,
is diagnostic only. Review proved the retained P6-06 request
fixture aborted the newly added Project projection request. The product
correctly showed its localized network-error state; it did not silently reuse
or invent Asset truth.

Forward repair `71bd18a` changes only that retained P6-06 E2E fixture and the
exact current-task path guard. The fixture now returns the same closed empty
projection response that an unavailable projection boundary requires. The
existing three P6-06 images then match byte-for-byte; no product DOM, CSS,
copy, visual baseline, assertion, threshold, tolerance or PASS criterion was
changed. Exact Linux/amd64 Node `24.18`, npm `11.16` and Playwright `1.61.1`
verification passed all `8/8` affected P6-06 semantic/command/visual cases.

## 3. Final exact-SHA ordinary CI

Pull-request run `31913915708` completed successfully for event
`pull_request` at exact head
`71bd18a610b685894ab2ed84df4a51a4306eacae`:

- repository `95082933283`: `1,957/1,957` tracked Python tests plus current-
  task, repository and V1.2 reconciliation verification;
- frontend `95082933315`: `60/60` files, `933/933` unit tests, `426/426`
  non-visual E2E tests, `7,641` literal English sources with `100%` direct
  `zh`/`zh-TW` coverage, statements `80.36%`, branches `80.20%`, functions
  `83.00%`, lines `82.99%`, production build and zero vulnerabilities;
- secret scan `95082933287`: `68` committed current-task paths, `26` first-
  parent commits and `501` complete branch commits with no leak; and
- visual `95082933361`: the complete fixed-Linux matrix passed `119/119`,
  including the three new P8-01 languages and the unchanged P6-06 languages.

Controlled preflight `95084236381` and runtime `95084236378` correctly skip;
checkpoint 3 has not yet claimed the final Level 3 runtime Gate.

Visual artifact `9254446244` has digest
`sha256:b31632b5c9ba7081825ea239c67057effcc2fa9020db52b30142df50c2dcaaf0`.
Gitleaks artifact `9254406588` has digest
`sha256:ca2dfa295d3237b39b6fd8eea72e6bf646a5330ab3d22b040094bcff1b769fb9`.

The reviewed new P8-01 fixed-Linux snapshots hash as follows:

- English:
  `94f5a7e5013c05bf1b58ae94e59161464ca82ca657eb84efbbdce9f35b89a276`;
- Simplified Chinese:
  `acfeb821aa7d801f854b590981ab1efd356b02267a1ff4be7202c2c9f8208b6f`;
- Traditional Chinese:
  `e6d22ff040358ec716bf1972bf64ea2d2c75dfe892c7b4ef2469e227315c005a`.

## 4. Task-scope review

The checkpoint-3 range
`da98e92165a1f3db2755211f0e8fb19c41742d6d..71bd18a610b685894ab2ed84df4a51a4306eacae`
contains `22` files, `3,732` insertions and `27` deletions. Every committed
path belongs to the strict projection data source, existing Project/Tooling
workspaces, direct translations/generated catalog, focused fixtures/unit/E2E
tests, three reviewed Linux baselines or the exact task-path guard.

The changed-files to affected-tests map is direct: API parsers map to their
unit tests; Project and Tooling workspaces map to component tests; translations
map to catalog generation and complete direct-language audit; Project truth
maps to five P8-01 non-visual E2E cases plus three governed visuals; the
retained P6-06 fixture maps to its five semantic/command cases and three
unchanged language visuals. Complete ordinary CI proves the cumulative
repository/frontend/security/visual impact.

Existing unrelated dirty Makefile, README, development docs, `LAST_RUN.md`,
Phase 4/local evidence, public assets and local runtime files were not staged
or changed.

## 5. Security, rollback and transition

Project and tenant authority remain ahead of every secondary identity. The
client rejects extra kinds/fields, invalid dates, invalid scope/source pairs,
unbounded collections and formal values that are not exact current
authoritative truth. Status always has text and no color-only meaning.

Rollback disables only the independent P8-01 projection route and UI
surfaces, reports projection truth unavailable and uses reviewed forward
repair. It never deletes an observation, rewrites a source version/hash,
moves a head backward, changes unavailable into available or compensates in
ERPNext.

This evidence closes checkpoint 3 only. Standing continuous-delivery
authority activates only the final P8-01 Level 3 Gate: complete repository,
frontend, secret and visual verification plus cumulative disposable-Site
projection runtime, migrations twice, exact duplicate/reorder/conflict/
restart/IDOR/redaction/route-recovery truth, zero target write, zero production
traffic and cleanup. P8-02 remains inactive until that exact-SHA Level 3 Gate
passes. Production ERPNext/JCE contact remains prohibited.
