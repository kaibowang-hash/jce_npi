# GOAL — NPI One / 注塑新项目、Tooling 与 NPI 一体化平台

## 1. 大目标

在不替换现有 ERPNext 的前提下，建设一套独立、统一、易用的工程协同产品，覆盖：

`客户输入 → 可行性/立项 → DFM/设计基线 → Tooling 设计与制造 → T0–Tn 试模 → 样件/质量批准 → NPI 就绪 → ERPNext 正式执行 → SOP 移交与观察期`

产品应让项目经理、工程、模具、质量、工艺、采购和生产在同一项目上下文中协作，减少“单据找单据”、Excel、聊天记录和跨系统手工核对。

## 2. 成功状态

### 业务成功
- 客户来模、按图新制模具、改模/工程变更均有可配置但受控的流程模板。
- 项目、阶段门、设计版本、模具、试模、样件、缺陷、NPI 清单和 ERP 执行形成完整追溯链。
- 阶段门基于证据和阻断项，不靠口头确认。
- Tooling 是一级领域，不是项目任务上的几个字段。
- 试模按轮次锁定模具/产品/材料/参数版本，支持穴号、缺陷、样件、测量和轮次比较。
- 正式制造对象和执行交易仍由 ERPNext 主责。

### 体验成功
- 产品使用独立工业化 App Shell，不以 Frappe Desk 单据列表为主要界面。
- 视觉、布局和交互层级以 Siemens iX Classic Light / 经典西门子工程软件为唯一参考：单一工业深青主色、灰白中性色、方正边界、紧凑表格、对象树、分栏工作区和停靠式检查器。
- 禁止彩色卡片墙、渐变、玻璃效果、过多大圆角、强阴影和消费级“活泼”风格。
- “我的工作”统一呈现任务、审批、阻断项、风险、决策请求、同步异常和即将到期事项。
- “项目驾驶舱”在一个页面提供对象头、阶段轨、健康度、下一行动、项目结构、阻断项和上下文抽屉。
- 常见动作最多经过两次上下文切换；不要求用户记住 DocType 名称。
- 对象来源、同步状态、可编辑端和正式/工作版本始终清楚。
- 英文为唯一源语言；简体中文和繁体中文完全由同一 Frappe 翻译事实源产生。除受控术语、缩写、业务数据、编码和单位外，同一页面不得混用中文与英文。
- 高风险操作先展示影响范围，再确认；异步执行有排队、处理中、成功、失败和重试状态。

### 工程成功
- 前端、领域后端、ERPNext 和集成可靠性边界清晰。
- UI token、组件适配层、翻译适配层和术语表有自动检查，不能靠开发人员记忆。
- 共享对象采用稳定 `global_id` 和字段级主责；无双主冲突。
- 接口可幂等、可重试、可回放、可对账、可观测。
- 权限、审计、版本、文件哈希、状态机和迁移均有自动测试和发布门禁。
- 每个 Codex 任务范围小、可验证、可回滚。

## 3. 不在大目标中的内容

第一阶段不建设或不替代：
- ERPNext 的财务、销售、采购、库存、生产、成本核算和正式 QMS 交易；
- CAD 建模、CAM 编程、CAE 求解器本身；
- 完整 MES、设备实时采集和高级排产；
- 大型通用 PLM 的全部能力；
- 多系统自由双向同步；
- 只为“看起来功能很多”而增加的低频模块。

## 4. 系统定位

NPI One 是**工程与 NPI 协同系统**，ERPNext 是**正式经营与制造执行系统**。两者技术可同属 Frappe 生态，但业务系统、Site 和数据库独立。

- NPI One 主责：项目、门禁、工程交付物、设计工作版本/基线、Tooling 开发、试模、缺陷、NPI 就绪、协作和移交。
- ERPNext 主责：正式主数据、采购/库存/生产/质量/成本、模具资产和量产维护。
- ERPNext 端执行：由 NPI One 创建“执行请求”，ERPNext 校验并完成正式动作后返回结果。失败必须可见并可安全重试。

## 5. 里程碑边界

- **M0 发现与决策**：只勘察、画现状、识别现有 DocType/API、形成 ADR 和任务拆分；不开发业务功能。
- **M1 统一体验原型**：先冻结 Siemens 式视觉基线、Frappe i18n 基线，再完成 App Shell、我的工作、项目驾驶舱、模具开发驾驶舱、试模工作区、ERP 执行面板的高保真可测原型。
- **M2 领域与安全底座**：身份、权限、审计、状态机、领域 API、全局 ID、文件/版本基础。
- **M3 项目与阶段门**：项目模板、团队、任务/里程碑、交付物、门禁、风险问题行动决策。
- **M4 设计与基线**：文档版本、评审、发布包、设计基线、EBOM 工作版本和差异。
- **M5 Tooling**：客户来模、新制模具、改模、设计版本、制造里程碑、预算/供应商、验收。
- **M6 试模与 NPI**：T0–Tn、参数/样件/穴号/缺陷、质量关联、就绪度、试产和移交。
- **M7 ERPNext 可靠集成**：执行请求、正式对象发布、状态回写、Outbox/Inbox、重试/死信/回放/对账。
- **M8 变更/组合/外部协作**：影响分析、复审、组合视图、供应商/客户受限门户。
- **M9 加固与试点**：性能、安全、迁移、灾备、培训、真实双类型项目试点和上线门禁。

## 6. 第一条指令

收到本 Goal 后，Codex 只执行 M0：
1. 阅读本执行包和仓库。
2. 输出事实清单与未知项，不做臆测。
3. 形成现状架构、目标架构差距、现有 ERPNext 自定义对象/API 盘点。
4. 提交 ADR 草案、M1 的最小纵切任务清单、测试策略和风险清单。
5. 不创建完整前后端脚手架，不修改数据库，不实现业务功能。
